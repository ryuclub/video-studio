# projects/ — 结果

**这个仓库分两半：代码在各条线自己的目录里，成品全部落在这儿。**

| 类型 | 成品在这儿 | 代码在哪 | 稿件 / 文档 / 素材在哪 |
|---|---|---|---|
| **记者读稿** | `记者读稿/` | `src/` | `txt/`、`RUNBOOK.md` |
| **段子 + 一页故事** | `段子与儿童故事/` | `joke-video/src/` | `joke-video/jokes/`、`joke-video/*.md` |
| **老马**（碎嘴老马 · 段子） | `老马/段子/{_待发,_已发}/` | `joke-video/src/` | `joke-video/jokes/laoma-*.json`、`joke-video/horse/` |
| **说书**（聊斋） | `说书/` | `joke-video/src/shuoshu-*.ts` | `shuoshu/*.md`、`shuoshu/出片手册.md` |
| **治愈系旁白** | `治愈/` | `joke-video/src/zhiyu-*.ts` | `zhiyu/*.md`、`zhiyu/musics/` |
| **小故事大道理**（禅佛典） | `禅佛典/` | 同治愈（`--line 禅佛典`） | `zhiyu/禅佛典向_*.md` |
| **心理洞察** | `心理洞察/` | 同治愈（`--line 心理`） | `zhiyu/心理洞察*.md` |
| **醒木不响 · 交付** | `醒木不响/{_待发,_已发}/` | `joke-video/src/deliver.ts` | —— |

以前每条线的成品散在自己目录下（`joke-video/projects/`、`shuoshu/projects/`、
`zhiyu/projects/`、根 `projects/`）。找片子要翻四个地方，
`.gitignore` 也得为每条线各写一套规则。2026-08-20 收拢成现在这样。

## 醒木不响：上传的时候只开这一个文件夹

上面那几行是**项目目录**（稿件、音频、场景图、成片，什么都有）。
`醒木不响/_待发/` 是**交付包**：说书 / 治愈 / 小故事大道理三条线的片子收在一起，
一个包 = 一次上传，按名字排就是上传顺序。

```
醒木不响/
├─ _待发/2026-08-25_1800JST_说书_婴宁-E05/
│    婴宁-E05.mp4 · 婴宁-E05.srt · 封面-横版1280.png
│    封面-微信1080.png · 自检-小图.png · 发布文案.md · 来源.txt
└─ _已发/
```

`npm run deliver` 重建（幂等）；`shuoshu:ship` 跑完会自动收一次。

**`醒木不响/index.html` 是这一堆的门面**：左边一栏目录，右边从上到下是
统计 → **档期**（往后四周）→ **话题**（做过什么，取发布文案里的「中心思想」）
→ **稿件库**（还能做什么，现读四条线的选题文档）→ 待发 → 已发。成片能直接播，旁边是发布文案。档期和话题两块能折起来。双击打开就行。

它跟交付包一起由 `npm run deliver` 重建
（只想重出页：`npm run xingmu:index`）。

**那张页一个字段都不自己存**，全部现算 —— 状态看它在 `_待发` 还是 `_已发`，
类型看包名第三段，文案现读包里的 `发布文案.md`，档期跟 `npm run schedule` 同一份数据
（`schedule.ts` 的 `upcoming()`，**两处不各算一套**）。
所以**改页面改不到源头**：要改内容改 `发布.json` 重跑 `publish`，要排期给项目目录改前缀。

**里面是硬链接，不是拷贝。** 同一个卷上 `link()` 出来的是同一份数据 ——
62 MB 的 mp4 不会变成两份，发布文案改哪一边都是改同一个文件。

实测（2026-08-23）：两个路径 inode 相同、链接数 2；`du` 把四个目录一起量（按 inode 去重）
时交付包只剩 **89K**，`projects/` 总量含不含它都是 4.8G。**净增量约等于零。**
单独量交付包会显示几百 MB —— 那是重复计数，别拿那个数下结论。

⚠ **磁盘上不重，git 里会重。** 封面 / 字幕 / 发布文案在各自项目目录里已经跟踪过一遍，
包里再跟一遍就是同一份内容占两个路径，diff 里凭空多一倍噪声。
所以 `.gitignore` 里 **`projects/醒木不响/` 整棵树都不跟**，`index.html` 也不例外。

> 中间有过一版是「只放行 `来源.txt`」，理由是「目录结构本身是账本（哪个包排哪一档、
> 发没发），跟老马线的排期树一个道理」。**那个类比不成立**：老马那棵树里躺着成片本身，
> 而这儿每个包都是别处文件的硬链接，**账本那一层在源目录名里已经有了**
> （`2026-08-26_1800JST_治愈_…`）。所以整棵树都是本地产物，`npm run deliver` 随时重建。

⚠ 硬链接的另外两个性质，用之前知道：**删掉一边不会释放空间**（数据活到最后一个链接消失为止）；
**打包/复制整个仓库时，有些工具会把硬链接摊成两份真文件**。

**为什么不把项目目录整个搬进来**：一个说书项目目录里躺着 `audio/全片.wav`（115 MB）
和 60 张场景图，搬过去等于把中间产物搬进要上传的文件夹；而且发布文案里的
`../../../` 相对链接会集体指错（见下面那条）。

**为什么一个包不是一个项目目录**：治愈线一个项目目录里有好几条片子 ——
方丈记有上篇下篇，每篇还各有一个文字版（那是另一档产品）。四条片子四次上传，
就该是四个包。

**包名跟项目目录名是同一套**：`<日期>_<时刻>JST_<类型>_<稿件名>-<序号>`，
后面再跟成片名里多出来的那一截（`_全` / `_上_文字版` / `_全_烧字幕`）。
从包名一眼能回推到项目目录，反过来也一样。

> ⚠ **2026-08-24 修过一次。** `deliver.ts` 原来是拿「成片名削掉日期」当身份，
> 再拼上档期前缀。说书线的成片名本来就只有身份（`婴宁-E05.mp4`），
> 老治愈目录（`2026-08-19_hojoki`）削完也正好剩 `hojoki_上` —— 两边都躲过去了。
> **第一个用新目录名的治愈项目一进来就露馅**：成片名是
> `2026-08-26_1800JST_治愈_别人怎么看你-E01_全.mp4`，削掉日期还剩 `1800JST_治愈_…`，
> 拼出来是 `2026-08-26_1800JST_治愈_1800JST_治愈_别人怎么看你-E01_全`。
> 不报错，只是包名难看 —— **而包名就是那本账，看着不对就没人信它**。
> 现在身份取 `parseDir().slug`，老格式照旧退回「去掉日期就是身份」。

**没排档的叫 `未排期_<类型>_<名字>`**（跟老马线一个写法）。要排期**给目录改前缀**，
改完 `npm run schedule` 就认得。发完把整个包挪进 `_已发`。
**目录树本身就是账本，没有第二张表。**

## 改路径要改两个地方

- `src/config.ts` 的 `projectsDir` —— 记者读稿那条线
- `joke-video/src/paths.ts` 的 `OUT_*` —— 其余三条线

**别在别处再写路径字面量。** 这两个文件就是技术和结果之间的那道门。

## 为什么段子和《一页故事》在同一个文件夹

它们**共用同一条管线**：稿件都在 `joke-video/jokes/` 一个平目录里，
`preview.ts` 也把它们汇总成同一个画廊（`段子与儿童故事/index.html`）。
按内容拆成两个文件夹的话，画廊和稿件源就对不上了 ——
要拆得先把 `jokes/` 和预览页一起拆，那是另一件事。

## 发布文案的约定（2026-08-20 起的新片）

各期的 `发布文案.md` 是**复制源，不是分析报告**：打开就该整块选中、
粘到平台的输入框里。只出标题、简介（含章节）、关键字、文件路径四样，
外加发片前的核对清单。

**备选标题、关键字取舍、写法上的理由**放各条线的手册或各期 `方案.md`，
不进这份文档 —— 混在一起的话人一边滚一边挑，挑的时候就容易连说明一起复制进去。

治愈线已经改成**生成**的（`joke-video/src/zhiyu-publish.ts` 读各期 `发布.json`），
顺带解决了两个人工项：章节时间戳按段落定位自动算、片长去量成片。

**之前已出片的不回改**（说书 E01/E02、记者读稿那几期还是旧格式）。
新片按这套走。

## 版本库里留什么

**留**：稿件、`script.json`、`方案.md`、`发布文案.md`、封面 PNG、字幕 `.srt`、
`manifest.json` —— 这些要么是人写的，要么小且是发片的依据。

**不留**：成片、音轨、配音分句、场景图、逐帧素材 —— 全部可以重新跑出来，
加起来十几个 G。复现方法写在各条线的手册里。

`.gitignore` 里有一条**不要犯的错**：别写 `/projects/` 把整棵树排除掉。
那是重整前的写法（当时这儿只有记者读稿一条线），现在一刀切会把四条线的
稿件和方案一起吃掉，而且父目录一旦被排除，子目录的 `.gitignore` 想再放行也放行不了。
