# 序号卡（`steps`）—— 方案

**需求**：稿件里出现「一 / 二 / 三」「第一 / 第二 / 第三」这类序号陈述时，
用类似金句卡的样式把几条列出来，**逐条从下往上滑入**，并且做成通用能力。

> **实现状态（2026-08-17）**：L1 + L2 已落地并编译，`video-pipeline/demo.mp4` 已更新
> （末尾追加 6 秒的 `steps` 整屏卡，52.2s → 58.2s）。
> 不做：**L3**（PPT 风 `list` 改上浮）—— 2026-08-17 决定纯 PPT 风暂停使用。
> 未做：`npm run vg -- fx` 扫描命令、
> 以及把序号卡真正放进某条片子（第九节的三个候选还没选）。

---

## 一、结论：可行，而且上浮比左滑更该做

动效本身是白送的 —— `scenes.ts` 的 `quote()` 在 `typewriter: false` 时
走的就是「整行淡入 + 位移」，只是方向是左滑。改成上浮不是新增能力，是**改回主流**：

| 位置 | 现在的入场 | 方向 |
|---|---|---|
| `scenes.ts:128` `titleBlock` 标题/副标 | `dy = (1-a) × 24u` | **上浮** |
| `scenes.ts:267` `counter` 说明行 | `dy = (1-a) × 18u` | **上浮** |
| `scenes.ts:299` `timeline` 节点 | `dy = (1-a) × ±16u` | 上浮 / 下沉（向外扩散） |
| `scenes.ts:541` `japanMap` 城市标 | `dx = (1-a) × ±14u` | 横移（避让轮廓，有理由） |
| `scenes.ts:358` `quote` 正文行 | `dx = (1-a) × −30u` | **左滑 ← 全仓唯一** |
| `slides.ts:451` PPT 风时间轴 | `\move(…±22k…,0,300)\fad(300,0)` | **上浮** |
| `slides.ts:716` PPT 风数字说明行 | `\move(+26k → 0, 0,320)` | **上浮** |
| `slides.ts:352` PPT 风 `list` 条目 | `\fad(260,0)`，**无位移** | — |

上浮 + 淡入是这套东西的既有母语，两条管线都是。序号卡跟着上浮，一分钱额外设计成本都不用付。

顺带两个实际好处：

- 竖版面板只有 972px 宽，左滑 30u 的横向位移占比明显，多条同时动会「晃」；
  上浮的方向与「从上往下逐条累加」的阅读方向正交，落位更稳
- 卡片下方留白足够，不会被画布裁掉（见第五节的实算）

**唯一不能复用的还是 `quote` 本身**：它按字数硬折行（`wrapCJK`）、`text` 上限 80 字、
没有序号位、行入场时间按面板时长均分。SVG 的 `<text>` 不认换行符，
所以「把三条拼进一个字段」这条捷径不成立 —— **「条」必须是数据结构里的一等公民**。

---

## 二、上浮有两种做法，选固定槽位

- **(a) 固定槽位上浮** ✅ ——
  按 `items.length` 预分行高、从内容区顶部往下排好槽位，每条在自己的槽位下方 24u 处起浮。
  已入场的条目**一个像素都不动**。
- **(b) 堆栈推入**（新条从卡底进来，已有条整体上移）❌ ——
  每来一条整块都在动，观众正在读的字被抽走。四条就是抽三次。
  而且卡片高度得跟着变，`PANEL_ASPECT` 那套按宽高比定尺寸的算法直接失效。

选 (a)。这也是 `timeline` 那边的做法（节点位置固定，只有入场时那一下位移）。

---

## 三、「通用」分三层，建议只做前两层

### L1 · 入场方向抽成公共零件（SVG 管线内通用）

```ts
// svgRender.ts，跟 easeOut / stagger 放一起
export type EnterDir = 'up' | 'down' | 'left' | 'right' | 'none';
export function enter(a: number, dir: EnterDir, dist: number): string {
  const d = 1 - clamp01(a);
  const [dx, dy] = dir === 'up' ? [0, d * dist] : dir === 'down' ? [0, -d * dist]
                 : dir === 'left' ? [-d * dist, 0] : dir === 'right' ? [d * dist, 0] : [0, 0];
  return `transform="translate(${dx.toFixed(1)},${dy.toFixed(1)})" opacity="${clamp01(a).toFixed(3)}"`;
}
```

用 `<g ${enter(a,'up',24*c.u)}>…</g>` 包一整条（序号徽标 + 主文 + note 一起动），
比给每个元素单独算 `dy` 干净，也让「一条」在代码里成为一个整体。

`SceneSpec` 加可选 `enter?: EnterDir` 允许单个 overlay 覆盖方向。

> **老场景的默认值一律不动。** `CLAUDE.md` 明写「不要修改 `THEME` 以外的视觉参数」，
> 理由是系列片的视觉语言会漂移 —— `quote` 已经出过片，默认保持左滑，
> 想改的话在 spec 里显式写 `"enter": "up"`，改不改是一次可回退的决定。

### L2 · 场景本身通用（`steps` 不绑序号）

序号只是徽标里的**内容**，跟布局和动效无关。把徽标样式开出来，一个场景就覆盖三种卡：

| `ordStyle` | 徽标 | 用在哪 |
|---|---|---|
| `cn`（默认） | 一 二 三 | 稿件里的「一/二/三」「第一/第二/第三」 |
| `num` | 01 02 03 | 步骤、流程 |
| `circle` | ① ② ③ | 并列要点 |
| `dot` | 正红小方块 | 没有序号的要点清单 |
| `none` | 无 | 纯文本清单 |
| `check`（布尔） | 勾号 | 核对清单 |

`items[].ord` 可以逐条显式覆盖（稿件里写「其一/其二」也能照搬）。

### L3 · 跨管线对齐（PPT 风的 `list` 也上浮）—— ❌ 不做（PPT 风已暂停使用）

`slides.ts:352` 的 `list`/`checklist` 条目现在只有 `\fad(260,0)`，没有位移，
是两条管线里唯一还在「纯淡入」的清单。ASS 侧的上浮惯例已经现成
（`\move(x, y+RISE, x, y, 0, 300)\fad(300,0)`，`RISE = 22k`），
改动是给 `block()` 和 `box()` 各加一个可选 `rise` 参数，把 `\pos` 换成 `\move`。

**但两条管线共享不了代码**（一边生成 SVG，一边生成 ASS），只能共享参数。
所以 L3 的正确做法是**对齐数值、不做抽象**：把位移距离、时长、缓动写成两边各自的常量，
在文档里注明它们必须一起改。

---

## 四、一处定义的动效参数

| 参数 | 值 | 说明 |
|---|---|---|
| 位移距离 `RISE` | `24 × u`（SVG）/ `22 × k`（ASS 待做） | 跟 `titleBlock` 同值，全仓在 16–28 这个带里 |
| 单条时长 | 0.45 秒 | ASS 侧是 300ms + `\fad(300,0)`，视觉等价 |
| 缓动 | `easeOut`（cubic） | 管线默认，线性看起来廉价 |
| 相邻条最小间隔 | 0.15 秒 | 均分兜底时要夹住，否则四条挤成一团一起浮 |
| 末条留白 | ≥ 1.5 秒 | 最后一条不能在 `fadeOut` 里入场 |
| 已入场条目 | 不动、不退场 | 整卡由 overlay 的 `fadeOut` 收 |
| 正文字号 | `min(56u, 内容区高 / 条数 / 1.95)` | 只按基准算的话，五条卡在扁面板里会溢出衬底 |
| 当前条指示 | 最新一条徽标正红实心，之前的退成描边 | 免费拿到「现在念第几条」 |
| 左侧竖条 | 高度 ∝ 已入场条数 | 跟金句卡同位置同粗细，视觉上认亲 |

---

## 五、几何实算（会不会被裁掉）

以推荐位（访日客四条，横版 `scale 0.5`）为例：面板 `960 × 480`，
`u = clamp(960/1920, 0.7, 1.6) = 0.7` → `RISE = 24 × 0.7 ≈ 17px`。

内容区 `box.y0 = 0.255H = 122`，`box.y1 = 0.805H = 386`，四条行高 66px。
最后一条槽位在 y≈386，起浮点 y≈403，**画布高 480，不裁**。
竖版 `scale 0.9` → `972 × 486`，`u` 同样被夹到 0.7，数字几乎一致。

（`u` 在 `makeCtx()` 里下限 0.7，所以小面板不会无限缩字 —— 这也是 `overlay.ts`
对 `< 620px` 面板告警的原因。）

---

## 六、什么样的稿件算命中（已在现有四份稿子上实测）

```js
/^(?:第)?([一二三四五六七八九十])(?:条|点)?(?:[，,、。：:]|(?<=条))/
```

加三个成组条件：序号从「一」连续递增、**2–5 条**、
`duration = 末条 start − 首条 start + 4s` **≤ 30 秒**。

| 稿子 | 命中 | 判定 |
|---|---|---|
| 永住 · 四种情况（行 31–34） | 一/二/三/四 `164.3 → 199.0` | ✅ 28.8s |
| 永住 · 现在能做什么（行 38/40/41） | 第一/第二/第三 `221.3 → 252.1` | ✅ 27.5s |
| 访日客 · 提前准备（行 40–43） | 一/二/三/四 `155.1 → 179.2` | ✅ 21.5s |
| 永住 · 三条要件（行 7/10/13） | 第一条/第二条/第三条 `27.0 → 74.1` | ❌ 跨 47s，不做卡 |

零误命中：`第二天，` `第二年，` `战后第一次` `别表第一之二` `第七条第一项第二号` 全被挡住。

---

## 七、场景定义

```jsonc
{ "kind": "steps",
  "title": "有四种情况要留神",
  "ordStyle": "cn",                  // cn（默认）/ num / circle / dot / none
  "enter": "up",                     // 可选，默认 up
  "items": [
    { "text": "转职当年的年收洼地", "note": "看的是持续性" },
    { "text": "自由职业 · 个人事业主" },
    { "text": "派遣与 SES" },
    { "text": "配偶进入扶养 · 育儿假" }
  ],
  "itemAt": [0, 6.9, 16.6, 24.8]     // 各条入场，相对面板起点的秒数；不给就均分
}
```

`itemAt` 从 `timeline.json` 对应解说行的 `startMs` 减去面板 `at` 得到，
跟现有四个动效取时间的做法一致。`FrameFn` 的第一个参数 `t` 就是面板内秒数
（`svgRender.ts`：`const t = i / o.fps`），按秒锚定是现成能力，渲染层不用改。

硬约束（写进 `validateScene.ts`，违反直接中断）：

- `items` 2–5 条；1 条该用 `quote`，6 条以上读不完
- `text` ≤ 14 字（超了在竖版必折行，一折行整卡行高就乱）
- `note` 仅在 `items ≤ 3` 时允许，≤ 22 字
- `itemAt` 若给：长度等于 `items`、非负、严格递增；最大值 `< duration − 1.5`（这条在 `overlay.ts` 查，scene 拿不到 duration）

---

## 八、要动的文件

| 文件 | 改动 | 估时 |
|---|---|---|
| ✅ `video-pipeline/src/layers/svgRender.ts` | 加 `enter()` 公共零件（`EnterDir` 放在 types.ts） | 已完成 |
| ✅ `video-pipeline/src/types.ts` | `SceneKind` 加 `'steps'`；`SceneSpec` 加 `items` / `ordStyle` / `check` / `itemAt` / `enter` | 已完成 |
| ✅ `video-pipeline/src/layers/scenes.ts` | 加 `steps()` + `badgeSvg()`，注册进 switch；`quote` 改走 `enter()`（默认值不变） | 已完成 |
| ✅ `video-pipeline/src/layers/validateScene.ts` | 加 `case 'steps'` 校验（7 条负例实测都拦住了） | 已完成 |
| ✅ `video-pipeline/src/overlay.ts` | 新增 `aspectOf()` 按条数算宽高比（`2.5 − 0.35×(n−2)`）；`itemAt` 越界检查 | 已完成 |
| ✅ `video-pipeline/CLAUDE.md` / `README.md` | 六种场景 → 七种；密度优先级加 `清单`；入场方向写成明文约定 | 已完成 |
| ✅ `RUNBOOK.md` | 动效一节补识别规则与实测命中表 | 已完成 |
| **L3（可选）** `src/lib/slides.ts` | `block()` / `box()` 加 `rise` 参数，`layoutList` 用上；`_ppt` 那条重渲 31 秒 | 45 分 |

`render.ts` / `footage.ts` / `tts.ts` / `ass.ts` 一行不动 —— 动效是叠加层，底片和配音不重跑。

**另计 1 小时（可选）**：`npm run vg -- fx <项目名>` 扫 `script.json` + `timeline.json`，
把命中的序号组连同 `at` / `duration` / `itemAt` 吐成 overlay 骨架，**文案字段留空由人填** ——
跟 `series` 留空同一条规矩：稿件里那句话是 20 多字的口语，压成 14 字的卡面文案只能人来。

---

## 九、放到哪条片子

- **① 访日客 · 提前准备四条（`155.1 → 179.2`，21.5 秒）—— 推荐先做这个**
  这条片子还没叠任何动效，不用跟谁抢窗口。四条内容也最适合上卡：
  办公空间 / 资金来源 / 收支预测 / 2 名·500 万，纯清单，空镜完全无能为力。
- **② 永住 · 四种情况（`164.0 → 194.0`，30 秒）** —— 就是 `PANEL-PLAN.md` 第 ③ 处那个位置，
  但要**替换掉 `ov3-points`**（199.5–209.2 的积分 counter）：两者只隔 0.5 秒，
  且 counter 已用到同类上限 2 次。换掉后四个动效间隔变成 49s / 93s，很干净。
- **③ 永住 · 现在能做什么（`221.3 → 248.8`，27.5 秒）** —— 内容更适合上卡，
  但保留 `ov3-points` 的话只隔 12.1 秒，会触发密度告警。要做就得二选一。

②③ 要改已成片的 `overlays.*.json`，各重跑一次 `overlay-onto`（横竖两版，底片各重编码一次）。

---

## 十、风险与取舍

- **停留时间长**：四条念完 25–30 秒，卡得一直挂着；现有最长的 `ov2-timeline` 才 20.5 秒。
  缓解：卡面只放主文不放 `note`，横版 `scale` 收到 0.5（现有是 0.6–0.74），
  让它更像一块图注而不是盖住画面
- **和底部字幕重复**：卡面文案必须是解说的压缩版，照抄那一行等于让观众读两遍同样的字
- **序号规则会漏**：「首先 / 其次 / 最后」这类没有数字的序列不在识别范围内，要人工标
- **L1 的克制**：`enter()` 抽出来之后很容易顺手把所有场景都改成上浮。
  别改 —— `japanMap` 的城市标横移是为了避让轮廓，`timeline` 的上下交错是为了防止文字打架，
  它们的方向都是有理由的，不是没统一
