# 视频制作可视化方案

> **这一份是唯一正文。** 它合并了三份：
>
> | 原文 | 2026-08-26 之后 | 它贡献了什么 |
> |---|---|---|
> | `网页版方案.md`（08-22 定稿，44 人日） | **只作留档** | 三件事实 · 写稿台全套 · 类型推荐 · 依赖图 · 五种稿件形态 |
> | `项目封装方案.md`（分析记录） | **只作留档** | 现状盘点 · 画面来源四种 · ABC 三级 · 参数化 · rig 契约 |
> | `可视化界面方案.md`（界面＋计划） | **只作留档** | 五页界面 · 三个跨页机制 · 去重后的任务表 |
>
> **那三份别再照着做** —— 它们互相之间已经在打架（§〇）。
> Skill 那一块也并进来了（§十一），因为它跟界面共用同一个底座：**步骤登记表**。
>
> **合计 65.5 人日，分八期。** 剪辑、转场、多用户挂起（§十四）。

---

## 〇、为什么必须合：三份已经在打架

| 冲突点 | 网页版说 | 可视化界面说 | **合并取** |
|---|---|---|---|
| 页面数 | 6 页（写稿台／封面台／工作台／稿件库／视频库／音频库） | 5 页（风格台／素材台／制作台／工作台／库） | **7 页**（§四）—— 封面台并进风格台和制作台 |
| 工作量 | 44 | 31.5（另一份写 19.5） | **65.5**，去重后合并**不是相加**（§十二） |
| 落地顺序 | 11 步 | 7 期 | **8 期** |
| 架构模块 | 6 个 | 5 个 | 一份（§十） |
| 封面脚本能不能 import | 「四个只能 spawn，各加 `--out`」 | 「六个里只有一个」 | **以复核为准**：只有 `xinli-cover.ts` |
| 参考图那张四栏表单 | 「不是一种产品，不能照做」 | 「照做，但换选项」 | **以后者为准**（§二） |
| 规范能不能在界面上改 | 没讨论 | ABC 三级 | **ABC 三级**（§三） |

> **三份文档写同一件事 ＝ 三个真相在等着分叉，而且已经分叉了。**
> 这仓库为「抄一份」栽过至少三次（`cpm`、绳结经验、标点规则）——
> 方案文档也一样。

---

## 一、三件不能绕开的事实

### 1.1 管线是重本地的，搬不上云

96 处 ffmpeg 调用、本机字体（缺了不报错只是变细）、resvg 原生模块、
`assets/cache/` 2.8 GB、一版片子 10–21 分钟。**所以是 `localhost` 服务。**

**先例已经在跑**：`voice-clone/src/server.ts`（117 行，`:5178` 试听页）、
`preview.ts` 生成的静态画廊。**新服务是把这两个收编，不是从零起。**

### 1.2 管线全是 CLI ＋ 文件产物，所以加壳比重写便宜一个数量级

六条线、72 个入口、42 个 npm 命令，每一步都是「吃文件吐文件」，没有常驻状态。
网页层只需 spawn 子进程、推日志、扫产物。
**不把管线改造成可调用的库** —— 收益低风险高。

### 1.3 库不需要数据库

`projects/<类型>/<期>/` 已经是事实来源。稿件库／视频库／素材库是同一次扫盘的三个视图。
**不引入 sqlite** —— 库和盘一分叉就得写同步，**而磁盘永远是对的那一边**。

---

## 二、现状盘点：能力比想象的多

### 2.1 ⚠ 画面来源是一个**维度**，已经有四种实现

「视频拼接方式」不是要新建的功能，**是把已经存在的四条路径收进一个下拉框**。

| 画面来源 | 现有实现 | 谁在用 | 成熟度 |
|---|---|---|---|
| **系统自动勾勒** | 六个 SVG 渲染器 | 除记者读稿外五条线 | ✅ 出过几十条片 |
| **自动抓取素材** | `src/steps/footage.ts` → Pexels，按 `clip` 关键词，330 行 | 记者读稿 | ✅ 缓存 **269 条 / 2.8 GB** |
| **提供素材** | `assets/manual/<keyword>.mp4`，**命中即整组覆盖检索** | 记者读稿 | ✅ 16 条 |
| **叠加到已有底片** | `video-pipeline/` 的 `assemble` ＋ `overlay`，**底片不动、音轨原样搬运** | 动效面板 | ⚠ 有实现，demo 素材缺 |
| **转场** | — | — | ❌ 真没有 |

**连「AI 根据文案生成视频关键词」都已经有了** —— `src/steps/script.ts` 的 SYSTEM
就在教模型出 `clip`（英文 2–4 词），还带三条踩出来的硬规矩。
实测一条 49 句的稿子里 22 行带 `clip`。

> **所以参考图那张四栏表单，跟这仓库是同一个形状** ——
> `记者读稿` 本来就是「AI 出文案 ＋ AI 出关键词 ＋ 抓素材 ＋ 拼接 ＋ TTS ＋ 字幕」。
> **照做，但每个下拉框的选项换成仓库自己的能力。**

⚠ 四种不平级：前三种是「这一镜的画面从哪儿来」，第四种的**输入是成片不是稿子** ——
属于二次加工，界面上不并列。

### 2.2 素材库已经很厚

| 库 | 现状 | 在哪 |
|---|---|---|
| 字体 | 10 个文件 **170 MB** | `fonts/` ＋ `FONT_FILES` |
| 音色 | **24 个具名**（含变声链）＋ **58 条预设** ＋ 音染模块 | `cast.ts`、`voice-clone/presets.ts` |
| 视频素材 | 手动 16 ＋ Pexels 缓存 **269 条** | `assets/manual/`、`assets/cache/` |
| 环境音 | 9 条（**电平差 20+ dB，脚本自己量**） | 仓库根 `voice/*.mp3` |
| 画面素材 | rig ＋ 摆件 13 ＋ 符号 18 ＋ 插画 | `horse/`、`_symbols/`、`zhiyu/pictures/` |
| 场景 | **26 个具名**（段子 10 ／ 老马短片 10 ／ 长片 6） | `scenes/index.ts`、`horse/scenes.mjs`、`longform.cjs` |

### 2.3 参数面已经很大

**稿件级约 100 个字段，逐句级 28 个。** 标题／副标题／署名／音色**都已经是参数**。
覆盖链已经是三段：`代码常量 → 环境变量 → 稿件字段`。
**缺的只是把最前面那段换成「从频道包读」—— 不是重构，是把常量搬个家。**

### 2.4 ⚠ 六条线里已经有两条把步骤登记表写成了代码

`joke-video/src/ship.ts:97` 和 `shuoshu-ship.ts:59` 各有一个 `STEPS`。
两份形状不一样，但是同一件东西。
**所以登记表不该手抄，该被导出**（§十一）。

---

## 三、三个核心判断

### 3.1 规范作为默认值 —— 分三级，而且仓库自己已经是三级的

| 级 | 谁的约束 | 界面行为 | 代码里现在的样子 |
|---|---|---|---|
| **A · 平台事实** | 平台 | **灰掉 🔒**，跟着选的平台变，不跟用户变 | `SAFE`，越界 `level:'error'` |
| **B · 物理事实** | 可读性／听感 | 能改，**越界必报 ⚠**，落留痕 | `minFs:109`、`ensure()` WCAG 3.2、静场 ≤1.5s |
| **C · 频道预设** | 醒木不响自己的 | 随便改，**但要留痕** | `LAYOUTS`、`T`、`ACCOUNT_LAOMA`、`CASTS` |

> **界面暴露的不是「参数」，是「这个参数属于哪一级」。**

⚠ **`laoma-long-cover.ts` 的 `L` 对象里，`minFs:109` 是 B 级、`centerK:0.46` 是 C 级 ——
它们现在在同一个对象里。参数化的第一步不是搬，是先把 B 和 C 分开**；
混着搬，用户就能把物理事实调坏。

⚠ **最大的风险不是「能不能改」，是「改了不留痕」。**
这仓库每条规范都带着「为什么」（绳结被切掉、引号被删光、字距边只剩 5%），
**界面上一个下拉框不会带那段理由**。所以两条配套是必须的：
**① 偏离时显示那条规范的原话；② 落进 `<项目>/.ui-overrides.json`。**

### 3.2 风格 = 默认值包

> **一个风格 = 一整套默认值 ＋ 一个渲染器 ＋ 一套体检闸 ＋ 一个封面版式库。**
> 选了风格，四栏的默认值全部被填好；界面上剩下的是「这一期哪儿不一样」。

⚠ **不能做成扁平下拉框。** 「线 → 线内体裁 → 版式」是三层，
上一层定了下一层的选项才成立（治愈线没有 `type:A/B`，段子线没有「上下分期」）。
**界面上是三级联动，数据上是一棵树。**

### 3.3 能被机器查的做控件，只在文档里的只做提示

| 规范 | 在代码里吗 | 界面能做什么 |
|---|---|---|
| 「4–7 字」「字号下限 109」「安全区 10%」 | ✅ 有断言 | **控件**，越界报错 |
| 「大字是钩子，不是书名」 | ❌ 只在 md | **只显示原话** |
| 「答不上来就是没写好」 | ❌ 且明写着机器查不了 | **四个必填的填空** |

> **判据：能被 `*-check.ts` 查的 → 控件；只在文档里的 → 提示。**
> 把只在文档里的规矩做成控件，等于给了它一个「可以调」的错觉。

---

## 四、页面（7 页，合并了两种切法）

| # | 页 | 干什么 | 写盘 |
|---|---|---|---|
| **P1** | **风格台** | 场景画风墙（26 个）＋ 封面版式墙（**连 `cost` 一起显示**） | ❌ |
| **P2** | **素材台** | 字体／音色（含试听 ＋ AB）／视频素材／环境音／画面素材（含 rig 上传） | 只写 `uploads/` |
| **P3** | **制作台** | 风格三级联动 → 四栏 → 封面实时预览 | 只写 `.ui-*.json` |
| **P4** | **写稿台** | 五种稿件形态的编辑器 ＋ 右栏三层校验 ＋ 局部重写 | **写稿件** ⚠ |
| **P5** | **工作台** | 竖进度条 ＋ 日志 ＋ 产物 ＋ **人工关卡** | 触发管线 |
| **P6** | **库** | 稿件库／视频库，同一次扫盘 | ❌ |
| **P7** | **音频台** | 并入 `:5178`：音色预设／变声链／AB 对比 | ❌ |

**P1 P2 P6 P7 全是纯读** —— 排在前面，档期跑着也能做。

---

## 五、P1 风格台 · P2 素材台 · P3 制作台

### 5.1 风格台

场景数据来自 `npm run scene -- <name>`（**不需要稿件**）和 `longform.cjs` 的 `SCENES`（可 import）。
版式数据来自 `LAYOUTS`，**连 `cost` 一起显示** —— 版式库每一档都必填代价。

⚠ **说书／治愈／心理／禅佛典的场景跟稿件绑，不能空跑**
（`shuoshu-scene` 要 `chapters`，`zhiyu-scene` 要 manifest）——
每条线要一份**只为出图用的样例稿**。不给的话那四格是空的，
**而空白比「没有这个功能」更让人以为是坏了**。

### 5.2 素材台

⚠ **两条必须显示在界面上的状态**：

1. **`ffmpeg` 缺 `rubberband` → 变声静默降级**，试听到的跟出片出来的**不是一个人**。
   `doctor.ts` 已经在查，**界面要挂在音色标签顶上**
2. **共享音色只读** —— 界面上**只有「基于 XX 新建」，没有「编辑」**。
   一个人改了共享音色，**所有人的片子都变音**

**rig 上传** = `rig.svg` ＋ `manifest.json`（viewBox／headCx／depth／eyeClamp／双眼中心／口型中心）

契约是现成的（`horse/bull-side/BULL_HANDOFF.md`），**而且被复制过一次** —— 老牛就是照老马做的。

⚠ **但有两个契约，轻重差一个数量级**：
长片渲染器只要 `viewBox` ＋ 五个 `#mouth-*` id（**传上去就能用**）；
短片渲染器要完整分组 ＋ **六个支点坐标**，而那六个数**硬编码在 `horse-pose.mjs` 里**
（`HEAD_CX=352`、`DEPTH=46`、clamp ±26/±22）。
**证据：老牛在短片稿件里是 `rig:'none'`** —— 他进不了短片渲染器，
不是画不出来，是没人把他的支点搬进驱动。

**checker 查（全可判定）**：viewBox 四个数／五个口型 id 齐／默认只显示 closed／
眼瞳分组齐／外接框裁紧／**分渲 `closed` 和 `A` 比 PNG 字节数**
（「不报错只是没变」是这条管线最常见的失败）。

⚠ **checker 查不了那六个支点对不对** —— **上传完立刻渲一张场景预览让人看**。

### 5.3 制作台

```
┌ 风格（三级联动）─────────────────────────────┐
│ [老马 ▾] › [长片 ▾] › [横版 ▾]               │ ← 定了它，下面全部填好默认值
└──────────────────────────────────────────────┘
┌ 文案 ───────┐┌ 画面 ────────┐┌ 音频 ───────┐┌ 字幕 ───────┐
│ 主题/选题    ││ **画面来源**▾││ 音色 ▾ 24    ││ 字体 ▾    C │
│ ✦AI 生成文案 ││ ·系统勾勒 ✓  ││   试听 ▷     ││ 字号/色/描边 C│
│ 稿件（可贴） ││ ·自动抓取    ││ 语速（线定） ││ 标点规则 ▾ C│
│ ✦AI 出关键词 ││ ·提供素材    ││ 环境音 ▾ 9   ││ 断行 ▾    C │
│ clip（英文） ││ 场景 ▾       ││ BGM / 电平   ││ 安全区  A 🔒│
└──────────────┘└──────────────┘└──────────────┘└─────────────┘
┌ 封面（实时，<0.3s）─────────────────────────┐
│ 版式档 ▾  ▣210px ▣200px ｜ ▢16:9 ▢1:1       │  ← **主位是缩略图**
│ ✓字数 ✓高度 ⚠压到脸 ✓安全区 ✓不剧透说破段   │
└─────────────────────────────────────────────┘
```

⚠ **主位是缩略图，大图次位。** 规范原话：**大图好看小图认不出等于没有**
（记着两次翻车：井口第一版被看成气球、竹简像相机镜头）。

**类型推荐**：规则在前，LLM 只判「古今顺序」和「题材调性」两件数不出来的。
⚠ **推荐器必须跟体检用同一个字数口径** —— 直接复用 `*-check.ts --json` 的计数，
**绝不自己数一遍**（实测 `page-01-turtle.json`：台词 267 汉字，整份 json 中文字符 1322）。

---

## 六、P4 写稿台

### 6.1 六条线的稿件是五种形态

| 形态 | 线 | 落在哪 | 编辑器 |
|---|---|---|---|
| **A** 结构化 JSON | 记者读稿 | `script.json` | 表格式，一行一句，字段是列 |
| **A′** 结构化 JSON（更重） | 段子 · 一页故事 · 老马 | `jokes/<id>.json` | 同上，`cast`/`delivery` 从 `cast.ts` 拉选项 |
| **B** 散文 md | 说书 | `shuoshu/<id>.md` | 文本 ＋ 右栏活体检 |
| **C** 多文件 md | 治愈 | `幕表.md` ＋ 四份幕 | 同上 ＋ **幕表是配额表，实时算** |
| **C′** 单文件 md | 心理 · 禅佛典 | `正文.md` | 同上，比 C 简单 |

### 6.2 ⚠ 改稿是**局部重写，不是整篇重生成**

> 「开头 637 字是样章的原文，**一字未改** —— 那是听过并通过的调性和节奏。」

**整篇重生成会把已经通过试听的部分冲掉。** 所以四个动作：
**局部重写**（选中一段 → 出 3 个候选 diff 三选一）／**锁段**（试听通过的不许碰）／
**提为落点**／**版本**（不覆盖，可回滚）。

### 6.3 右栏三层

**① 文本闸**（机器，实时，红波浪线）／**② 数值**（提醒不是拦截）／
**③ 人味儿**（**只有人**）—— 做成**四个必填的填空**，不是复选框（复选框会被随手勾掉）：

```
① 具体到不能替换的那个人和事，是哪一个？   [__________]
② 听众带走的那句话，抄在这儿：             [__________]
③ 你自己也没想通、并且说出来了的那一处：   [__________]
④ 只可能出自这个人的那一句：               [__________]
```

**四格填不满，「定稿」按钮是灰的。**

### 6.4 ⚠ 定稿写盘这一下最危险

> `zhiyu-ep.ts` 原话：「忘了改哪一个，那一步就把新书的产物写进方丈记的目录 ——
> **这是数据损坏，不是麻烦**」

**写盘前先把旧文件挪进 `.web-versions/<时间戳>/`；期号冲突要拦，不给默认值、不猜。**

---

## 七、P5 工作台

```
体检 ✓ → 配音 ✓ → 试听 ▷ → 静帧 ▣ → 封面 ▣ → [出片]
                    ↑人确认   ↑人确认  ↑人确认    ↑三格没点＝灰
   每步：[重跑][日志][产物]        日志 SSE 实时滚
```

`gate` 三档：`auto` ／ `blocking`（退出码非零就停）／ `human`（人点过才解锁）。

⚠ **「出片」是「从当前这一步往下跑」，不是「从头一键到底」** ——
长片渲 **485 秒**、说书一期 10–21 分钟，一个大按钮会让人拿八分钟的渲染当调试。

**依赖图**：`RUNBOOK` §二 那张「改了什么要重跑哪一步」变成真的图。
两个坑做成红点：**`long-parse` 会冲掉 `align` 回填的 `dur`**（不报错，只是嘴和声音差着）、
**换了 BGM 必须手动删 `voice_mix.m4a`**。

---

## 八、三个跨页机制

### 8.1 默认值三层

```
平台包（共享只读）   platforms/youtube.json · 抖音 · B站 · 微信
        ↓
频道包（用户自己的） channels/<频道>.json   ← 现在散在代码里的常量搬进来
        ↓
稿件（单期覆盖）     约 100 个字段          ← **已经有了**
```

### 8.2 人工关卡

`试听` `静帧` `封面缩略图` 三格是 `human`。**没点过，出片按钮是灰的。**
这是 `RUNBOOK` §五「只有人能查的」从文档变成界面的那一下。

### 8.3 ⚠ 字节级回归 —— 参数化唯一的安全网

**最大的风险不是做不出来，是搬完之后片子不一样了而且没人发现**
（这条管线大多数错误不报错，只是结果不对）。

```bash
npm run regress -- --record   # 建基线：老马长片001 / 老马短片 / 说书E05 / 治愈taoan-E01
npm run regress               # 每个碰管线的任务做完跑一次
```

**判据：频道包填现在的值，重跑已出片的期，产物逐字节一致。**
这招 2026-08-26 用过 —— 改了 `longform.cjs` 的 `frame()` 之后 `md5sum -c` 验了
7 张样张逐字节一致，那是「画法一个像素没动」唯一站得住的证据。

---

## 九、明确不做（合并三份）

| 不做 | 原因 |
|---|---|
| 云端渲染 / SaaS | 本地依赖搬不动 |
| 多人协作、权限 | 加进来只是负担 |
| 把管线重构成可调用的库 | 96 处 ffmpeg、72 个入口，收益低风险高 |
| sqlite / 任何数据库 | **磁盘永远是对的那一边** |
| 并发渲染 | ffmpeg 吃满 CPU，并发只会都变慢 |
| **一键从需求直接到成片** | 会把「build 是交付动作」冲掉。按钮可以有，**人工关卡挡在前面** |
| **通用旋钮** | 六条线的参数不通用。**每个下拉框的选项必须是仓库自己的能力** |
| **界面改共享音色 / 基础库** | 一个人改了，**所有人的片子都变音** |
| 整篇重生成 | 会冲掉已经通过试听的段落 |
| 扁平的「视频类型」下拉框 | 会出现「治愈线 + type A」这种不存在的组合 |
| 推荐器自己数字数 | 跟体检口径分叉，**比不推荐更糟** |
| 把 A 级（平台事实）做成可调项 | 那不是偏好，改了就是错 |
| 偏离默认值不留痕 | 规范带着「为什么」，界面不会带 |
| **每个闸配一个「忽略」按钮** | **天天报错的闸只会教人加 `--anyway`**。闸的**阈值**可配、**存在与否**由频道包声明 |

---

## 十、架构与隔离纪律

```
浏览器  Vite + React + TS
  │  REST（数据）+ SSE（日志）
  ▼
web/  :5180   ← 独立 workspace，加进根 package.json 的 workspaces
  ├─ script/     写稿台：adapters(A·A′·B·C·C′) · prompts · classify · llm · versions
  ├─ preview/    封面（import 拿 Buffer）· 场景（spawn/import）
  ├─ assets/     素材扫描 · rig checker · uploads
  ├─ steps.ts    ← 从 joke-video/src/steps.ts **import，不抄**
  ├─ runner.ts   spawn tsx，单并发，日志落盘 + SSE
  ├─ library.ts  扫 projects/ + jokes/ + shuoshu/ + zhiyu/（mtime 缓存）
  ├─ gates.ts    人工关卡，落 <项目>/.web-state.json
  └─ media.ts    mp4/wav，**必须支持 Range 206**
  ▼
现有管线   ← **单向依赖：管线永远不 import web/**
```

**⚠ 不另开仓库，理由只有一条：常量。** 界面必须读 `paths.ts` / `config.ts` /
`cast.ts` / `zhiyu-lines.ts` 的 `cpm` / `LAYOUTS` / `T` / `FONT_*`。
**跨仓库就只能抄一份**，而这仓库为「抄一份」栽过至少三次。
**独立仓库能买到的隔离，workspace ＋ 单向依赖同样买得到。**
先例：`voice-clone` 就是这么跑的。

| # | 隔离纪律 | 怎么验 |
|---|---|---|
| 1 | 管线不 import `web/` | `grep -rn "from '.*web/" src/ joke-video/src/` **必须为空**。删掉 `web/`，出片一行不受影响 |
| 2 | 默认值只读不抄 | web 里出现硬编码的 `150` `0.062` `#FFD400` 就是 bug |
| 3 | 前端依赖只进 `web/package.json` | React/Vite 不出现在根和 `joke-video` 的依赖里 |
| 4 | **CLI 永远可用** | **网页坏了不影响出片 —— 这是它能被信任的前提** |
| 5 | 管线只加两个 flag | `*-check.ts --json`、`xinli-cover.ts --out`。**只加分支，不改逻辑** |

---

## 十一、底座：步骤登记表（也是 Skill 的底座）

**一份数据，三个消费者**：工作台的进度图 ／ Skill 的 `references/lines.md` ／
`ship.ts` 和 `shuoshu-ship.ts` 自己。

```ts
// joke-video/src/steps.ts —— 唯一的登记表
export interface Step {
  key: string; desc: string;
  cmd?: string; script?: string; args?: string[]; out?: string[];
  gate: 'auto' | 'blocking' | 'human';
  hint?: string;      // human 那一档：让人确认什么
  verified?: string;  // ← **验过没有，谁验的，哪天**
}
```

- `ship.ts` 和 `shuoshu-ship.ts` **改成从这儿 import**，各自的 `STEPS` 删掉
- 另外四条线照着补
- `--md` 生成 Skill 的 reference，`--json` 给网页版

⚠ **`verified` 那一栏是这张表的全部价值。** 抄进来容易，**逐条真跑一遍验证难**。
现状：老马长片 ✅（2026-08-26 全程走过）／老马短片 ⚠／说书 ⚠／段子 ⚠／
记者读稿 ❌／治愈·心理·禅佛典 ❌。

### 11.1 Skill（`.claude/skills/vidgen/`）

**给 Claude 用的，不是给人用的** —— 让下一次对话不用重读 48 份、1.3 MB 规范。

**只做路由，不复制规范**（规范会变，复制一份就是第二个真相）。
`references/lines.md` **由登记表生成，不手写**。

---

## 十二、制作计划

### 12.1 任务表（去重后的唯一一张）

| # | 任务 | 人日 | 依赖 | 碰管线 | 状态 |
|---|---|---|---|---|---|
| B1 | 服务骨架 ＋ 任务队列 ＋ SSE 日志 | 2 | — | ❌ | ☐ |
| B2 | Windows 坑（中文目录／路径空格／CRLF） | 2 | — | ❌ | ☐ |
| B3 | 媒体服务（Range 206 ／ 缩略图 ／ 波形） | 1 | B1 | ❌ | ☐ |
| B4 | **回归基线**（4 期锁 md5，`npm run regress`） | 0.5 | — | ❌ | ☐ |
| L1 | 库扫描 ＋ 三视图 API（mtime 缓存） | 2 | B1 | ❌ | ☐ |
| L2 | 稿件库 ＋ 视频库前端 | 2.5 | L1 B3 | ❌ | ☐ |
| L3 | 素材台（字体／视频／环境音／画面素材） | 2 | B1 | ❌ | ☐ |
| L4 | 音频台（并 `:5178` ＋ 环境状态条） | 1.5 | B1 | ❌ | ☐ |
| P1 | 封面预览（实时 ＋ 版式档可换 ＋ 四尺寸） | 2 | B1 | ❌ | ☐ |
| P2 | 场景画风预览（26 个 ＋ 四条线样例稿） | 1.5 | B1 | ❌ | ☐ |
| S1 | **步骤登记表**（导出 2 ＋ 补 4 ＋ 逐条验证） | 3.5 | — | ⚠ 只导出 | ☐ |
| S2 | Skill | 2 | S1 | ❌ | ☐ |
| S3 | 拆开 B 级／C 级常量 | 1 | B4 | ✅ | ☐ |
| S4 | 频道包 schema ＋ 导出并断言一致 | 1 | S3 | ✅ | ☐ |
| S5 | 代码改成读频道包（常量留 fallback） | 2 | S4 B4 | ✅ | ☐ |
| S6 | 音色两层（基础库只读 ＋ 频道表继承） | 1.5 | S4 | ✅ | ☐ |
| S7 | 平台包（遮挡区／比例／时长） | 1 | S4 | ✅ | ☐ |
| M1 | 风格 → 默认值包（三级联动 ＋ ABC 分级） | 3 | S4 S7 | ❌ | ☐ |
| M2 | 四栏制作台（预览 ＋ 偏离留痕） | 3 | M1 P1 | ❌ | ☐ |
| M3 | 类型推荐器（规则 ＋ LLM 兜底） | 2.5 | W4 | ❌ | ☐ |
| R1 | 工作台前端（进度条／日志／产物） | 2 | S1 | ❌ | ☐ |
| R2 | gate 状态机 ＋ 人工确认落盘 | 2 | R1 | ❌ | ☐ |
| R3 | 依赖图（改什么重跑什么） | 2 | S1 | ❌ | ☐ |
| W1 | 适配层 A / A′ | 2 | — | ❌ | ☐ |
| W2 | 适配层 C（治愈，多文件 ＋ 配额表实时算） | 2 | — | ❌ | ☐ |
| W3 | 适配层 B / C′ | 2 | W2 | ❌ | ☐ |
| W4 | `*-check.ts --json` ＋ 右栏三层呈现 | 3 | — | ✅ | ☐ |
| W5 | LLM 写稿（prompt 抽成 md ＋ 选题库检索） | 3 | W1–W3 | ✅ | ☐ |
| W6 | 局部重写 ＋ diff 三选一 ＋ 锁段 ＋ 版本 | 4 | W5 | ❌ | ☐ |
| W7 | 四条硬判据填空 ＋ 定稿闸 | 1.5 | W6 | ❌ | ☐ |
| W8 | 定稿落盘 ＋ 备份 ＋ 期号冲突拦截 | 1.5 | W7 | ❌ | ☐ |
| X1 | 上传 rig（checker ＋ manifest ＋ 预览接线） | 3 | S3 P2 | ✅ | ☐ |
| | **合计** | **65.5** | | | **0 / 32** |

### 12.2 分期

| 期 | 内容 | 人日 | **验收判据** |
|---|---|---|---|
| **一 · 骨架与预览** | B1 P1 P2 | **5.5** | 换封面版式档能实时看到 210／200；26 个场景铺成一面墙，四条不能空跑的线也有图 |
| **二 · 库与素材** | B2 B3 L1 L2 L3 L4 | **11** | 六条线的成品和素材在一个视图里；点音色即试听；**rubberband 缺失显示在顶上** |
| **三 · 底座** | B4 S1 S2 | **6** | `npm run regress` 绿；六条线步骤机读、`verified` 如实；Skill 能路由 |
| **四 · 参数化** | S3 S4 S5 S6 S7 | **6.5** | **回归全绿 —— 唯一判据**。频道包填现值，四期产物逐字节一致 |
| **五 · 制作台** | M1 M2 | **6** | 选风格 → 四栏默认值全填好；改任一项 → 落 `.ui-overrides.json` 并显示规范原话 |
| **六 · 审片台** | R1 R2 R3 | **6** | 三个 `human` 关卡没点过，出片按钮是灰的；改 `text` 只亮 `render` |
| **七 · 写稿台** | W1–W8 ＋ M3 | **21.5** | 四格填空不满，「定稿」是灰的；覆盖前先备份；期号冲突被拦 |
| **八 · 素材扩展** | X1 | **3** | 传一个新 rig → checker 逐条报 → 立刻渲一张场景预览 |

### 12.3 顺序为什么是这个

1. **期一、期二不碰管线一行代码** —— 档期跑着也能做
2. **B4 回归基线必须在 S3 之前** —— 没基线不敢动那些常量
3. **写稿台排到期七**，因为它是唯一**写稿件目录**的（数据损坏风险最高），
   而且它最大（21.5，占三分之一）
4. **X1 排最后** —— 它依赖 S3 的「支点搬家」。
   **它不是一件交互，是一次参数化，只是长着交互的样子**

### 12.4 ⚠ 三条排期纪律

档期缓冲实测：老马 **7 期已排期在未来**（08-27 → 09-17，一周三档 ≈ **3 周**），
醒木不响待发 1 期 —— **有缓冲，但不厚**。

1. **档期日（周二／周四／周日）不装依赖、不动管线脚本** —— `npm install` 那天炸了会连累出片
2. **界面不排渲染队列**，只做纯读 ＋ 秒级预览 —— ffmpeg 吃满 CPU，抢了档期就迟发
3. **每完成一个碰管线的任务（S3–S7 W4 W5 X1），立刻跑 `npm run regress`**

### 12.5 依赖与关键路径

**这是单人仓库**（`网页版方案.md` §九 就写着「多人协作、权限 —— 加进来只是负担」），
所以**并行没有意义，65.5 就是日历上的 65.5 个工作日**。
依赖图的用处不是排并行，是**看哪些能先动、哪些必须等**：

```
先动（零依赖，随时能起）
  B1 服务骨架 · B2 Windows坑 · B4 回归基线 · S1 登记表
  W1 适配层A/A′ · W2 适配层C · W4 --json

等 B1（有骨架才有页面）
  B3 媒体 · L1 扫描 · L3 素材台 · L4 音频台 · P1 封面预览 · P2 场景预览

等 B4（没基线不敢动常量）
  S3 拆BC → S4 频道包 →┬ S5 改读
                        ├ S6 音色两层
                        └ S7 平台包 → M1 默认值包 → M2 四栏

等 S1（登记表是两个消费者的底座）
  S2 Skill · R1 工作台 · R3 依赖图

最长的一条链（写稿台）
  W2 → W3 → W5 → W6 → W7 → W8 = 14 人日，**中间没有能省的**
```

⚠ **`W6 局部重写` 4 人日是单个任务里最大的一块**，而且方案原文写着
「**写稿台的核心，别省**」。它前面压着 W2→W3→W5 共 7 人日，
**所以写稿台一旦开工就是连续 14 天下不来** —— 这是它排在期七的第三个理由。

### 12.6 时间轴：三档速度

**人日 ≠ 日历日。** 档期实测：老马一周三档（周二／四／日 21:00）、
醒木不响一周五更（说书占三档），出片本身要占时间。

| 速度 | 每周投入 | 65.5 人日 ≈ | 期一（5.5）能看见 | 期四（累计 29）参数化完 |
|---|---|---|---|---|
| **慢档**（档期占大头） | 2 人日/周 | **33 周 ≈ 8 个月** | 第 3 周 | 第 15 周 |
| **中档** | 3 人日/周 | **22 周 ≈ 5 个月** | 第 2 周 | 第 10 周 |
| **快档**（档期靠存货顶） | 5 人日/周 | **13 周 ≈ 3 个月** | 第 2 周 | 第 6 周 |

⚠ **快档只有三周能顶** —— 老马已排期 7 期（08-27 → 09-17）≈ 3 周，
醒木不响待发只有 1 期。**想跑快档，先把存货攒到 6 周以上。**

### 12.7 里程碑（每个都是二值判据，不是「差不多了」）

| # | 里程碑 | 累计人日 | **判据（能回答是/否）** |
|---|---|---|---|
| **M0** | 能看见 | 5.5 | 浏览器里换一个封面版式档，**210px 那张跟着变** |
| **M1** | 家底在一个页面里 | 16.5 | 六条线的成品、26 个场景、24 个音色、269 条素材，**都能在页面上点到** |
| **M2** | 底座可信 | 22.5 | `npm run regress` 绿；六条线的步骤能机读且 `verified` 如实 |
| **M3** | **参数化没改变结果** | 29 | 频道包填现值，**四期产物逐字节一致** |
| **M4** | 能配一期出来 | 35 | 选风格 → 四栏默认值全填好；改一项 → 落 `.ui-overrides.json` |
| **M5** | 能出片 | 41 | 三个 `human` 关卡没点过，出片按钮是灰的；点过之后能出片 |
| **M6** | 能写稿 | 62.5 | 四格填空不满「定稿」是灰的；覆盖前先备份；期号冲突被拦 |
| **M7** | 能加角色 | 65.5 | 传一个新 rig → checker 逐条报 → 立刻渲一张场景预览 |

⚠ **M3 是整条计划里最关键的一格。** 它之前的都是「加东西」，
它是唯一一个「**改了东西但结果必须一模一样**」的里程碑 ——
不绿就不许往下走，因为后面每一步都踩在它上面。

### 12.8 进度怎么记

**记在 §12.1 那张表的「状态」列里，不另开一个跟踪器**
（这仓库的规矩：账本就是目录树，方案的账本就是方案本身）。

| 记号 | 意思 |
|---|---|
| ☐ | 未开始 |
| ◐ | 进行中（**同时只许有一个**） |
| ☑ 2026-09-xx | 完成，附日期 |
| ☑ 2026-09-xx `regress ✓` | 完成，且**碰管线的任务必须附回归结果** |

**三条进度纪律：**

1. **一次只开一个任务。** 单人并行只会都慢 —— 跟 §九「并发渲染 不做」同一条道理
2. **碰管线的任务（S3 S4 S5 S6 S7 W4 W5 X1）完成时必须贴 `regress` 结果**，
   没贴的不算完成
3. **一期没过 §12.2 的验收判据，不开下一期。**
   这跟 `gate` 那套是同一件事 —— **不给自己开「这次忽略」**

### 12.9 重估点：不要一次估到底

**三个最可能超的任务**，理由都是现成的：

| 任务 | 人日 | 为什么可能超 |
|---|---|---|
| **S1 登记表** | 3.5 | 方案原文自己写着「**最容易低估**：抄进来容易，逐条真跑一遍验证难」。说书一期跑一遍就 10–21 分钟 |
| **W6 局部重写** | 4 | 单任务最大，而且「diff 三选一 ＋ 锁段 ＋ 版本」是三件事挤在一格里 |
| **X1 上传 rig** | 3 | 它其实是一次参数化（支点搬家），**只是长着交互的样子** |

**两个重估点：**

- **期三结束（M2）** —— 那时 S1 的真实成本已经知道，把它的偏差按比例摊到 W6 和 X1 上
- **期四结束（M3）** —— 参数化跑完，回归绿不绿决定后面还敢不敢动常量

⚠ **重估只调后面的，不回头改已完成任务的估值。**
改了就看不出「当初估得准不准」，下一次还是估不准。

---

## 十三、风险

| 风险 | 大小 | 怎么顶 |
|---|---|---|
| **参数化搬完，片子悄悄变了** | **高** | B4 回归基线，逐字节验 |
| **规范每周在变，界面默认值变旧** | **高** | 隔离纪律 2：只读不抄，默认值全部 import 自代码 |
| **写稿台写错目录** | **高** | 覆盖前挪进 `.web-versions/`；期号冲突拦截，不猜 |
| 把只在文档里的规矩做成控件 | 中 | §3.3 那条判据 |
| 闸退化成天天加 `--anyway` | 中 | 阈值可配、存在与否由频道包声明，**不给「这次忽略」** |
| S1 登记表低估 | 中 | 六分之二已是代码，**导出不手抄**；`verified` 如实标 |
| **写稿台保证不了人味儿** | — | 规范自己写着机器查不了。**网页版让改稿变快，不让判断变便宜** |

---

## 十四、挂起（不在 65.5 里）

| 项 | 触发条件 |
|---|---|
| **剪辑**（导入／时间轴裁切／转场） | **要先有代理预览**（360p·6fps）。现在「渲完才能看」，长片 485 秒 —— **不解决预览，剪辑界面是摆设** |
| **转场** | 六条线现在一个都没有，节奏靠停顿和换镜做的。**加之前先问它替代什么** |
| **多用户** | 等 B4 回归基线 ＋ S5 频道包跑起来，再动 `paths.ts` 那一处。⚠ `projects/` 分租户会动到所有已出片目录 |
