# YouTube 封面规范 v3

> **醒木不响频道的跨线缩略图规范**，管的是「在信息流里被不被看见」。
> 各线的片头首帧、片内画面仍看各自那份文档，这份只管一张图：**传到 YouTube Studio 的那张缩略图。**
>
> | 线 | 配色档 | 本线文档 |
> |---|---|---|
> | 说书（聊斋） | `ink` | [shuoshu/出片手册.md](shuoshu/出片手册.md) §六 |
> | 心理洞察 | `ink` | [zhiyu/心理洞察出片方案.md](zhiyu/心理洞察出片方案.md) |
> | 禅佛典·小故事大道理 | `night` | [zhiyu/禅佛典向_小故事大道理_书目与稿件.md](zhiyu/禅佛典向_小故事大道理_书目与稿件.md) |
> | 治愈系旁白（助眠档） | **`paper`**（2026-08-24 改，见 §二） | [zhiyu/治愈系出片方案.md](zhiyu/治愈系出片方案.md) · [zhiyu/治愈档封面设计规范.md](zhiyu/治愈档封面设计规范.md) |
> | 哲理向 | `ink` | 本仓库尚无单独文档 |
>
> 段子线 / 《一页故事》儿童故事线 / 碎嘴老马**单点式与累积式**（那三条是 9:16 竖版短视频）
> **不在本规范范围内**——封面走 [joke-video/封面设计规范-COVER.md](joke-video/封面设计规范-COVER.md)。
> **它们要是发 YouTube Shorts，本规范也不适用**，见 §十一。
>
> ⚠ **老马长片是例外：它是 16:9 横屏，就是发 YouTube 的。** 但它**不走 V3 版式**，
> 走自己那一档（§〇之二 表里第三行，规范在 COVER 那份的 §九）——
> 那条线本来就有一整套画好的场景和角色，做成两个大字加一个色块，等于把片子的长相扔了。
> **只有 §一 那条右下安全区（时长角标）对它照样成立**，那是 YouTube 的界面，不是版式偏好。
>
> **所有数值都是最终值，不是建议值。** 配套实现见 §八（**注意：脚本还没进仓库**）。
> v3 相对 v2 的变化：副标新增重点词变色（§五末 + §四 `HOOK_MARK`）；新增 Shorts 不适用说明（§十一）。
> v2 相对 v1：主字换宋体＋错位影；眉标右界改为实测；副标错行；新增踩坑记录。

---

## 〇、三张图是三回事，不要统一

| | 给谁看 | 任务 | 用什么 |
|---|---|---|---|
| **缩略图** | 还没点进来的人 | 在白底信息流里被看见 | **本规范** |
| **片头首帧** | 已经点进来的人 | 接住、定调子 | 各线自己那套（说书是竖排题名卡，治愈是和纸插画） |
| **片内画面** | 正在听的人 | 陪伴、不抢注意力 | [zhiyu/治愈档封面设计规范.md](zhiyu/治愈档封面设计规范.md) §一那张对照表 / `style/inkwash.ts` |

原先一套素材三处用，浅米白封面在 YouTube 的白色界面里没有边界、
直接融进背景，是点击率被压住的直接原因。

**缩略图与首帧无绑定关系**——在 Studio 里单独上传即可，视频文件不用重新合成。
已发布的片子不必回头重制首帧。

**唯一验收标准是 210px 宽下的可读性**，不是 1280px 下好不好看。

---

## 〇之二、频道有两套版式，两套都留着（2026-08-24 立）

| 版式 | 脚本 | 长相 | 谁在用 | 状态 |
|---|---|---|---|---|
| **V3 横版**（就是这份规范） | `yt-cover.ts` | 左边两字主字＋错位影，右边一个实心图形 | 说书 E01–E05、治愈 taoan-E01 | 新片默认走这套 |
| **窗格版** | `xinli-cover.ts` | 和纸底＋木格窗＋竖排篇名＋朱印＋底部一行钩子 | 心理洞察（`psychInsight`）、小故事大道理（`zenParable`） | **保留，随时可用** |
| **长片档**（2026-08-26 加） | `joke-video/src/laoma-long-cover.ts` | **片子里的一帧当底**＋左边一块贴纸大字（黄底墨边） | 老马长片（横屏五分钟，`format: "long"`） | 规范在 [joke-video/封面设计规范-COVER.md](joke-video/封面设计规范-COVER.md) **§九** |

**窗格版不是遗留物，是一档。** 它跟片内画面、片头首帧是同一套视觉语言
（同样的和纸底和木格窗），这是 V3 换不来的东西 —— 想让某条线的封面跟片子长一个样，
就用它。数据在 `发布.json` 的 `series` / `motif` / `coverTitle` / `coverLine` / `hook`：

```bash
npx tsx src/xinli-cover.ts --line 禅佛典 --ep 第七个饼
npx tsx src/xinli-cover.ts --line 心理 --ep 已读不回
```

2026-08-24 复跑过一次《第七个饼》，**出来的图跟 08-21 那版逐字节一致** ——
这一档随时拿得出来，不用先修。

⚠ **一期只能选一套跑。** 两套都往 `cover/<篇>/upload-1280x720.png` 写，
后跑的那套覆盖前面那套，而两张长得完全不一样、都不报错。

### V3 自己又分两排：`hook` 和 `title`（2026-08-24 加）

同一个脚本、同一套配色和图形，**主位那一块排法不同**，靠 `发布.json` 里
`yt.layout` 选，不写就是 `hook`（V3 原样，已出的几期都没动）。

| | `hook`（缺省） | `title` |
|---|---|---|
| 眉标 | `线名 · 篇名` | 频道名就够了 |
| 主位 | **两个字**，250px | **完整标题**，按标点断行，字号自动收（108 起） |
| 斜杠 | 钉在 y=412，是主字和副标之间的分隔 | 跟在标题块下面 54px，是收尾 |
| 副标 | 两行，各 ≤10 字 | **没有** |
| 纵向中线 | 266 | 392 |

**赌的东西不一样。** `hook` 赌「两个大字够勾人」，篇名退到眉标、解释交给副标；
`title` 赌「把话说全了更可信」。标题本身已经是一句话的时候（「别人怎么看你，不归你管」），
拆成主字「不归」＋副标两行，等于把同一句话拆开再拼回去 —— 拆的那一下就漏了。

代价写在明处：主位从 250px 掉到 ~108px。**这是知情的**，
§七.4 那条「主字外接高度 ≥ 画面 30%」在这一版量的是**整块标题的外接框**，
两行 108px 约 229px，正好在线上。标题再长会自动降字号，降到块高不够就报警 ——
**那是在说标题该改短，不是版式不行**。

```json
"yt": {
  "layout": "title",
  "kicker": "醒木不响",
  "big": "别人怎么看你，不归你管",
  "motif": "snow_boat", "palette": "paper", "seal": "醒"
}
```

三个数是自动的，别手填：断行按 `，、。；：｜/` 断（**标点本身不上屏**）、
字号从 108 往下收到最宽那行装进 x=720 为止、错位影按字号等比缩
（13px 是配 250px 的，照抄到 108px 会糊成重影）。

首期：治愈 `2026-08-26_1800JST_治愈_别人怎么看你-E01`。
加这一排的时候拿 taoan-E01 和婴宁-E05 对过 —— **两张都逐字节一致**，`hook` 版没被碰。

---

## 一、画布与安全区

| 项 | 值 |
|---|---|
| 尺寸 | 1280 × 720 px |
| 格式 | PNG（RGB，不带 alpha） |
| 边距 M | 72 px |
| 文字区 | x ∈ [72, 690]，左对齐 |
| 图形区 | 中心 (968, 344) |
| **右下安全区** | x ∈ [1080, 1280]，y ∈ [640, 720] **必须留空**（YouTube 时长角标） |

---

## 二、三套配色

按稿件线选，不按单期心情选。哪条线用哪档见开头那张表。

### `ink` —— 聊斋说书 / 心理洞察 / 哲理向

| 用途 | 变量 | 值 |
|---|---|---|
| 底 | bg | `#141C24` |
| 主字·主图 | ink | `#F2EFE8` |
| 重音（含主字错位影） | acc | `#D6452F` 朱砂 |
| 弱化图形 | mute | `#5C6B75` |
| 印章 | seal | `#D6452F` |

### `night` —— 治愈 / 助眠 / 古典随笔 / 禅佛典

| 用途 | 变量 | 值 |
|---|---|---|
| 底 | bg | `#142234` |
| 主字·主图 | ink | `#F1EDE3` |
| 重音（含主字错位影） | acc | `#E8C87A` 暖月色 |
| 弱化图形 | mute | `#3A4E66` |
| 印章 | seal | `#C4432F` |

**规则**：night 线的错位影用暖月色，**不用朱砂**。夜蓝底上同时出现朱砂红与月黄
会撞成第三种色关系，整张脏掉；治愈线朱砂只留在印章里。

### `paper` —— 治愈线（2026-08-24 加）

| 用途 | 变量 | 值 |
|---|---|---|
| 底 | bg | `#E8DFCB` 宣纸（**压深过一档**，见下） |
| 主字·主图 | ink | `#2F4A3F` 墨绿 |
| 重音（含主字错位影） | acc | `#E8A95E` 暖带 |
| 弱化图形 | mute | `#7E9184` |
| 副标字色 | hook | `#5B6F63` |
| 印章 | seal | `#C0503C` 朱砂 |

**这一档是知情地违反上面那条线—色对照表的。** 治愈线本来定在 `night`，
换成浅底的理由只有一个：**跟片内画面、片头首帧保持同一套颜色**（那两样都是和纸浅底）。
代价写在 §〇：浅底封面在 YouTube 的白界面里边界弱。

两条因此定死的规矩：

- **底色是 `#E8DFCB`，不是片内那张米白 `#F6F2E8`。** 两版都渲过、都贴到白底上看过：
  米白那版边界基本没有，压深一档才立得住。**这半档就是不换深底付的钱，别再往回调浅。**
- **浅底档必须给 `hook`（副标字色）。** 代码里副标的兜底色是写死的白，
  深底档正好，浅底档整行看不见 —— 而四项自动检测全绿：它们查位置、宽度、安全区，
  **没有一项在查「看不看得见」**。

副标固定纯白 `#FFFFFF`（比主字的纸白更实，压在错位影上不会被拖糊）。

⚠ **不要拿这两档去对齐各线自己的配色。** 说书竖版封面那套
（`shuoshu-cover.ts` 的 `SERIES.liaozhai`：渐变靛青 `#0e1b29`→`#193448` + 朱砂 `#c5381c`）
跟这里的 `ink` 不是同一组值，也不该改成一样——
竖版封面在片头深色环境里被看，缩略图在 YouTube 白底信息流里被看，两张图各自服各自的场。

---

## 三、字体

| 用途 | 字体 | 字重 |
|---|---|---|
| **主字** | Noto Serif CJK SC（思源宋体） | **Black** |
| 眉标 / 副标 | Noto Sans CJK SC | Bold |
| 印章「醒」 | Noto Serif CJK SC | Bold |

⚠ **TTC 索引必须传 2**（0=JP，1=KR，2=SC，3=TC，4=HK）。传 0 不报错，
只是拿到日文字形，很容易漏掉。

⚠ **Windows**：脚本原来写的是 Linux 路径，本机要装思源宋体／思源黑体后改路径。
退回微软雅黑则字重不足，主字要加到 270px。
仓库里 `fonts/NotoSerifCJKsc/OTF/SimplifiedChinese/` 那七个静态 OTF 已经是真字重
（当初为说书封面渲染放的，见 [shuoshu/出片手册.md](shuoshu/出片手册.md)），
移植时直接指过去，别再走可变字体那条老路。

---

## 四、字号与坐标

```
MAIN_PT   = 250     # 主字（2 字）；3 字降到 190
HOOK_PT   = 58      # 副标；眉标同此值
SEAL_R    = 34      # 印章圆半径
SEAL_PT   = 42      # 印章「醒」
HOOK_INDENT = 64    # 副标第二行起的错行缩进
HOOK_MARK   = "color"  # 副标重点词提示（none / color / underline / block）
HOOK_MAX_X  = 690   # 副标最右
KICKER_PAD  = 30    # 眉标与图形的最小间隙
SHADOW_OFF  = 13    # 主字错位影偏移
```

| 元素 | 位置 | 规格 |
|---|---|---|
| 印章圆 | 圆心 (104, 88)，r=34 | fill=seal |
| 印章「醒」 | (104, 90) 居中 | Serif Bold 42px，fill=bg |
| 眉标 | 起点 (162, 88) | Sans Bold 58px，fill=seal，字距 +8 |
| 主字影 | (77, 279) 左对齐纵向居中 | Serif Black，fill=acc |
| **主字** | (64, 266) 左对齐纵向居中 | Serif Black 250px，fill=ink |
| 重音短线 | (72, 412) → (204, 423) | fill=acc |
| 副标第 1 行 | (72, 500) | Sans Bold 58px，fill=`#FFFFFF` |
| 副标第 2 行 | (136, 500+58×1.32) | 同上，**缩进 64** |
| 图形 | 中心 (968, 344) | 见 §六 |

---

## 五、文案规则

### 主字

- **2 个字。** 3 个字会跌破「高度 ≥ 画面 1/3」这条底线（文字区宽 618px，
  3 字最大只能到 190px＝26%），只在图形让位时才允许
- **是钩子，不是书名。**「崂山道士」是书名 → 换成「撞墙」
- 优先用[说破层规范](说破层规范.md)的**诊断词**（虚荣 / 自欺 / 奉行 / 不服），一图两用
- 具体的动作或数字 > 抽象名词。「撞墙」「三米」可以，「虚妄」不行

⚠ **说书线最容易搞错的一处**：`scenes.json` 的 `cover.title` 是繁体书名
（畫皮 / 嶗山道士 / 促織），那是给竖版封面用的。**缩略图主字不能照抄它**，
要另取两个字的诊断词，而且写简体。书名请放眉标。

### 眉标

- 格式 `线名 · 篇名`，例 `聊斋 · 促织`
- 书名放这里，不放主字位
- 右界不是固定值，由程序实测（见 §七），不用为了长度改文案

### 副标（两行）

- 每行 ≤ 10 字，两行为准，绝不三行
- 内容是**说破句**，走 `X 不是 A，是 B`——说「他要什么」，不说「发生了什么」。
  造句的三个模子见[说破层规范](说破层规范.md) §三
- 第二行缩进 64px 错开。齐头的两行会跟主字的左边线撞成一个方块，
  错开后左缘形成台阶，与主字错位影是同一种语言
- 210px 下副标是糊的，它不承担点击任务

### 副标重点词（`HOOK_MARK = "color"`）

文案里用 `{}` 圈出重点词，程序自动分段上色（`ink` 线用朱砂，`night` 线用暖月色）：

```python
["上面随口要一只{蟋蟀}", "底下赔进一个{孩子}"]
```

- **圈的是对照关系的两端，不是形容词。** 蟋蟀／孩子、回复／说法、第七个／前六个——
  两个词单拎出来要能自己构成一句话。圈「随口」「赔进」这种动词没用，它们不构成对照
- 一行只圈一处，两行合计两处，多了就没有落点了
- 备选模式：`underline`（白字＋红下划线，更克制，但 210px 下那道线基本没了）、
  `block`（色块反白，最跳但画面上的红会散成三处）、`none`
- **什么时候该从 `color` 换成 `underline`**：某张的主字笔画少、错位影红得比较整时，
  副标的红词会跟它打架。参数是每张独立的，单独改那一张即可

---

## 六、图形（motif）契约

新增 motif 必须同时满足：

1. **只用 3 个色**：bg / ink（或 acc）/ mute。不做渐变、不做描边叠加
2. **最小笔画宽度 ≥ 7px**
3. **主体是实心色块，不是线稿**
4. 主体外接尺寸 ≈ 400 × 540 px，中心 (968, 344)
5. 不得进入右下安全区
6. 缩到 210px 仍能一眼说出画的是什么——不能，就重做

### 现有九个

| id | 内容 | 用于 | 配色 |
|---|---|---|---|
| `wall` | 砖墙出血 + 人形撞上去 + 撞击线 | 说书 E03 崂山道士 | ink |
| `face` | 侧脸 + 底下偏移一层 | 说书 E01 画皮 | ink |
| `face_split` | 正面脸中线劈开，一半人一半鬼 | 画皮（备选） | ink |
| `read` | 已发出的气泡 + 双勾 + 永远没回的空框 | 心理 E01 已读不回 | ink |
| `seal_bug` | 「令」字大印 + 底下一只小蟋蟀 | 说书 E04 促织 | ink |
| `three` | 三个人头排队，第三个变色 | 心理 E02 第三个人 | ink |
| `cakes` | 七个饼，前六空心第七实心 | 禅佛典 E01 第七个饼 | night |
| `hut` / `hill` | 满月 + 极小的屋 / 三重山影 | 治愈 方丈记 | night |
| `dawn` / `dusk` | 山脊托起的日轮＋萤火 / 沉落的日轮＋归鸦＋雪脊 | 治愈 枕草子 上 / 下 | night |
| `sparrow` | 满月 + 麻雀剪影 | 枕草子（备选） | night |
| `smile_flat` | 上面一道上翘的实心月牙，下面一条同宽的平杠 | 说书 E05 婴宁 | ink |
| `snow_boat` | 暖月 + 两层山影 + 一道雪面 + 湖上一条小船 | 治愈 陶庵梦忆 E01 | paper |

**`smile_flat` 的一条实测**：平杠第一版用 `mute` 画，1280 上看得见，
缩到 210px 就化进深底里没了 —— 剩一个笑，跟主字「不笑」正好打架。
**改成跟月牙同色才读得出"同一样东西被压平了"。**
契约⑥"缩到 210px 仍能一眼说出画的是什么"，说的就是这种失败：不报错，1280 上还挺好看。

**night 线统一「满月 + 剪影」**：月亮是完整色块，缩到 210px 还是个圆。

**复用进度线标记**：麻雀（治愈）、骷髅（聊斋）、蜗牛（方丈记）放大即可做 motif，
一图两用能积累辨识度。

---

## 七、程序必须自动做的四件事

1. **眉标右界实测。** 画完图形后，扫描眉标横带 y ∈ [52,126]，找图形最左的非底色
   像素，右界 = 该 x − 30；带内无图形则放到右边距。**不要用固定阈值**（理由见 §九）
2. **副标自动降档**：含缩进后超过 x=690 则逐级减 2pt
3. **右下安全区检测**：x ∈ [1080,1280]、y ∈ [640,720] 内非底色像素 > 5% 则报警
4. **主字高度检测**：外接高度 ÷ 720 < 0.30 则报警

---

## 八、程序接口与实现现状

```python
render(path, kicker, big, hook, motif, palette, style=None)
# kicker: "聊斋 · 促织"
# big:    "抵命"                    2 字
# hook:   ["上面随口要一只{蟋蟀}", "底下赔进一个{孩子}"]   {} 圈重点词，见 §五末
# motif:  见 §六
# palette: ink | night
# style:  留空＝用全局 STYLE（serif_shadow）
#         plain / serif / shadow / serif_shadow / block
```

批量入口是 `jobs` 列表，一行一期。跑完自动输出 `YT_预览210.png`。
**文件命名**：`YT_篇名.png`；同一书拆多期用 `YT_书名_篇名.png`。

### ✅ 2026-08-23：走了乙，脚本入库了

实现在 [`joke-video/src/yt-cover.ts`](joke-video/src/yt-cover.ts)，跑法：

```bash
cd joke-video
npm run yt:cover -- --ep E05      # 单跑
npm run shuoshu:ship -- --ep E05  # 已经接进管线，在「封面」和「成片」中间
```

数据写在期目录 `scenes.json` 的 `yt` 块里，跟 `cover` 并排 ——
加一期只写一次数据，跟封面那边是同一条纪律：

```json
"yt": {
  "kicker": "聊斋 · 婴宁", "big": "不笑",
  "hook": ["压垮她的不是{官司}", "是婆婆那句{好话}"],
  "motif": "smile_flat", "palette": "ink"
}
```

产物三个：`YT_<篇名>.png`（1280×720，上传用）、`YT_预览210.png`（**先看这张**）、
`YT_<篇名>.svg`（矢量源）。§七 那四项检测每次都跑，结果打在命令行上。

**ship 里这一步是 soft 的**：期目录还没写 `yt` 块时它退出 1，但不掐断出片 ——
片子是好的，缺的是一张可以事后单独上传的图。交付物那一节会提醒。

字体：主字用仓库 `fonts/NotoSerifCJKsc/` 那七个静态 OTF（真字重）；
**眉标/副标的思源黑体仓库里没有，退到系统的微软雅黑 Bold。**
§三 那条"微软雅黑字重不足"针对的是 250px 的主字，58px 的眉标副标用雅黑够。
哪天思源黑体入库，改 `SANS_FAMILY` 一处即可。

### 当初的两条路（留档）

原型是 `cover_yt.py`（Pillow，无外部依赖），没有入库，
本机也没装 Python（`python3` 只是 Windows 商店的占位符，跑起来直接跳应用商店）。
当时那十张 `YT_*.png` 是在别处渲的。两条路选一条：

| | 做什么 | 代价 |
|---|---|---|
| **甲** | 装 Python + Pillow + 思源字体，改 §三 的路径，脚本入库 | 多一个运行时；跟仓库其余部分（全 TS）不同语言 |
| **乙** | 把这套版式移植到 `shuoshu-cover.ts` 已经在用的 resvg 链路 | 字体现成、自检缩图现成；§七 那四项像素级检测要重写成位图扫描 |

**当时的纪律是「在此之前别把它写进 `npm run`」** —— 文档里有、跑不动，就是假接口，
跟体检脚本那两个「报成功的失败」是同一个教训。**现在它真的跑得动了，才进的 `npm run`。**

---

## 九、踩坑记录（都是返工换来的，别再走一遍）

**线稿在 210px 全部消失。** 第一版蟋蟀是几条线拼的，缩小后只剩一团白影加两根须子。
改成实心身子＋折起的后腿才认得出。这不是审美偏好，是缩略图尺寸下的物理事实。

**侧脸轮廓一做偏移或挖空就崩。** 试过「外面一张空皮、里面另一张脸」，
做出来是个兜帽形状，鼻子嘴巴那几个凹口太小扛不住。侧脸只能整块用。

**朱砂错位影对笔画少的字露怯。**「抵命」笔画密，红影被吃掉大半只剩边缘，好看；
「已读」横画又长又平，红影变成一条条平行红杠，像套色没对准。**换宋体后解决**——
横细竖粗把影子切碎了，成了木刻套印的效果。这就是主字用宋体而非黑体的真正原因，
不只是气质。

**固定的横向阈值是错的。** 曾经定死「眉标不得越过 x=760」，结果
「小故事大道理 · 第七个饼」被压到 42pt。但眉标在 y≈88 那条带上，饼的最高点在
y=228、月亮在 y=166、气泡在 y=168——底下全是空的，被一堵不存在的墙拦住了。
改成实测后十条眉标全部回到 58pt，**「缩短线名」那个文案方案也就不需要了**。

**竖排版式试过，放弃了。** 篇名竖排在右缘看着像书脊、挺搭题材，但「线名 · 篇名」
整串竖排放不下（十三个字 ×125pt = 1600px，画布只有 720），必须拆成两处；
而且右缘一让，图形被压窄，主字直接怼到图形上。
**这跟说书竖版封面不冲突**——那边画布 1920×1080，高出一倍，放得下。

**背景出血要留角标位。** 砖墙原先从画布顶出血到底，右下安全区被占 86%。
改成落在 y=624 的地面线上，人站同一条线，安全区才干净。
这类问题肉眼很难发现，检测脚本一跑就出来。

**副标重点词与主字错位影同色，可能打架。** 主字笔画密时（如「抵命」），影子被宋体笔画切碎、
红是零散的，副标那两个红词反而成了唯一完整的红块，是好的落点；主字笔画少时影子红得整，
两处红就会抢。那一张改用 `underline` 即可。

**图形放大会吃掉主字。** 饼半径 56 时「白吃」两字直接怼到第一个饼上。
图形宽度要按主字最长的情况留余量。

---

## 十、换封面之后的纪律

- **三张以上一起换**，否则对比不干净
- 换完 YouTube 会重新做一轮推荐测试，**数据 3–5 天才稳，别第二天下结论**
- 看**展示点击率**，不看观看次数。1.4% 是起点，一般区间 2%–10%，先奔 4%
- 点击率上去之前，不要动片长和结构——那是下一个瓶颈，
  同时动两个变量什么都测不出来

---

## 十一、本规范不适用于 Shorts

Shorts 是另一套规则，**不要拿这份文档去做竖版**：

- 尺寸 9:16（1080×1920），16:9 的图在首页／探索／订阅页会被替换成自动生成的 4:5
- 首页卡片是裁过的，**关键信息必须收进正中间的 1080×1080 内（y ∈ [420, 1500]）**，
  上下各 420px 是牺牲区
- 自定义缩略图上传需要 YPP 资格且账号已验证，目前只能在电脑版 Studio 添加；
  否则只能用「把封面烧进片头某一帧 + 手机 App 手动选帧」
- Shorts 缩略图不出现在 Shorts 信息流里，只影响搜索、频道页、订阅、首页这几个入口

需要时另立一份规范，**不并入本文档**——版式逻辑差别太大，混在一起容易看错。

### 对本仓库三条竖版线意味着什么

段子 / 儿童故事 / 老马三条线的封面本来就是 1080×1920
（[joke-video/封面设计规范-COVER.md](joke-video/封面设计规范-COVER.md) §三），
所以尺寸不用改。**要改的是安全区：多了一条比现有那条更紧的上界。**

| 裁法 | 保留区 | 定在哪 |
|---|---|---|
| 个人主页九宫格 3:4 | y ∈ [240, 1680] | `cli.ts` 的 `crop=1080:1440:0:240` |
| **Shorts 首页卡片** | **y ∈ [420, 1500]** | 本节。**这条更紧，是新的约束** |

老马线实测（`cover.ts` 的 `T.baselineK = 0.22`，字号 182）：
主标题墨迹顶边 = 基线 422 − 182 × (0.88 + 0.075) ≈ **y 249**。
**过得了 3:4 的 240，过不了 Shorts 的 420。** 打开 `titleLow`（`lowBaselineK = 0.4`）
顶边落到 ≈ y 594，两条都过。

**所以这三条线发 Shorts 之前，得先决定 `titleLow` 是不是要变成默认**，
以及 `-安全区.png` 要不要多画一条 y=420／y=1500 的线。**没定案，别动代码。**

---

## 十三、⚠ 说书线从 E05 起：缩略图 = 片头首帧

**2026-08-23 用户决定：说书线只保留一种封面版式，就是本规范这一套。**

`yt-cover.ts` 现在一次出六个文件 —— 1920×1080 的 `cover.png`（压在成片第一帧）、
1280×720 的 `YT_<篇名>.png`（上传）、210px 自检、1080×1080 微信方版、200px 方版自检、
两个 svg 源。`shuoshu-cover.ts` 留在仓库里但 ship 不再调它。

**这跟 §〇「三张图是三回事，不要统一」是冲突的，冲突是知情的。**

| | |
|---|---|
| §〇 的理由 | 缩略图在信息流里让人点，首帧接住已经点进来的人。任务不同，版式就不该同 |
| 反过来的账 | 两套版式并行了这么久，结果是**缩略图那套一次都没跑过**。一种版式只有一处要维护，也不会再出现「忘了做缩略图」 |
| 实际代价 | **成片第一帧变成主字（「不笑」）而不是篇名（「嬰寧」）。** 说书线篇名是身份，这是实打实的损失；篇名只剩眉标那一行小字 |

§〇 里「浅米白封面融进 YouTube 白底」那条**不适用于说书**：
说书旧封面是深靛青，不是浅米白。所以并版的风险主要在上面那条"篇名没了"，
不在"缩略图不够跳"。

**其余三条线（心理洞察 / 治愈 / 禅佛典）没有跟着改**，它们的首帧仍走各自那套；
尤其治愈线还有 §十二 末尾那个"两套 1280×720 抢同一个位子"的未定案，**别顺手一起动**。

---

## 十二、覆盖现状（2026-08-23）

已出片但**还没有缩略图**的，就是待办。

| 线 | 期 | motif | `YT_*.png` |
|---|---|---|---|
| 说书 | E01 画皮 | `face` | ⚠ 主字「自欺」已入库，**但没上传** —— 该期已发布，不回改 |
| 说书 | E02 聂小倩 | **还没定** | **缺**（该期已发布，不补图；说破已于 08-23 补进卡片） |
| 说书 | E03 崂山道士 | `wall` | ⚠ 主字「撞墙」已入库，**跟线上那张不是同一张** —— 线上是在别处渲的 |
| 说书 | E04 促织 | `seal_bug` | ⚠ 同上 |
| 说书 | E05 婴宁 | `smile_flat` | ✅ 主字「不笑」，**首帧也是它**（见下面 §十三） |
| 心理洞察 | E01 已读不回 | `read` | ✅ `YT_已读不回.png` |
| 心理洞察 | E02 第三个人 | `three` | ✅ `YT_第三个人.png` |
| 治愈 | 方丈记 上 / 下 | `hut` / `hill` | ✅ 两张 |
| 治愈 | 枕草子 上 / 下 | `dawn` / `dusk` | ✅ 两张 |
| 治愈 | 陶庵梦忆 E01 | **还没定** | **缺** |
| 禅佛典 | E01 第七个饼 | `cakes` | ✅ `YT_第七个饼.png` |

**归档位置定了**：`projects/<类型>/<期目录>/YT_<篇名>.png`，跟成片躺在一起，
`yt-cover.ts` 直接写到那儿，ship 的交付物核对会扫。
原先散在 `~/Downloads` 的那十张不用管 —— 说书四期已经用新脚本重渲入库，
心理/治愈/禅佛典六期等各线接上时一并重渲。

⚠ **E01–E04 那四张是备用图，没上传。** 那四期已经在平台上发布，目录里的 `cover.png`
还是旧版式，跟线上一致。真要换，照 §十 的纪律：三张以上一起换，数据 3–5 天才稳。

聂小倩的「说破」和「现代对照」原先是**待补**，2026-08-23 补进卡片了
（「能让他不动心的不是定力，是他心里压根没摆那张可以谈价的桌子」）。
当时没有硬编一句，是因为副标两行就是说破句，现编正好犯
[说破层规范](说破层规范.md) §五「先想到一个标签再往故事上套」那条禁令。

### ⚠ 未定案：治愈线有两套 1280×720 封面在抢同一个位子

[`zhiyu/治愈档封面设计规范.md`](zhiyu/治愈档封面设计规范.md) 是治愈线自己那套 1280×720 缩略图
（左竖条 384px + 书名/期标题两列 + 圆角印 + 底部色带），`zhiyu-cover.ts` 一直在出，
枕草子上期的 `cover/上/upload-1280x720.png` 就是它，连 `check-210.png` 都有。

**本规范这套也是 1280×720，也是给同一个上传位的**，而且方丈记、枕草子四期的
`YT_*.png` 已经按本规范渲出来了。两套版式，一个位子。

三条路，**没定之前别把治愈线的 `zhiyu-cover.ts` 改掉**：

| | |
|---|---|
| 甲 | 本规范统管四条线，`zhiyu-cover.ts` 的 1280×720 退成播放列表/专辑封面 |
| 乙 | 治愈线保留自己那套（书封语言更贴助眠档），本规范只管说书/心理/禅佛典 |
| 丙 | 各出各的，A/B 跑三到五天看展示点击率（照 §十 的纪律，一次只动这一个变量） |

说书线没有这个问题：`shuoshu-cover.ts` 的 `upload-1280x720.png` 是竖版封面的等比缩图，
已经改标成 B站/西瓜用，YouTube 归本规范。
