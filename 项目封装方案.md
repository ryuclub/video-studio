# 项目封装方案：Skill + 可视化 · v2

> ⚠ **已并入 [`可视化方案.md`](可视化方案.md)（2026-08-26）。本份只作留档，别照它做。**
>
> 三份文档写同一件事已经打起架来了：页面 6 页 vs 5 页、工作量 44 vs 31.5 vs 19.5、
> 落地顺序 11 步 vs 7 期、架构两份 —— **三个真相在等着分叉，而且已经分叉了**。
> 冲突点逐条列在合并方案 §〇，取哪一边也写在那儿。


> **v2 相对 v1 改了什么**：v1 断言「参考图那四栏里，视频来源／拼接模式／转场三样
> 这仓库根本没有对应物」——**核实之后，两样有，而且是完整实现**。
> 这一版按「素材库 · 风格库 · 画面来源 · 规范作为默认值」重新组织，
> 结论从「不能照做」变成「**照做，但每个下拉框的选项换成仓库自己的能力**」。
>
> **界面长什么样、怎么排期，在 [`可视化界面方案.md`](可视化界面方案.md)** ——
> **这一份是「为什么这么定」的分析与决策记录**，那一份是「长什么样」和「怎么做」。
>
> ⚠ 这份仍旧**不重写** [`网页版方案.md`](网页版方案.md)（434 行，2026-08-22 定稿）——
> 写稿台／审片台／五种稿件形态以那一份为正文。

---

## 〇、v1 那个判断错在哪

v1 写：「视频来源／拼接模式／转场 —— 这三样这儿根本没有对应物。」

**核实（2026-08-26）：**

| 参考图上的东西 | 仓库里 | 证据 |
|---|---|---|
| 视频来源 = Pexels | ✅ **完整实现** | `src/steps/footage.ts` 330 行：检索、按分辨率打分、下载、缓存、出处（`ClipCredit`）。缓存里躺着 **269 条、2.8 GB** |
| 「上传视频素材」 | ✅ **已实现** | `assets/manual/<keyword>.mp4`，**命中即整组覆盖检索**，还分横竖版和多条。现有 16 条 |
| 「AI 根据文案生成视频关键词」 | ✅ **已实现** | `src/steps/script.ts` 的 SYSTEM 就在教模型出 `clip`（英文 2–4 词），**还带三条踩出来的硬规矩** |
| 「视频关键词（英文，可选）」输入框 | ✅ **已实现** | `script.json` 每行一个 `clip` 字段。实测一条 49 句的稿子里 22 行带 `clip` |
| 按关键词分组拼接 | ✅ **已实现** | `footage.ts:61` 按 `clip` 变化切组；`render.ts` 按时间轴切片 |
| **转场** | ❌ **真的没有** | 全仓库没有 transition |

**所以参考图那张表单，跟这仓库的距离比我说的近得多** ——
`记者读稿` 那条线本来就是「AI 出文案 ＋ AI 出关键词 ＋ 抓素材 ＋ 拼接 ＋ TTS ＋ 字幕」，
**跟参考图是同一个形状**。我把它漏了，因为我盯着另外五条画出来的线看。

---

## 一、核心判断：画面来源是一个**维度**，仓库已经有四种实现

这是这一版的骨架。「视频拼接方式」不是一个要新建的功能，
**是把已经存在的四条路径收进一个下拉框**。

| 画面来源 | 现有实现 | 现在谁在用 | 成熟度 |
|---|---|---|---|
| **系统自动勾勒画面** | 六个 SVG 渲染器（`render.ts` / `longform.cjs` / `shuoshu-scene` / `zhiyu-scene` …） | 段子·一页故事·老马·说书·治愈·心理·禅佛典 | ✅ 出过几十条片 |
| **自动抓取素材** | `footage.ts` → Pexels，按 `clip` 关键词 | 记者读稿 | ✅ 269 条缓存 |
| **提供素材** | `assets/manual/<keyword>.mp4`，命中即覆盖检索 | 记者读稿 | ✅ 16 条在用 |
| **叠加到已有底片** | `video-pipeline/` 的 `assemble.ts` ＋ `overlay.ts`，**底片不动、音轨原样搬运** | 动效面板（`PANEL-PLAN.md`） | ⚠ 有实现，demo 素材缺 |
| **转场** | — | — | ❌ 没有 |

> **一句话：那个下拉框不是想象出来的，它是这仓库的现状，只是从来没人把它摆到一个地方。**

⚠ **但四种不是平级的。** 前三种是「这一镜的画面从哪儿来」，第四种是
「在一条已经出好的片子上再加一层」——**它的输入是成片，不是稿子**。
界面上不该并列，第四种属于「二次加工」，见 §五。

---

## 二、规范作为默认值：可行，但要分三级

你问的是对的方向。但**不是所有规范都能降级成默认值** —— 分三级，
而且**仓库自己已经是三级的**，只是没人把这件事说破。

| 级 | 是什么 | 界面怎么做 | 代码里现在的样子 |
|---|---|---|---|
| **① 硬约束** | **平台事实**：YouTube 时长角标 x≥1080∧y≥640、九宫格 `crop=1080:1440:0:240`、Shorts 只留 y∈[420,1500] | **不给开关。** 违反了报 error，改不了 | `laoma-long-cover.ts` 的 `SAFE`，越界直接 `level:'error'` |
| **② 频道识别** | 署名「碎嘴老马」、贴纸大字 `#FFD400`＋`#1A1A1A`、老马是云希不是晓晓、牌匾用站酷快乐体 | **可覆盖，但要留痕 ＋ 警告**。理由：改了不报错，但**做到第十条观众认不出是同一个号** | `ACCOUNT_LAOMA`、`T.fill`、`CASTS.老马` —— 都是常量，没人拦 |
| **③ 本期取舍** | 封面版式档、`centerK`、鱼的位置、场景、方版横排/竖排 | **本来就该在界面上**，随便调 | `LAYOUTS` ＋ `DEFAULT_LAYOUT` ＋ `cover.long.*` 单期覆盖 |

**所以「规范作为默认值」的正确说法是：**

> **界面暴露的不是「参数」，是「这个参数属于哪一级」。**
> ① 灰掉并说明为什么；② 能改但要写一句为什么；③ 随便改。

### 2.1 字幕设置这一栏，具体怎么落

我 v1 说「字幕是规范定死的，做成颜色选择器＝把规矩打开了」—— **说过头了**。分开看：

| 字幕的属性 | 级 | 理由 |
|---|---|---|
| 字号、颜色、描边色、描边粗细、位置 | **③** | 本来就是每条线各调各的（长片旁白 40 白字 8px 深墨边，台词 50 角色本体色 10px 场景底色边）。**放开** |
| 字体 | **②** | 换字体是换语气 —— 老马线用得意黑不用站酷快乐体，理由写在 `config.ts`：「圆滚滚的字自带『我在逗你笑』的语气，**字体先笑了台词就不好笑了**」。**能改，但要看见这句话** |
| **标点规则**（结尾留不留、句中句号换不换逗号、引号扶不扶正） | **②** | 这是频道规矩且**跨线不一样**：短片「结尾不留标点＋句中句号换逗号」，**长片明文不照搬**。做成开关可以，但**默认值必须跟着线走**，而且改了要留痕 |
| 断行规则（优先在标点后 / 每行字数上限） | **③** | 数值型，放开 |
| 安全区 | **①** | 见上 |

⚠ **最大的风险不是「能不能改」，是「改了不留痕」。**
这仓库的每一条规范都带着「为什么」——那些 ⚠ 注释全是返工换来的
（绳结被切掉、引号被删光、字距边只剩 5%）。**界面上一个下拉框不会带那段理由。**

**所以两条配套机制是必须的，不是锦上添花：**

1. **偏离默认值时，把那条规范的原话显示出来**（一句话，不是整节）
2. **偏离要落进成品目录**（`<项目>/.ui-overrides.json`），出片后回看得到「这一期为什么跟别的不一样」

---

## 三、素材库：已经很厚，缺的是一个入口

盘点（2026-08-26 实测）：

| 库 | 现状 | 在哪 |
|---|---|---|
| **字体** | 10 个文件 **170 MB**（思源宋体 7 个真字重 OTF、站酷快乐体、得意黑） | `fonts/` ＋ `config.ts` 的 `FONT_FILES` |
| **音色** | `cast.ts` **24 个具名音色**（含 morph 变声链）＋ voice-clone **58 条预设** ＋ 音染模块 | `joke-video/src/cast.ts`、`voice-clone/src/presets.ts` |
| **视频素材** | 手动 16 条 ＋ Pexels 缓存 **269 条 / 2.8 GB** | `assets/manual/`、`assets/cache/` |
| **环境音** | 9 条（风声／街道／办公室／键盘…，**电平差 20+ dB，脚本自己量**） | 仓库根 `voice/*.mp3` |
| **画面素材** | 老马 rig ＋ 摆件 13 个、治愈插画、说书符号、`_symbols/` 18 个 | `horse/`、`zhiyu/pictures/`、`projects/老马/_symbols/` |
| **色板** | 各线一份 | `style/palette.ts` 等 |
| **LUT** | `cold.cube` 132 KB | `assets/` |

**已经有一个能用的试听页**：`voice-clone` 的 `:5178`
（`server.ts` 117 行，音色预设／变声链／AB 对比都在）。
`网页版方案.md` §六 已经说了要把它**并进来当「试音」标签**，不是重做。

⚠ **音色库有一条纪律必须进界面**：共享预设**只读**，要变化就新增，改完跑 `npm run cast:check`。
界面上如果能直接改共享音色，**下一次别的线的片子就变了音**。

---

## 四、风格库 = 默认值包

「治愈系／老马／说书是不同风格的视频」—— 这个说法比我用的「线」更适合做界面。
**但要说清它到底是什么：**

> **一个风格 = 一整套默认值 ＋ 一个渲染器 ＋ 一套体检闸 ＋ 一个封面版式库。**
> 选了风格，四栏的默认值**全部被填好**；界面上剩下的是「这一期哪儿不一样」。

| 风格 | 画面来源 | 语速 | 体检 | 封面档 |
|---|---|---|---|---|
| 记者读稿 | 抓取／提供素材 | — | `preflight` | V3 `ink` |
| 段子 · 一页故事 | 自动勾勒 | — | `preflight` ＋ `cast:check` | 竖版米白纸片 |
| 老马（单点／累积／长片） | 自动勾勒 | 长片 **5.6 字/秒** | `laoma-check`（**长片跳过**） | 竖版贴纸／长片档 |
| 说书 | 自动勾勒 | 实测 **233.7 cpm** | `shuoshu-preflight` ＋ `voicecheck` | V3 `ink` |
| 治愈 | 自动勾勒 | **226 cpm** | `zhiyu-check` | V3 `paper` |
| 心理 · 禅佛典 | 自动勾勒 | **258 / 248 cpm** | `xinli-check` / `chan-check` | 窗格版 / V3 `night` |

⚠ **风格不能做成扁平下拉框。** 「线 → 线内体裁 → 版式」是三层，
上一层定了下一层的选项才成立（治愈线没有 `type:A/B`，段子线没有「上下分期」）。
理由见 `网页版方案.md` §2.1。**界面上是三级联动，数据上是一棵树。**

---

## 五、剪辑：思路整理（这条确实没有，先不动手）

「提供视频，进行剪辑」拆开是四件事，**仓库各有几分**：

| 要素 | 现状 | 差多少 |
|---|---|---|
| 导入任意视频 | `assets/manual/<keyword>.mp4` —— **靠文件名约定**，不是导入 | 要一个上传 + 落盘 + 登记 |
| 切片与排序 | `render.ts` 按时间轴自动切；**没有手动裁切** | 要时间轴 UI ＋ 每片的 in/out |
| 叠加图层 | ✅ `video-pipeline/` overlay + assemble，**底片不动、音轨原样搬运** | 已有，缺 demo 素材 |
| 转场 | ❌ 无 | ffmpeg `xfade` 能做，但**要先想清楚要不要** |
| 预览 | ❌ 只能整片渲（长片 485 秒） | **这是真正的拦路虎**，见下 |

**先想清楚的三件事：**

1. **⚠ 预览比剪辑本身难。** 这条管线是「渲完才能看」，长片 4180 帧 485 秒。
   剪辑要的是**秒级反馈**。可行的路子是**代理预览**：低分辨率 + 低帧率
   （比如 360p / 6fps）先出，定稿再全量渲。**这一件不解决，剪辑界面就是个摆设。**
2. **转场要不要，是内容判断不是技术判断。** 这六条线现在一个转场都没有，
   而且长片方案明写着「没有画面变化的地方，静场不超过 1.5 秒」——
   节奏是靠停顿和换镜做的。**加转场之前先问它替代什么。**
3. **剪辑落在哪一层。** 建议是**第四种画面来源**（叠加到已有底片），
   不是第五条线 —— 它的输入是成片，跟「从稿子出片」是两个入口。

**建议：这一块只出思路，不进第一刀。** 等预览的代理链路有了再说。

---

## 六、界面形状：参考图的四栏，换成仓库自己的选项

```
┌ 风格 ─────────────┐  ← 先选这个，下面全部被填好默认值
│ 老马 › 长片 › 横版 │
└───────────────────┘

┌ 文案设置 ────┐ ┌ 画面设置 ──────┐ ┌ 音频设置 ────┐ ┌ 字幕设置 ────┐
│ 主题/选题     │ │ **画面来源** ▾ │ │ 音色 ▾（24）  │ │ 字体 ▾ ②     │
│ AI 生成文案   │ │  · 自动勾勒 ✓  │ │  试听 ▷       │ │ 字号/色/描边 ③│
│ 稿件（可贴）  │ │  · 自动抓取    │ │ 语速（线定）  │ │ 标点规则 ▾ ② │
│ **AI 出 clip**│ │  · 提供素材    │ │ 环境音（9）   │ │ 断行 ③       │
│ 关键词(英文)  │ │ 场景 ▾（6）    │ │ BGM           │ │ 安全区 ① 🔒  │
│               │ │ 比例 ▾         │ │ 音量/电平     │ │              │
└───────────────┘ └────────────────┘ └───────────────┘ └──────────────┘

┌ 封面 ─────────────────────────────────────────────┐
│ 版式档 ▾（版式库）  ▣210px ▣200px ← 主位是缩略图   │
└───────────────────────────────────────────────────┘

体检 ✓ → 试听 ▷（人确认）→ 静帧 ▣（人确认）→ 封面 ▣（人确认）→ [出片]
                                                        ↑ 三格没点，灰的
```

**跟参考图的三处不同，每一处都有理由：**

| 参考图 | 这儿 | 为什么 |
|---|---|---|
| 四栏平铺 | **上面多一个「风格」** | 四栏的默认值全从它来。没有它，四栏就是通用旋钮 |
| 底部一个「生成视频」 | **一条带人工关卡的进度条** | 长片渲 485 秒、说书 10–21 分钟。**没点过「听过了」出片按钮是灰的** —— 这是 `RUNBOOK` §五「只有人能查的」变成界面的那一下 |
| 字幕设置全是旋钮 | **标 ①②③ 三级，① 上锁** | 平台事实不能当偏好调 |

⚠ **「生成视频」那个按钮我仍旧建议不做成一键到底**，但 v1 说得太绝对。
**折中：按钮在，但它是「从当前这一步往下跑」，而且人工关卡会挡住它。**
这样既有一个触发点，又不会让人拿八分钟的渲染当调试。

---

## 七、顺序与工作量（重算）

```
① 登记表（导出 2 条 ＋ 补 4 条 ＋ 逐条验证）      3.5   ← 底座
② Skill（SKILL.md ＋ 三份跨线 reference）        2
③ 素材库一页（字体/音色/视频/环境音，含并入 :5178） 3     ← 纯读，零风险
④ 封面台（实时预览，版式档可换）                 2     ← 已复核便宜了
⑤ 风格 → 默认值包（三级联动 ＋ ①②③ 分级）        3     ← 新，是四栏的地基
⑥ 四栏界面（只读 + 预览，不写盘）                3
⑦ 工作台（进度条 ＋ 人工关卡 ＋ 出片按钮）        3     ← 依赖 ①
                                          ─────────
                                           合计 19.5 人日
剪辑（含代理预览链路）                        ？     ← 先只出思路，见 §五
```

**建议的第一刀 = ③ ＋ ④ ＝ 5 人日**：两页都是**纯读、零写盘风险**，
而且素材库那一页正好用来验证「六条线的素材到底能不能收进一个视图」——
这是 ⑤⑥ 成立的前提。

---

## 八、明确不做

| 不做 | 原因 |
|---|---|
| 一键从需求直接到成片 | 会把「build 是交付动作」冲掉。按钮可以有，但**人工关卡挡在前面** |
| 让界面改**共享音色预设** | 改了别的线的片子会变音（`cast:check` 那条纪律） |
| 把①级（平台事实）做成可调项 | 那不是偏好，改了就是错 |
| 偏离默认值不留痕 | 这仓库的规范都带「为什么」，界面不会带 —— 所以必须落 `.ui-overrides.json` |
| 现在就做剪辑时间轴 | **预览没有代理链路之前，剪辑界面是摆设** |
| 现在就加转场 | 六条线一个转场都没有，节奏靠停顿做的。**先问它替代什么** |
| 手写步骤登记表 | 六分之二已经是代码，手抄＝制造分叉 |

---

## 九、要确认的五件事

1. **第一刀是不是 ③素材库 ＋ ④封面台（5 人日）**，还是先要 ⑤⑥ 那个四栏界面
2. **②级（频道识别）到底给不给改** —— 我建议「给改 ＋ 留痕 ＋ 显示原话」，
   但也可以更严（只读，要改去改代码）
3. **「出片」按钮的形态** —— 我建议「从当前步往下跑 ＋ 人工关卡挡住」，
   要不要更接近参考图那种一键
4. **剪辑这条线的优先级** —— 现在只出思路，还是要排进计划
5. **风格的第三层（版式）要不要在四栏里露出来** —— 露出来更全，但三级联动会更绕

---

## 十、要不要单独一个项目？—— 不要，但要独立 workspace

### 10.1 决定性的理由只有一条：常量

界面必须读这些东西才能给出默认值：

| 要读什么 | 出处 |
|---|---|
| 六条线的路径 | `joke-video/src/paths.ts` · `src/config.ts` |
| 音色表（24 个）与变声链 | `joke-video/src/cast.ts` |
| 语速 `cpm`（226 / 258 / 248 / 233.7） | `joke-video/src/zhiyu-lines.ts` |
| 封面版式库、字号上下限、安全区 | `joke-video/src/laoma-long-cover.ts` 的 `LAYOUTS` / `L` / `SQ_SAFE_K` |
| 贴纸大字的色与比例 | `joke-video/src/cover.ts` 的 `T` |
| 字体族与文件 | `joke-video/src/config.ts` 的 `FONT_*` |

**跨仓库就只能抄一份。** 而这仓库为「抄一份」栽过至少三次，每次都留了记录：
`zhiyu-lines.ts` 的 `cpm`、`plaque.mjs` 的绳结经验（长片重踩了一遍，
因为那条经验只写在单点式的代码里）、`tidyCaption()` 的标点规则（长片差点套上短片那条）。

> **独立仓库能买到的隔离，`workspace` ＋ 单向依赖同样买得到；
> 独立仓库要付的「抄一份常量」，`workspace` 不用付。**

### 10.2 先例已经在跑

`voice-clone` 就是这么做的：独立 workspace、自己的 `package.json`、
自己的 `:5178`（`server.ts` 117 行）、import 仓库的东西。**跑了很久，没影响过出片。**
`网页版方案.md` §五 也是这么排的（`web/` 加进根 workspaces）。

### 10.3 隔离靠四条纪律，不靠仓库边界

| # | 纪律 | 怎么验 |
|---|---|---|
| 1 | **单向依赖**：`web/` import 管线，**管线永远不 import `web/`** | `grep -rn "from '.*web/" src/ joke-video/src/` **必须为空**。删掉整个 `web/`，出片一行不受影响 |
| 2 | **默认值只读不抄**：界面的每个默认值都 import 自代码 | web 里出现硬编码的规范数值（`150` / `0.062` / `'#FFD400'`）就是 bug |
| 3 | **前端依赖只进 `web/package.json`** | React/Vite 不出现在根和 `joke-video` 的依赖里 |
| 4 | **CLI 永远可用** | 网页跑的是同一条命令，终端照样能跑。**网页坏了不影响出片 —— 这是它能被信任的前提**（`网页版方案.md` §十.5） |

---

## 十一、跟「改规范」「出档期片」会不会打架

### 11.1 档期压力：量出来是三周缓冲

| 线 | 已排期在未来 | 频次 | 缓冲 |
|---|---|---|---|
| 老马 · 段子 | **7 期**（2026-08-27 → 09-17） | 周二／周四／周日 21:00 | **≈ 3 周** |
| 老马 · 未排期成片 | 4 期 | — | 再加一截 |
| 醒木不响（说书／治愈／禅佛典） | 待发 1 期，已发 18 | 一周五更（说书占三档） | **薄** |

**结论：有缓冲，但不厚。** 所以三条硬的：

1. **档期日（周二／周四／周日）不装依赖、不动管线脚本。** `npm install` 在档期日炸了会连累出片
2. **第一刀那两页（素材库 · 封面台）一行不写盘** —— 冲突面直接为零
3. **渲染是单并发的**（`网页版方案.md` §九）。界面**不排渲染队列**，
   只做纯读 ＋ 秒级预览（封面 0.3 秒、扫盘毫秒级）。抢 CPU 的活留给 CLI

### 11.2 ⚠ 真正的冲突面是「规范在变」，不是「档期在跑」

**规范每周都在动。** 光是 2026-08-26 这一天就改了：
`封面设计规范-COVER.md`（加了铁律 4、§六之二 方版、§七 版式库）、
`长片_出片方案.md`（标牌四处、标点两处、srt 一节）、
`YouTube封面规范.md`（加了长片档）。

**界面把规范固化成默认值 → 规范一变界面就旧 → 第二个真相。**
解法不是「同步」，是 §10.3 那条 **不抄**。

### 11.3 但规范里有一大半不在代码里 —— 这条才是分界线

| 规范原文 | 在代码里吗 | 界面能做什么 |
|---|---|---|
| 「大字 4–7 字」「字号下限 109」「安全区 10%」 | ✅ 有断言 | **可以做成控件**，越界报错 |
| 「大字是钩子，不是书名」 | ❌ 只在 md | **只能做成提示**（把原话显示出来） |
| 「圈的是对照关系的两端，不是形容词」 | ❌ 只在 md | 同上 |
| 「答不上来就是没写好，不是风格问题」 | ❌ 只在 md，而且**明写着机器查不了** | 做成**四个必填的填空**（`网页版方案.md` §3.4） |

> **判据：能被 `*-check.ts` 查的 → 可以做成控件；只在文档里的 → 只能做成提示。**
> 把只在文档里的规矩做成控件，等于给了它一个「可以调」的错觉。

### 11.4 而且这是正反馈，不是冲突

**界面不是规范的消费者，是规范的压力测试** —— 它逼着「只在文档里的规矩」
变成「代码里守得住的规矩」。

**2026-08-26 就是活例子**：排版安全距离本来一条都没有（方版第一版字距边只有 5%），
用户指出来之后，它同时变成了三样 —— `SQ_SAFE_K` 常量、一条 `level:'error'` 的闸、
以及 `封面设计规范-COVER.md` §一 的**第四条铁律**。

**规范落进代码，界面才拿得到；界面要拿，就会推着规范落进代码。**
所以做界面这件事本身，会让「完善视频规范」这条线走得更快，不是更慢。

⚠ **但有个前提**：落进代码的必须是**可判定的那部分**。
「人味儿」那一条落不进去，也不该硬落 —— 它在界面上的正确形态是四个填空，不是一个开关。

---

# 第二部分 · 参数化（2026-08-26 追加）

> **前提变了**：这套界面**后续要面向不同用户**。
> 那么上面 §二 那个「①硬约束／②频道识别／③本期取舍」的分级**就不成立了** ——
> ②级是以「只有醒木不响一个用户」为前提才成立的。
> 对别人来说，署名「碎嘴老马」、老马是云希、贴纸黄 `#FFD400`
> **不是「要警告的偏离」，那是别人的默认值。**

## 十二、重新推导分级：轴换成「谁的约束」

| 级 | 谁的约束 | 对新用户意味着 | 例 |
|---|---|---|---|
| **A · 平台事实** | 平台定的 | **硬，但跟着选的平台变** | YouTube 时长角标 x≥1080∧y≥640；抖音底部 20%／右侧 15%；九宫格 `crop=1080:1440:0:240`；Shorts 只留 y∈[420,1500]；比例、时长上限 |
| **B · 物理事实** | 可读性 / 听感 | **硬，可越权但要报** | 210px 下描边 ≥2.7px 才看得见；对比度 WCAG ≥3.2；CJK 上伸 0.88；静场 >1.5s 就是卡住；字体缺了静默变细 |
| **C · 频道包** | **醒木不响自己的** | **整包可替换，也能新建** | 署名、音色表、配色、字体、标点规矩、封面版式库、闸的阈值、甚至「要不要这条闸」 |

> **核心结论：「醒木不响」必须从代码里降级成一份数据。**
> 现在它是散在 `config.ts` / `cast.ts` / `cover.ts` / `palette.ts` 里的一堆常量。

## 十三、可行性：已经做了一大半，而且做法是对的

### 13.1 参数面盘点（实测）

| | 数量 | 例 |
|---|---|---|
| **稿件级参数**（`JokeCfg` 顶层） | **约 100 个字段** | `title` `cover` `characters` `pace` `bgm` `ambience` `scene` `camera` `format` `type` … |
| **逐句参数**（`LineCfg`） | **28 个** | `cast` `delivery` `tone` `padBefore/After` `look` `turn` `emote` `silent` `scene` … |

**你点名的四样，三样已经是参数：**

| 你说的 | 现状 | 缺什么 |
|---|---|---|
| **标题** | ✅ `cfg.title` ＋ `cover.title` | — |
| **副标题** | ✅ `cover.sub` | — |
| **署名** | ✅ `cover.tag` 单期覆盖 ＋ `JOKE_ACCOUNT` 环境变量 | **默认值** `'碎嘴老马'` 还是常量 |
| **音色** | ✅ `characters[].cast` / `line.cast` 单期指定 | **音色表本身**（24 个）是常量，**不能扩** |

### 13.2 覆盖链已经是三段，只差最前面那一段

```
代码常量  →  环境变量  →  稿件字段
 ↑ 要换成                （JOKE_ACCOUNT / JOKE_FONT / JOKE_FONT_HEAVY 都在）
 「从频道包读」
```

**所以这不是重构，是把常量搬个家。** 覆盖机制不用发明，它已经跑了几十条片。

### 13.3 还硬编码的清单（要搬的东西）

| 项 | 在哪 | 搬的难度 | 备注 |
|---|---|---|---|
| 署名 | `config.ts` `ACCOUNT` / `ACCOUNT_LAOMA` | 易 | 已有 env 覆盖 |
| 字体族与文件 | `config.ts` `FONT` / `FONT_HEAVY` / `FONT_LAOMA` / `FONT_FILES` | 易 | 已有 env 覆盖 |
| 贴纸大字的色与比例 | `cover.ts` 的 `T`（`fill` `line` `strokeK` `lineH` `trackK`…） | 易 | 纯常量对象 |
| 配色 | `style/palette.ts` 的 `P`、`inkwash.ts` | 中 | 多线共用，要分线还是分频道得定 |
| **音色表** | `cast.ts` 的 `CASTS`（24 个 ＋ 变声链） | **中偏难** | 要支持「**继承基础库 ＋ 频道自己新增**」，见 §14 |
| 电平与增益 | `config.ts` `PEAK_DBFS` / `GAIN` | 易 | |
| **标点规矩** | `shuoshu-srt.ts` 的 `tidyCaption` / `long-caption.ts` | **中** | 跨线不一样，本来就该是数据 |
| **闸的阈值** | 9 个 `*-check.ts` 里散着 | **难** | 字数区间、段长、停顿密度、分幕下限…**这一项最大** |
| 封面版式库与尺寸 | `laoma-long-cover.ts` 的 `LAYOUTS` / `L` / `SQ_SAFE_K` | 中 | `L` 里混着 **B 级物理事实**（`minFs` 109）和 **C 级偏好**（`centerK`）—— **要拆开** |
| 语速 `cpm` | `zhiyu-lines.ts` | 易 | |

⚠ **`L` 那一行是这张表里最该注意的**：`minFs: 109` 是**物理事实**（210px 下描边 2.7px 才看得见），
`centerK: 0.46` 是**构图偏好**。它们现在在同一个对象里。
**参数化的第一步不是搬，是先把 B 和 C 分开** —— 混着搬，新用户就能把物理事实调坏。

## 十四、音色表：参数化里唯一有纪律要守的一项

`cast.ts` 有一条现成的纪律：**共享预设只读，要变化就新增，改完跑 `npm run cast:check`。**
多用户之后这条更重要 —— **一个用户改了共享音色，所有人的片子都变音。**

所以音色的正确形状是**两层**：

```
基础库（只读，跨用户共享）  voice-clone/presets.ts 的 58 条 ＋ cast.ts 的 24 个
        ↓ 继承
频道音色表（用户自己的）    channels/<频道>/casts.json  ← 只放"改了什么"
        ↓ 覆盖
稿件里的 line.cast          ← 已经有了
```

⚠ **不许「编辑基础库」这个动作存在于界面上。** 界面上只有「基于 XX 新建一个」。

## 十五、⚠ 验收判据：字节级回归

参数化最大的风险不是做不出来，是**搬完之后出来的片子不一样了，而且没人发现**
（这条管线大多数错误不报错，只是结果不对）。

**判据**：把常量搬成数据、频道包填上**现在的值**，重跑已出片的期，
**产物逐字节一致**。

**这招这次会话已经用过**：改了 `longform.cjs` 的 `frame()` 之后，
`md5sum -c` 验了 7 张样张逐字节一致 —— 那是「画法一个像素没动」唯一站得住的证据。

**所以第 0 步是建回归基线**，不是改代码：

```bash
# 选 3–5 期已出片的，锁住产物的 md5
npm run regress -- --record     # 建基线
npm run regress                 # 每次搬完跑一遍
```

候选：老马长片 001（out.mp4 ＋ 4 张封面 ＋ srt）、老马单点式一条、
说书 E05、治愈 taoan-E01 —— 四条线覆盖四种渲染器。

⚠ **有基线才敢在档期跑着的时候动这些常量。** 没基线就只能等档期空窗，
而档期缓冲只有三周（§11.1）。

## 十六、三样不能参数化，但要说清为什么

| 不能参数化 | 为什么 | 界面上的正确形态 |
|---|---|---|
| **可判定性** —— 「人味儿」四条硬判据 | 规范自己写着「**这一条机器基本查不了**」 | 四个**必填的填空**，不是四个复选框（复选框会被随手勾掉） |
| **平台事实（A 级）** | 时长角标的位置不是偏好 | 跟着平台变，不跟着用户变 |
| **物理事实（B 级）** | 210px 下 2.4px 的描边就是糊，不因为用户想要而不糊 | 可越权，但**必须报**，并写进 `.ui-overrides.json` |

⚠ **但有一条要小心：不能因为要参数化，就把每个闸都配一个「忽略」按钮。**
长片方案原话：**天天报错的闸门只会教人加 `--anyway`**（那样连另外两条有用的闸也一起失效）。

**正确做法**：闸的**阈值**可配、闸的**存在与否由频道包声明**；
但**不给每次运行一个「这次忽略」**。

## 十七、⚠ 多用户还牵出一件路径的事

现在所有成品在 `projects/<类型>/<期>/`。**多用户就要 `projects/<频道>/<类型>/<期>/`。**

- 改动只在两处（`paths.ts` ＋ `src/config.ts`），这是仓库自己的纪律
- **但会动到所有已出片的目录**，而 `preview.ts`、`deliver.ts`、`laoma-schedule.ts`
  都在按现在的形状反查
- ⚠ `zhiyu-ep.ts` 顶上那句在多租户下更狠：
  「忘了改哪一个，那一步就把新书的产物写进方丈记的目录 —— **这是数据损坏，不是麻烦**」

**建议**：**第一刀不做多租户的目录**。先让频道包跑起来（单租户、一份包），
路径那一层等真有第二个用户再动 —— 那时候回归基线也已经有了。

## 十八、分步与工作量（在 §七 的 19.5 之上追加）

```
⓪ 回归基线（选 4 期，锁 md5，写 npm run regress）      0.5   ← **必须第一个做**
Ⓐ 把 L 那类对象里的 B 级和 C 级拆开                     1
Ⓑ 频道包 schema ＋ 从现有常量导出一份，断言跟常量一致    1
Ⓒ 代码改成读频道包（常量留作 fallback），跑回归          2
Ⓓ 音色两层（基础库只读 ＋ 频道表继承）                   1.5
Ⓔ 平台包（遮挡区／比例／时长，跟着选的平台变）           1
                                                  ─────
                                                   7 人日
```

⚠ **这里原先写的是 6，加错了**（0.5+1+1+2+1.5+1 = 7）。
**总账不在这儿算** —— §七、§十八、§二十四 三处的任务有重叠
（「封面台」和「封面预览」是同一件，「素材库」含了「音频试听」）。
**去重之后的唯一一张任务表在 [`可视化界面方案.md`](可视化界面方案.md) §六。**

**顺序上唯一不能动的是 ⓪。** 其余每一步做完都要跑一次回归。

## 十九、要确认的（追加三条）

6. **多租户的目录要不要现在就动** —— 我建议不动（§17）
7. **配色是按线分还是按频道分** —— 现在六条线各一份色板，
   多用户之后是「一个频道一套色」还是「一个频道的每条线各一套」
8. **闸的存在与否谁说了算** —— 我建议频道包声明，但**不给"这次忽略"**（§16）

---

# 第三部分 · 四件交互（2026-08-26 追加）

> 多用户**先列入计划，不进这一刀**（§17 已经建议不动多租户目录，这里确认）。
> 这一节只看四件：**上传 SVG 指定角色 · 音频试听 · 封面预览 · 场景画风预览**。
>
> **它们有一条共同的结构：全是「纯读 ＋ 秒级 ＋ 一行不写盘」。**
> 所以这四件正好构成第一刀 —— 冲突面为零（§11.1 那三条硬纪律天然满足）。

## 二十、上传 SVG 素材、指定人物角色

### 20.1 好消息：契约已经写下来了，而且被复制过一次

`horse/bull-side/BULL_HANDOFF.md` 把 rig 的契约写得很完整：

```
#bull
├ #body                    躯干+双臂+双腿（一条连续轮廓）
└ #head          transform 钩子，扭头缩放这一层
  ├ #head-shape
  ├ #face        transform 钩子，扭头时额外横移做视差
  │ ├ #eye-white-l / -r
  │ ├ #pupil-l / -r        transform 钩子，转眼珠
  │ ├ #mouth-closed        默认显示
  │ └ #mouth-A / -I / -O / -E   默认 display:none
  └ #head-front            压在最上层（牛角）
```

**老牛就是照着老马这套契约做出来的** —— 所以「再加一个角色」不是新问题，
**是已经走过一遍的路**，只是那一遍是手工走的。

### 20.2 ⚠ 但有两个契约，轻重差一个数量级

| 渲染器 | 要什么 | 上传 SVG 够不够 |
|---|---|---|
| **长片**（`longform.cjs` 的 `loadRig`） | **只要 `viewBox` ＋ 五个 `#mouth-*` id** | ✅ **够，传上去就能用** |
| **短片**（`horse-pose.mjs`） | 完整分组 ＋ **六个支点坐标** | ❌ **不够** |

短片那六个支点**硬编码在驱动里**：

```js
// horse/horse-pose.mjs
const HEAD_CX = 352;   // 扭头的垂直轴
const DEPTH   = 46;    // 视差量
clamp(eyeX, -26, 26);  // 眼珠位移上限
clamp(eyeY, -22, 22);
```

而老牛那一套是 **`450 / 56 / ±32 / ±26`，写在 handoff 文档里，没有对应的驱动**。

> **证据：老牛在短片稿件里是 `rig: 'none'`。** 他进不了短片渲染器 ——
> 不是因为画不出来，是因为**没人把他的支点搬进驱动**。

**所以「上传 SVG 指定角色」的真实成本 = 把支点从驱动里搬进 rig manifest**
—— 跟 §十三 那个参数化是同一件事，同一个做法。

### 20.3 方案：svg ＋ manifest ＋ checker ＋ 预览

```
uploads/rigs/<名字>/
  rig.svg
  manifest.json   { viewBox, headCx, depth, eyeClamp:[x,y],
                    eyeL:[x,y], eyeR:[x,y], mouthCenter:[x,y] }
```

**checker 能查的（全是可判定的，一条都不该漏）：**

| 查什么 | 判据 |
|---|---|
| `viewBox` 存在且四个数 | 正则 |
| 五个口型 id 齐 | `#mouth-closed` `-A` `-I` `-O` `-E` |
| 默认只显示 `mouth-closed` | 其余四个带 `display:none` |
| 眼与瞳孔分组齐 | `#eye-white-l/r` `#pupil-l/r` |
| 外接框裁紧 | 渲一张，扫非透明像素的外接框 ÷ viewBox ≥ 某个比例 |
| **口型切换真的有变化** | 分别渲 `closed` 和 `A`，**比 PNG 字节数** ——「不报错只是没变」是这条管线的常见失败 |

⚠ **checker 查不了的**：那六个支点数对不对。
眼珠会不会飘出眼眶、扭头 ±25 度穿不穿帮 —— **只能渲出来让人看**。
**这正好由第 ④ 条（预览）兜住**，见 §23。

---

## 二十一、音频试听 —— 最成熟的一条，已经有一个跑着的页面

| 现成的东西 | 在哪 |
|---|---|
| 试听页（音色／变声链／**AB 对比**） | `voice-clone` 的 `:5178`，`server.ts` **117 行** |
| 基础音色 24 个 ＋ 预设 58 条 ＋ 音染模块 | `cast.ts`、`voice-clone/src/presets.ts` |
| 单句试念 / 列音色 / 整条试听 | `npm run try` / `voices` / `cast` / `audio` |
| 整条试听音轨（长片还顺带出 `.srt`） | `npm run audio` |

**成本 ≈ 并进来，不是重做**（`网页版方案.md` §六 本来就是这么排的）。

⚠ **一条必须显示在界面上的环境状态**：`ffmpeg` 缺 `rubberband` 会让**变声静默降级**
—— 出来的音色跟调好的**不是一个人**，而且不报错。`doctor.ts` 已经在查了，
**界面要把它显示出来**，否则试听听到的跟出片出来的是两回事。

---

## 二十二、封面预览 —— 已复核，最便宜的一条

- 单张 1280×720 光栅化 **不到 0.3 秒**（`video.ts` 实测：1080×1920 是 240ms，**99.6% 是 resvg**）
- `yt-cover.ts` / `laoma-long-cover.ts` **导出返回 Buffer 的函数，连盘都不用落**（§3.2）
- 长片一次出四张：`16x9` / `210` / `1x1` / `200`，还带版式库可换档

**所以是「改标题就重渲」的实时预览，不是「点一下等一会儿」。**

⚠ **主位是缩略图（210 / 200），大图次位。** 规范原话：
「**大图好看小图认不出等于没有**」，后面记着两次翻车（井口第一版被看成气球、竹简像相机镜头）。

---

## 二十三、场景画风预览 —— 已经有 CLI 入口，成本最低

```bash
npm run scene -- shore            # 单渲一个场景成 PNG，**不需要稿件**
node horse/长片/longform.cjs      # 长片六个场景，一次七张样张
```

**场景库盘点（实测）：**

| 线 | 场景数 | 能不能空跑 |
|---|---|---|
| 段子 · 一页故事 | 10（grass / room / office / street / abstract / shore / hillside / hillrocks / pond / eaves） | ✅ `npm run scene` |
| 老马短片 | 10（office-desk / meeting-room / rental-living / kitchen-table / bedroom / elevator / subway / hospital / home-living / street-night） | ✅ 同上 |
| 老马长片 | 6（office_dawn / office_day / corridor / dinner / street_dusk / office_night） | ✅ `frame()` 可 import，我这次会话直接调过 |
| 说书 · 治愈 · 心理 · 禅佛典 | 程序化，跟稿件绑 | ⚠ **不能空跑**：`shuoshu-scene` 要 `chapters`，`zhiyu-scene` 要 `episode` 的 manifest |

**合计 26 个具名场景可以直接铺成一面墙。**

⚠ **那四条不能空跑的线，要给一份样例稿**（每条一份最短的、只为出图用的）。
不给的话界面上那四条线就是空的，而**空白比"没有这个功能"更让人以为是坏了**。

### 23.1 场景预览顺带兜住 §20 的洞

上传 rig 之后 **立刻用它渲一张场景预览** ——
眼珠飘不飘出眼眶、扭头穿不穿帮，checker 查不了，**一眼就看得出**。

> 这是「**机器查可判定的，人看不可判定的**」那条原则的又一次落地
> （体检查字数、人听调子；封面查安全区、人看缩略图）。

---

## 二十四、这四件的工作量与顺序

```
③ 封面预览        2     ← §22，最便宜
④ 场景画风预览    1.5   ← §23，有现成 CLI；＋0.5 给那四条线的样例稿
② 音频试听        1.5   ← §21，并 :5178 进来，不重做
① 上传 rig        3     ← §20：checker 1 ＋ manifest/支点搬家 1.5 ＋ 预览接线 0.5
                 ────
                  8 人日
```

**四件都是纯读 ／ 秒级 ／ 不写盘**（rig 上传是唯一写盘的，但写的是 `uploads/`，
**碰不到 `projects/` 和 `jokes/`** —— 数据损坏那条风险绕开了）。

**建议第一刀 = ③ ＋ ④ ＝ 3.5 人日**：两件都不改任何管线代码，
只是把已有能力接出来。跑顺了再上 ② 和 ①。

⚠ **① 依赖 §十三 那个「支点搬家」**，所以它排最后 —— 它不是一件交互，
**是一次参数化，只是长着交互的样子**。

## 二十五、多用户：列入计划，不进这一刀

确认 §17 的建议：**先单租户**。理由不变 ——
`projects/` 分租户会动到所有已出片的目录，而 `preview.ts` / `deliver.ts` /
`laoma-schedule.ts` 都在按现在的形状反查；`zhiyu-ep.ts` 那句
「写进别的书的目录 —— **这是数据损坏**」在多租户下更狠。

**触发条件**：等 ⓪ 回归基线（§15）有了、频道包（§13）跑起来了，
再动 `paths.ts` 那一处。**那时候「搬完是不是还一样」是能自动验的。**
