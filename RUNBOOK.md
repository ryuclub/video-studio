# 出片手册

一条片子从选题到成片的完整流程、每一步要人看什么、以及踩过的坑。
最后一次更新：2026-08-16（`projects/记者读稿/2026-08-16_访日客五年首降，经营管理签证还稳吗`）。

README 讲的是「这套东西怎么用」，这份讲的是「今天这条片子是怎么出的、下次怎么更快」。

> **这份只管空镜解说风那条线**（代码 `src/`，成品 `projects/记者读稿/`）。
> 四条线的成品都收在根级 `projects/<类型>/` 下，见 [projects/README.md](projects/README.md)。
> 另外四条线各有各的手册，管线完全不同，别串着看：
> · **说书线**（`shuoshu/` + `joke-video/src/shuoshu-*.ts`）→ [`shuoshu/出片手册.md`](shuoshu/出片手册.md)
> · **段子线**（`joke-video/`）→ [`joke-video/README.md`](joke-video/README.md)
> · **儿童故事线**（`joke-video/`，旁白叙述型）→ [`joke-video/儿童故事出片方案.md`](joke-video/儿童故事出片方案.md)
>   一条命令：`npm run ship -- jokes/<稿件>.json`
> · **治愈系旁白线**（`zhiyu/` + `joke-video/src/zhiyu-*.ts`）→ [`zhiyu/治愈系出片方案.md`](zhiyu/治愈系出片方案.md)
>   单人女主讲、清新画风、45–60 分钟助眠档。**第一期《方丈记》进行中**
>
> **换机器或新人上手**：先 `npm run doctor` 自检环境，要求见 [README 的「运行环境」](README.md#运行环境)。

---

## 一、一条片子的时间账

3 分半的片子（约 50 行 / 1100 字 / 20 个镜头），从选题到两版成片：

- **机器时间约 15 分钟**，其中渲染占 10 分钟
- **人的时间 20–40 分钟**，全在审稿上 —— 这才是真成本

片长翻倍，渲染时间大致也翻倍（渲染时长基本跟成片长度成正比）。
只出一版竖版的话机器时间降到约 6 分钟。

| 步骤 | 命令 | 耗时 | 产出 |
|---|---|---|---|
| 写稿 | `npm run vg -- script --topic "…"` | 1–2 分钟 | `script.json` |
| **人工审稿** | — | **这里才是真成本** | 改 `script.json` |
| 配音 | `npm run vg -- tts <项目名>` | **45 秒**（51 行实测 36 秒） | `voice/` `voice.mp3` `timeline.json` |
| 素材 | `npm run vg -- footage <项目名>` | 新关键词 3–5 分钟（卡在下载），全命中缓存约 10 秒 | `shots.json` `credits.txt` |
| 渲染 | `npm run vg -- render <项目名> --profile both` | **约 10 分钟**（横 5.5 + 竖 4.5，实测三次都一样） | `subtitle_*.ass` `voice_mix.m4a` `landscape.mp4` `portrait.mp4` |

后三步可以一条命令走完：`npm run vg -- all <项目名>`。
**但第一次出片建议分开跑** —— 分步跑才能在配音出来后先听一遍，不合适就改稿重跑，
不用白等 10 分钟渲染。

改了管线代码，先 `npx tsx src/smoke.ts`（不联网、不烧钱、20 秒）。

> **写稿这一步现在跑不了**：`.env` 里的 `ANTHROPIC_API_KEY` 还是占位符 `sk-ant-...`。
> 填上真 key 才能用 `npm run vg -- script`；不填就得自己写 `script.json`
> （2026-08-16 那条就是手写的）。

**渲染每次都会把所有镜头重新归一化一遍**（`work_*/seg_*.mp4`），
所以重渲不比首渲快 —— 改一个字也是 10 分钟。想省时间就一次把要改的都改完。

---

## 一之二、两种风格

仓库里现在有两条并行的管线，靠 `script.json` 顶层的 `style` 分流：

> **2026-08-17 起：纯 PPT 风暂停使用** —— 整片都是版式屏，太单调。代码和已出片都留着，
> 新选题一律走空镜风；需要「图」的地方用 `video-pipeline` 的动效面板叠上去。

| | `style` 缺省（空镜解说风） | `"style": "slide"`（PPT 演示风） |
|---|---|---|
| 适合的选题 | 人物、场景、历史叙事 | **政策、数字、流程** |
| 画面 | Pexels 空镜 + 运镜 + 冷调 | 米白纯色底 + 九种版式 |
| 配音 | 逐行合成 | 整条合成 + 词级时间戳 |
| 渲染 | 约 10 分钟 / 90 MB | **31 秒 / 8.6 MB** |
| 素材成本 | 3–5 分钟下载 + Pexels 配额 | **零** |

命令完全一样（`tts` / `render` / `all`），管线自己认。
PPT 风的设计与踩坑见 `history/PPT-STYLE.md`（已停用，见 history/README.md）。**注意 PPT 风的解说词不能出现
外文缩写和百分号**，TTS 归一化会让词级时间戳跟原文对不上，管线会直接报错。

---

## 一之三、视频动效

动效一律通过 `./video-pipeline` 生成，**禁止另起炉灶**：

- 不要 pip/npm 装绘图库，不要用 matplotlib、canvas、headless browser
- 场景类型和字段以 `video-pipeline/src/types.ts` 为准
- 新增场景 → 在 `src/layers/scenes.ts` 加一个 `(t, i, total) => svgString` 函数
- 拼接改动前先读 `src/assemble.ts` 顶部注释（xfade 有静默丢数据的坑）
- 字幕时间按 `build/timeline.json` 对齐，不要用 storyboard 的 `start`

生成动效：

```bash
node dist/build.js render <storyboard> --out <file>
# 叠加到已有底片（改现有片子用这个，底片不动、音轨原样搬运）
node dist/build.js overlay-onto <底片.mp4> --spec overlays.json --out final.mp4
```

密度上限（`overlay.ts` 会告警）：4 分钟主片 **4–6 个**动效、同类 **≤2**、
相邻至少 **25 秒**、同窗口优先级 `地图 > 时间轴 > 图表 > 数字 > 清单 > 金句`。

**稿件里出现「一/二/三」就上序号卡（`steps`）。** 行首匹配
`/^(?:第)?([一二三四五六七八九十])(?:条|点)?(?:[，,、。：:]|(?<=条))/`，
再加三个成组条件：序号从「一」连续递增、**2–5 条**、
`duration = 末条 start − 首条 start + 4s` **≤ 30 秒**。跨度超了就是一段解说而不是一张卡。

四份现有稿子实测零误命中（`第二天，``第二年，``战后第一次``别表第一之二` 都不命中）：

| 稿子 | 组 | 判定 |
|---|---|---|
| 永住 · 四种情况（行 31–34） | `164.3 → 199.0` | ✅ 28.8s |
| 永住 · 现在能做什么（行 38/40/41） | `221.3 → 252.1` | ✅ 27.5s |
| 访日客 · 提前准备（行 40–43） | `155.1 → 179.2` | ✅ 21.5s |
| 永住 · 三条要件（行 7/10/13） | `27.0 → 74.1` | ❌ 跨 47s，不做卡 |

`itemAt` 各条的入场秒数 = `timeline.json` 里对应解说行的 `startMs` − overlay 的 `at`。
**卡面正文必须是解说的压缩版（≤14 字，超了直接报错）**，照抄那一行等于让观众读两遍同样的字。

**图表数据由人填，不要自动提取**——稿件里的数字是散文式的，自动提取一定会错。
LLM 只标位置和建议图型，`series` 留空，`validateScene.ts` 会硬拦截空数据。

**2026-08-17：从 `../fx-kit` 移植了 13 个特效**（交叉溶解 / 横扫切换 / 闪白打点 /
逐字打出 / 关键词变红 / 三级字幕 / 遮罩上滑 / 故障标题 / 地图亮点 / 左右对照 /
聚光引导 / 颗粒漏光 / 箭头注记）。fx-kit 是无头浏览器实现的，本管线禁用浏览器，
搬的是规格不是代码 —— 对应关系、被迫改掉的四处、以及哪 7 个早就有对应物，
都写在根目录的 `FX-PORT.md`。

三类形态要分清：

| 形态 | 有哪些 | 怎么用 |
|---|---|---|
| 面板场景 | 三级字幕 / 遮罩上滑 / 故障标题 / 左右对照 / 地图 | 跟别的场景一样，`scale` `x` `y` 有效 |
| **全幅**场景 | 闪白 / 聚光 / 箭头注记 / 颗粒漏光 | 铺满底片、不画衬底，`scale` `x` `y` **无效**；坐标是 0–1 归一值 |
| 转场预设 | `crossfade` `whip` `whipRight` `flash` | storyboard 里 `"transition": "whip"` |

两个体积上的坑：**颗粒几乎不可压缩**（80 秒的 demo 铺 14 秒颗粒 4MB→13.6MB，
铺满 52 秒→36MB），要整片常驻就得接受体积翻几倍；地图那 46 条 path 会进每一帧
SVG，渲染比别的场景慢一截。

**哪句话该上哪个特效**，见 `video-pipeline/CLAUDE.md` 的「触发标准」一节：
每个特效的词面信号 + 附加条件，以及哪些能光看稿件判、哪些位置能看出来但内容必须人给。

完整规矩见 `video-pipeline/CLAUDE.md`。

### 首次使用要做的两件事

管线交付时文件是拍平的，而源码 import 的是 `./util/ffmpeg`、`./layers/layerA`
这种嵌套路径。已按它自己的 import 恢复成 `src/` + `src/util/` + `src/layers/`，
并补了 `package.json`（必须显式 `"type":"commonjs"` —— 父项目是 ESM，
而管线的 import 不带 `.js` 后缀，ESM 解析不了）和 `tsconfig.json`。

```bash
cd video-pipeline && npx tsc -p tsconfig.json && node dist/build.js doctor
```

本机 ffmpeg 是 gyan.dev full 版但**不带 librsvg**，所以走 `sharp` 降级
（管线自己的可选依赖，doctor 里就写着 `npm i sharp`，不是禁令里说的绘图库）。

### 动效怎么加到现有片子上

底片先出干净的（**script.json 里不要有任何自建的动效字段**），再叠加：

```bash
cd video-pipeline
node dist/build.js overlay-onto <底片.mp4> --spec <overlays.json> --crf 23 --out <成片.mp4>
```

- `--crf 23` 要显式给，**管线默认 18**，不给的话体积会翻一倍（实测 102MB → 163MB）
- 横竖两版各写一份 spec：竖版只改 `scale`（0.88~0.92）和 `y`，内容一样
- `at` / `duration` 从 `timeline.json` 对应解说行的起止取，覆盖「念这段的那几句话」
- 动效入场会铺满整个 `duration`。20 秒的窗口意味着最后一个节点要到 14 秒才进来 ——
  想要利落就把 duration 收短

---

## 二、改了什么，要重跑哪一步

这张表是「快速出片」的关键 —— 大部分调整**不需要**重跑全流程。

| 改动 | 要重跑 | 时间 |
|---|---|---|
| `script.json` 的 `text`（屏幕字，不动 `speech`/`pauseAfterMs`） | `render` | 10 分钟 |
| 标题（`title`） | `render` | 10 分钟 |
| `hookLayout`（换开场版式） | `render` | 10 分钟 |
| `.env` 的 `VG_CRF` / `VG_PRESET`（体积） | `render` | 10 分钟 |
| `src/lib/ass.ts`（字幕排版） | `render` | 10 分钟 |
| 补了 `assets/manual/` 的素材 | `footage` + `render` | 10 分钟 |
| `assets/bgm.mp3`（加背景乐） | 删掉 `voice_mix.m4a` 再 `render` | 10 分钟 |
| `speech` / `pauseAfterMs` / 增删行 | `all`（tts 起） | 15 分钟 |
| `.env` 的 `VG_VOICE` / `VG_RATE` | `all` | 15 分钟 |
| `assets/lexicon.json`（读音） | `all` | 15 分钟 |

只出一版可以省一半时间：`--profile portrait`。

注意 `voice_mix.m4a` 有缓存：它比 `voice.mp3` 新就直接复用，所以单纯改字幕重渲不会
重新混音。**换了 BGM 或改了混音参数必须手动删掉它**，否则改动不生效。

---

## 三、稿件约定（记者读稿风格）

README 里写的是默认的纪实解说风格（12–22 字短行、多 stack 组）。
今天这条走的是「记者读稿」——连贯播报，跟默认风格是冲突的，约定不一样：

- **一句话一行，20–31 字**，从句不拆行。句读交给标点在同一次 TTS 合成里完成，
  跨行才是断点。要连贯就别拆行
- `pauseAfterMs`：段内 80–120，句末 200–260，段落切换 450
- **全片只留 1 个 chip、1 组 stack**。多了就散，播报感就没了
- 其余全部连读

### 怎么写才没有「AI 味」

音色只解决一半，另一半在稿子上。这几种写法一出现，人一耳朵就听出是机器写的：

- **套话连接词**：「值得注意的是」「不仅…而且」「综上所述」「随着…的发展」
- **排比堆砌**和**形容词叠加**
- **结尾升华喊口号**
- 每段结构一模一样

换成人说话的方式：短句、有主语、敢用「你」、用「但」「问题在于」「说白了」这类
口语连接；结尾落在一个具体事实或一句克制的判断上。
2026-08-16 那两条片子的稿子都是按这个写的。

### 音色与语速（实测净语速）

Edge TTS 的 zh-CN 只有四个音色能用，女声只有两个：

| 音色 | 性别 | 官方标签 | 语速 | 实测净语速 |
|---|---|---|---|---|
| `zh-CN-YunyangNeural` | 男 | News | `-2%` | **179 ms/字** |
| `zh-CN-XiaoxiaoNeural` | 女 | News/Novel・Warm | `-5%` | **210 ms/字** |
| `zh-CN-YunjianNeural` | 男 | 纪实旁白腔 | `-4%` | 197 ms/字 |
| `zh-CN-XiaoyiNeural` | 女 | Cartoon・Lively | — | 没用过 |

**女声选 Xiaoxiao，不要选 Xiaoyi** —— Xiaoyi 是 Cartoon/Lively 标签，那个活泼调子
恰恰是「AI 味」最主要的来源。Xiaoxiao 带 News 标签，配 `-5%` 约 286 字/分，
接近人正常讲解的语速。

估时长：`字数 × 单字秒数 + 停顿合计`。两条片子实测误差都在 1–2%。

### chip 必须带具体信息

角标占着画面最好的位置，写「访日客数」「失业率」这种**类目名等于什么都没说**。
竖线右边要给数。

- 好：`2026年\n上半年 | 访日客2108万人\n同比 -2.0%`
- 坏：`2026年8月\n上半年 | 访日客数`

右边最多两行、每行 12 字以内。超了竖版会自动缩字，缩多了就看不清。
这条已经写进 `src/steps/script.ts` 的 `SYSTEM`，新写的稿子会自动遵守。

### 开场标题卡是自动的

不用写进 `lines`。`buildAss()` 从 `title` 按逗号拆关键词，做成盖在第一个镜头上的
大字卡，停 2.6 秒，角标顺延到它之后。四套版式由**标题哈希**选，不满意就在
`script.json` 顶层加 `"hookLayout": 0`（0–3）重跑 `render`。

**标题就是封面文案**，写标题的时候按封面来写：两段、每段 7–12 字、逗号分隔。

---

## 四、出片前检查（机器能查的）

渲染结束时管线自己会查两条，不过还是列出来知道它在防什么：

- `assertAudioContinuous()` —— 音轨逐包检查，有空洞直接报错
- 成片时长必须装得下整条音频，短了报错

自己再看三处：

1. **第 0 帧**：应该是完整的标题卡（各平台默认拿它当封面）
   `ffmpeg -i portrait.mp4 -vf "select=eq(n\,0)" -frames:v 1 f0.jpg`
2. **随机抽 3 帧看字幕有没有出画**，竖版尤其
3. **听开头 10 秒和结尾 10 秒** —— 掐头掐尾是最容易出问题的地方

---

## 五、发片前检查（只有人能查的）

模型能把结构和节奏搭好，**事实核查必须人来做**。今天这条的清单：

- 具体数字、日期、人名 —— 逐个对源稿，源稿是单一来源的还要对最新官方表述
- 法令条文号、机构全称
- 免责/风险提示有没有漏
- `credits.txt` 贴进视频简介（Pexels 的 API 使用指南要求署名）

---

## 六、注意事项（都是踩出来的）

### 1. 混音必须单独跑一遍 ffmpeg

`loudnorm` 和 `libx264` 放进同一次 run **会整块丢音频**：成片里出现 3.00 秒的空洞，
播放器表现为「画面卡住、声音消失几秒然后跳过去」，而**时长和文件大小全都正常**，
肉眼查不出来。丢的位置取决于视频内容，横版竖版还不一样。

已由 `buildAudioMix()` 修掉（音频单独一遍，视频那遍只 `-c:a copy`），
`assertAudioContinuous()` 兜底。**不要为了省事把它合回一次 run。**

自查命令：

```bash
ffprobe -v error -select_streams a -show_entries packet=pts_time -of csv=p=0 x.mp4 \
  | awk -F, 'NR>1{d=$1-prev; if(d>0.03) print "空洞 " prev " 缺 " d} {prev=$1}'
```

### 1 之二、纯视觉行的 pauseAfterMs 不能写 0

`speech: ""` 的行（chip、stack 组的首行）靠 `pauseAfterMs` 决定停留时长。
**写 0 会生成一段 0 秒静音** —— 产物是个 333 字节、只有帧头没有音频帧的 mp3，
ffmpeg 的 concat 读到它**静默截断整条人声**，退出码 0、日志照常打印总时长。

2026-08-17 就这么出了一版：视频 295.4 秒、音轨只有 124 秒，后三分之二没声音，
两版底片 + 两版成片全渲完才发现。

已经修掉了（`tts.ts`）：

- 0 秒的段根本不生成、也不进 concat
- 拼接后实测时长与逐行累加对不上超过 0.5 秒**直接抛错**，并提示去
  `ffprobe voice/*.mp3` 找读不出时长的那一段

原来那行写的是 `totalMs: Math.max(cursor, actual)` —— 截断被逐行累加盖住了，
这是它能一路蒙混到成片的原因。**自查一条命令**：

```bash
ffprobe -v error -show_entries stream=codec_type,duration -of csv=p=0 成片.mp4
# video 和 audio 的时长必须一致
```

### 2. ASS 的透明度不能按公式推

整帧压暗用绘图矩形 + `\1a`。按 `(255-a)/255` 算，`&H72&` 应该是 55%，
**实拍帧量出来只有 37%** —— libass 实际压出来约是名义值的六成。
现在用的 `&H30&` 是实测 55%。要调就改完渲一帧量亮度：

```bash
ffmpeg -ss 1.0 -i x.mp4 -frames:v 1 \
  -vf "crop=400:300:0:0,signalstats,metadata=print:key=lavfi.signalstats.YAVG" -f null -
```

### 3. `\n` 有两种写法，两种都会遇到

`script.json` 里的换行，模型有时写 `"\\n"`（字面量两个字符），有时写 `"\n"`（真换行符）。
真换行符直接落进 ASS 会把一个 Dialogue 撕成两行，**整条字幕报废**。
`toAssBreaks()` 和 tts 的 spoken 归一都已经兜住两种，但改这块代码时别把它改回去。

### 4. 竖版字幕跟横版不是同一套系数

竖版 sub 用 **1.15** 的字号系数，不是 profile 的 1.45（1.45 的话一行只放得下 12 个字）。
左右边距按同一系数缩放，**折行算法和 ASS 样式必须共用这个系数** —— 两边不一致，
折出来的行照样出画。

### 5 之前：clip 关键词怎么写（2026-08-17 出过事）

「关键词写画面不写概念」只是底线，还有三条：

| 规矩 | 反面教材（2026-08-17 的《牛来》片） |
|---|---|
| **不要为具体的人检索** | 解说「他六十多岁的母亲孙丽芳」，clip 写了 `older woman hands closeup`，拿回来一个**西方白发女性正面肖像** |
| **不要用跨行业歧义大的术语** | 解说「导演一人身兼数职」，clip 写了 `3d modeling software screen closeup`，拿回来**牙科口扫软件**（界面上写着 Orthodontics） |
| **找不到贴切的就用宽泛的** | 空影院 / 城市夜景 / 屏幕光 / 键盘 / 走廊，重复用两三次都行 —— 宁可平淡，不要离谱 |

那条片子 50 个关键词里，**22% 是人物类、20% 是专业术语类**，出事的正好是这两类。

人物镜头的风险是质变不是量变：画面主体一旦是人，观众会立刻把 TA 认成稿子里说的那个人。
物件和环境没这个问题 —— 一台电脑、一间影院，观众只当氛围。

### 5. Pexels 对日本本土题材的命中率很差

关键词写画面不写概念是基本要求，但即便这样，日本本土的检索结果经常是**别的国家**。
今天 `narita airport arrival hall` 拿到的是曼谷素万那普机场（画面里有泰文招牌），
就出现在开场的硬新闻段。

**这一步是硬检查，不是可选项。** 2026-08-17 那条片子只抽了 6/50 帧，
而且抽的都是有动效的点位，恰好避开了普通镜头，上面两个离谱素材就这么漏出去了。

对策：出片后逐个镜头抽帧看一眼，不对的手动补到
`assets/manual/<关键词_下划线分隔>.mp4`（竖版专用命名 `<关键词>_portrait.mp4`），
重跑 `footage` + `render`。手动素材优先于检索。

### 6. 体积

`.env` 的 `VG_CRF`，默认 23（3 分半约 77 / 85 MB，3.2 Mbps）。
各平台上传后都会二压，超过 3 Mbps 基本是白给。要更小换 26（再省一半），
代价是密集人群在暗部会看出块状。**渲染耗时不受 CRF 影响。**

### 7. 别动 `preset`

`medium` → `slow` 只省 3% 体积却翻倍耗时。

---

## 七、快速起片模板

```bash
# 1. 写稿
npm run vg -- script --topic "选题" --note "记者读稿风格，一句一行 20-31 字，全片 1 个 chip、1 组 stack"

# 2. 人工审稿：改 projects/记者读稿/<日期_标题>/script.json
#    重点看：标题（=封面文案）、chip 有没有带数字、数字对不对

# 3. 先只做配音，听一遍
npm run vg -- tts "<项目名>"

# 4. 素材 + 渲染
npm run vg -- footage "<项目名>"
npm run vg -- render  "<项目名>" --profile both

# 5. 出处
npm run vg -- credits "<项目名>"
```
