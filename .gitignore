# ── 依赖与构建产物 ──
node_modules/
dist/
build/
.cache/
.smoke/

# ── 密钥 ──
# .env 里有真的 PEXELS_API_KEY，永远不要提交。
# 要加新变量先加到 .env.example（只写键名和占位符）
.env

# ── 生成的媒体 ──
# 成片、音轨、配音分句、BGM —— 全部可以重新跑出来，一共 16G 多，不进版本库。
# 复现方法写在各条线的手册里（joke-video/儿童故事出片方案.md、shuoshu/出片手册.md）
*.mp4
*.mov
*.wav
*.mp3
*.m4a

# ── 生成的图 ──
# 逐镜静帧和场景图，跑一次 preview / scene 就有，加起来 101M
**/stills/

out/

# 素材缓存（Pexels 下载的空镜，2.8G）
assets/cache/
assets/manual/test_*.mp4

# ── 字体 ────────────────────────────────────────────────────────────
#
# 整个 fonts/ 162M，大头是 Noto Serif CJK（七个字重，可自由下载，见 fonts/README.md）。
#
# ⚠ **但出片真正依赖的那两个必须进版本库**：得意黑（字幕）和站酷快乐体（牌匾）。
# 两个都是 SIL OFL，可自由分发，加起来 3.4M。
# 不提交的话，别的机器上 `FONT_FILES` 那条 `existsSync` 过滤掉它们，
# resvg **静默回退到系统字体** —— 字幕规范 §二 专门警告过这个坑：
# 不报错，你只会觉得「字怎么没变」。
fonts/*
!fonts/README.md
!fonts/ZCOOLKuaiLe-Regular.ttf
!fonts/ZCOOLKuaiLe-OFL.txt
!fonts/smiley-sans-v2.0.1/
fonts/smiley-sans-v2.0.1/*
!fonts/smiley-sans-v2.0.1/SmileySans-Oblique.otf
!fonts/smiley-sans-v2.0.1/OFL.txt

# ── 成品目录 projects/ ──
#
# 2026-08-20 重整：四条线的成品从各自目录（joke-video/projects、shuoshu/projects、
# zhiyu/projects、根 projects）收拢到 `projects/<类型>/`，代码留在原地。
#
# **绝对不要在这儿写 `/projects/` 把整棵树排除掉。** 那是重整前的写法，
# 当时根 projects/ 只有记者读稿那一条线。现在四条线都在里面，一刀切会连
# 方案.md、发布文案.md、script.json、幕*.md 一起吃掉 ——
# 而且**父目录一旦被排除，子目录的 .gitignore 想再放行也放行不了**，这个坑实测踩过。
#
# 大件靠上面的媒体后缀规则挡（*.mp4 *.wav *.png 之类），
# 下面只列各线特有的、后缀挡不住的中间产物。

# 记者读稿：逐帧素材和渲染中间件，12G，全部可重跑
projects/记者读稿/*/stills/
projects/记者读稿/*/out/
# 渲染的中间件（ffmpeg 的 concat 清单、分段片），跟成片一起重跑就有
projects/记者读稿/*/voice/
projects/记者读稿/*/work_landscape/
projects/记者读稿/*/work_portrait/

# 段子与儿童故事：预览页的公用素材（角色图/场景图）。
# **这些是入库的** —— 重整前就在版本库里，别顺手加规则把它们排除掉：
# 画廊页直接引用它们，没有的话 index.html 是一堆裂图。

# 说书：场景图
projects/说书/*/scenes/

# 心理洞察：片内画面（每 30 秒一张 + 题句卡淡入淡出那几张，E01 96 张 7.9M）+ concat 清单
# **补于 2026-08-21**：这条线开线时漏了这两条，治愈和说书都有。
# E01 那批图一直等在待提交列表里，没人注意到 —— 文字版一上来量还会更大。
projects/心理洞察/*/成片/*/scenes/
projects/心理洞察/*/成片/*/_concat.txt

# 禅佛典：片内画面 + concat 清单，跟治愈/心理洞察同一套（E01 146 张 14M）
# 开线时又漏了一次 —— 每开一条线记得回来补这两行。
projects/禅佛典/*/成片/*/scenes/
projects/禅佛典/*/成片/*/_concat.txt

# 治愈：片内画面（一景不换、每 30 秒一张，两期 81 张 15M）+ concat 清单
projects/治愈/*/成片/*/scenes/
projects/治愈/*/成片/*/_concat.txt

# 老石 2D 口播：逐帧人物图（一条 49 秒的片子 1465 张、201M）＋ 配音包络。
# 帧是 `node shi-2d/shoot.mjs <项目目录>` 重跑就有的，包络是 3d-render/envelope.mjs 算的。
# **稿子.txt / 动作表.json / 字幕.ass 要入库** —— 那三份是稿，不是产物。
projects/老石/*/frames/
projects/老石/*/frames-probe/
projects/老石/*/envelope.json
# 2026-09-06 补：build.mjs 每次现画的垫图（暗底/舞台）、--静帧 的调试帧、
# concat 清单（mp3 本身被全局 *.mp3 挡着，单留清单没用）、汇总页
# ⚠ **vo/manifest.json 要留** —— 那是时间轴（每句 start/duration），不是产物：
# 配音重跑一次时长就变了，没有它对不回当初那一版
projects/老石/*/_*.png
projects/老石/*/静帧/
projects/老石/*/vo/list.txt
projects/老石/index.html

# 旧选题的留档素材（27M），文档留着、素材不留
/history/**/*.png

# ── 交付包 projects/醒木不响/ ──────────────────────────────────────────
#
# `npm run deliver` 生成的**硬链接视图**：一个包 = 一次上传。
# 包里的 mp4 / png / srt / 发布文案 都是别处那份的硬链接，
# **在这儿再跟一遍等于同一份内容占两个路径**。
#
# ⚠ **整棵树都不跟，包括目录结构。** 2026-08-23 定的：这是本地产物，不进版本库。
# 上一版只放行 `来源.txt`，理由是「目录结构本身是账本（哪个包排在哪一档、发没发），
# 跟老马线的排期树一个道理」—— 那个类比不成立：**老马那棵树里躺着成片本身**，
# 而这儿每个包都是别处文件的硬链接，账本那一层在源目录里已经有了。
projects/醒木不响/

# ── 音波层的帧缓存 ──
# 二十分钟一期约 2 万张 640×150 的小 png（去重之后，一张 1.7K，合计 30M 上下）。
# 每次出片先清空再重建，**是中间产物不是交付物**：删了照样跑得出来。
# 目录名带下划线跟 _concat.txt 一个规矩：下划线开头的都是给 ffmpeg 看的中间文件。
**/_wave/

# 高总英语口播（第九条线）：出片产物 811M，进库的只有 13M（代码 ＋ 档案原件）。
# 又是开线时要补的那两行 —— 这次连着别的一起写清楚。
#
# 逐帧人物图（649 张 118M，`node gao-2d/出帧.mjs` 重跑就有）
projects/高总/_帧/
# 汇总页的插图（42 张 2.4M，`node gao-2d/index.mjs --force` 重渲）
projects/高总/_图/
# 汇总页本身（跟老石那条线同一个判据：重跑就有）
projects/高总/index.html
# 试音产物
projects/高总/_试音/
# ffmpeg 中间件：分段片、叠加序列、字幕、concat 清单
projects/高总/*/_tmp/
# 配音包（mp3/wav 被全局后缀挡着，这行挡的是目录里的其余中间件）。
# ⚠ **时间表不在这儿** —— 它在 `gao-2d/稿件/<slug>/时间表.mjs`，是稿不是产物
projects/高总/*/audio/
# 封面（`node gao-2d/封面.mjs` 重抽就有）
projects/高总/*/*_cover.png
projects/高总/*/*_cover_1x1.png
#
# ⚠ **下面这些是要进库的，别顺手加规则排除掉**：
#   `<slug>/media/*.pdf` 和 `media/*.png` —— **一手档案原件和圈红截图，9.4M**。
#   方案 §13 要求「来源无误则保留完整证据链」，而政府 PDF 会改版、会下架，
#   删了是真的跑不回来。这跟 `_素材/`（可替换的通用空镜）不是一类东西。
#   `<slug>/发布文案.md` —— 是稿不是产物。
#
# 共用库 `_素材/` `_音乐/`：**媒体文件不进库，账本进库**。
# 这两个目录是外部拿来的原件，回不来 —— 所以**要单独备份**，只是不进版本库。
projects/高总/_素材/**/*.jpg
projects/高总/_素材/**/*.jpeg
projects/高总/_素材/**/*.png
# 素材库的验证图（2.4M）：`_头动.png` `_表情.png` `_对照.png` 这些全是
# 头.mjs / 画.mjs / 看.mjs 跑一条命令就出的演示表，README §怎么跑 里写着命令。
# 下划线开头＝产物，跟老石那条线同一个惯例。
# ⚠ **svg/ 那 12 张要进库** —— 那是美术给的原件，删了整条线跑不出来。
gao-2d/素材库/_*.png
