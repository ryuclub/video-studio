# 空镜风 + PPT 面板 —— 方案

在现有的空镜解说风成片上，选 2–3 个点盖一块 PPT 版式的面板，
素材视频继续在底下走。**不是第三种风格，是给空镜风加一个元素。**

参考：现成片 `1:56–1:59` 的 stack 效果（白字直接浮在素材上）。
面板要解决那个效果的短板 —— 那三秒的字压在一把浅色椅子上，发飘。

---

## 一、蒙版做法：跟封面同一块

已定：**全幅暗场蒙版，透明度用封面标定过的 `\1a&H30&`（实测压暗 55%）**，
面板文字用空镜风自己的配色（白字 + 正红 `&H002222EE`），不搬 PPT 的米白。

理由：片头 0:00–2:6s 已经有一块一模一样的蒙版了。中途再出现同一块，
观众读到的是「同一套视觉语言又出现了一次」，而不是「贴进来一张别的东西」。
米白卡片浮层在冷调素材上会像截图贴上去。

样张已渲（`三条时间线` 那一屏盖在法条书架的素材上），文字清晰、素材还看得见纹理。

---

## 二、放哪三处

时间点取自现成片 `projects/2026-08-16_你4月交的永住申请，可能已经在新规则里/`。

### ① 三条时间线 —— 必做

- **位置**：第 17–19 行，**94.4–111.7s**（约 17 秒）
- **版式**：`timeline`，三个节点 `2026.04 / 2026.10 / 2027.04`
- **为什么**：这是全片唯一一处素材完全无能为力的信息。三个日期靠听记不住，
  靠空镜更记不住。做面板的收益在这里最大。

> **一个要先定的取舍**：稿件里这三条是按「2027年4月 → 10月1号 → 今年4月」讲的，
> 而时间轴按时间排。要做逐点点亮，节点会跳着亮。
> - **(a) 改稿成时间顺序** —— 效果最好，但要重跑 TTS + 素材 + 渲染（约 15 分钟）
> - **(b) 三个节点一次全亮，只把正在讲的那个标红** —— 不用改稿、不用重跑 TTS，
>   只重渲（10 分钟）
>
> **建议 (b)**。这一版的稿子已经定了，不值得为一个动效重跑整条链路。

### ② 配偶者特例 3年+1年 → 5年+3年 —— 必做

- **位置**：第 13–14 行，**71.2–78.0s**（约 8 秒）
- **版式**：`compare`，左「现行 婚姻3年 / 在留1年」右「改定案 婚姻5年 / 在留3年」
- **为什么**：纯数字的前后对比，念一遍记不住，看一眼就懂。8 秒刚好。

### ③ 四种情况要留神 —— 建议做

- **位置**：第 30–31 行起，**156–168s**（约 12 秒，不覆盖完整的四段解说）
- **版式**：`list`，四条一次列出，随解说逐条变亮
- **为什么**：这是全片唯一「别人没讲」的部分，值得给一个记忆锚点。
- **注意**：四段解说完整讲完要 34 秒，**面板只盖前 12 秒**，
  列完四条就淡出，后面回到纯素材逐条展开。

**备选**（想换掉③的话）：「补缴 ≠ 按期缴」那条，第 40 行附近，`statement`，8 秒。
全片最反直觉的一条，但信息量不如四种情况。

**蒙版总时长 ≈ 37 秒 / 4分52秒 = 12.7%。**
三处是上限 —— 再多就变成「PPT 片配了点空镜」，空镜风的意义就没了。

---

## 三、技术怎么接

**核心结论：`render.ts` / `footage.ts` / `tts.ts` 一行都不用动。**
面板本质上就是多几条 ASS 事件，所以改动只发生在字幕层。

| 文件 | 改动 |
|---|---|
| `src/lib/slides.ts` | 版式函数加一个 `theme: 'paper' \| 'overlay'` 参数，overlay 用白字+正红 |
| `src/lib/ass.ts` | `styleHeader` 加 `Panel` 样式；`buildAss` 末尾追加面板事件 + 锚点校验 |
| `src/types.ts` | `Script` 加可选 `panels?: Panel[]` |
| `script.json` | 顶层加 `panels` 数组 |

面板的时间锚定，跟 PPT 风的 `anchor` 同一套思路，但空镜风是逐行时间轴，更简单：

```json
"panels": [
  { "fromText": "它不是一刀切从某天开始，而是分了三条时间线。",
    "spanLines": 3, "layout": "timeline",
    "kicker": "这次改定最容易被漏掉的", "title": "三条时间线",
    "items": ["2026.04", "2026.10", "2027.04"],
    "captions": ["收入要件\\N追溯适用", "改定日\\N收入要件施行", "日语·制度理解\\N配偶者特例"] }
]
```

- `fromText` 必须**唯一命中一行**，命中 0 次或多次直接报错（跟 PPT 风的锚点校验同款）
- `spanLines` 是覆盖几行，面板从该行起点淡入、到第 n 行终点淡出
- 用行文本而不是行号 —— 行号在改稿后必然错位

**共存规则**（写稿时保证，代码里加校验）：

- 面板期间底部 `sub` 照常显示，面板版式必须避开底部那一条
- 面板**不能跟 `stack` 组或 `chip` 时间重叠** —— 都在画面中部，撞上就是一团糊
- 第 ① 处（94–112s）跟 `stack` 组（115.3–119.8s）刚好错开，不冲突

---

## 四、代价与工作量

**每次改面板只需重跑 `render`（10 分钟），不碰 TTS 和素材。** 这是这个方案最大的好处 ——
面板是纯字幕层的东西，调版式、改文案、换位置都不需要重新配音或重新下素材。

| 项 | 估时 |
|---|---|
| `slides.ts` 加 overlay 主题 | 2 小时 |
| `ass.ts` 接面板 + 锚点校验 + 重叠检查 | 2 小时 |
| 三处面板的内容与调版（含竖版重排） | 2 小时 |
| 重渲 + 抽帧验证 | 30 分钟 |

合计**约一天**。竖版的 `timeline` 转纵向、`compare` 转上下堆叠，PPT 风那边已经有现成实现，直接复用。

**要留意的一条**：蒙版期间画面基本等于消失。单块别超过 15 秒 ——
第 ① 处的 17 秒已经在边界上，真做出来觉得闷的话，把它压到 12 秒（只盖第 17–18 行）。

---

## 五、跟已有两套东西的关系

- **空镜风成片**：这就是给它加元素，稿件和配音完全不动
- **PPT 风独立版**（`..._ppt` 目录）：保持不动，两者是不同产品 ——
  一个是「视频里插三张图」，一个是「整片都是图」。版式代码共用，产出各自独立
