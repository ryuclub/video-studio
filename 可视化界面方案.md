# 可视化界面方案 + 制作计划

> ⚠ **已并入 [`可视化方案.md`](可视化方案.md)（2026-08-26）。本份只作留档，别照它做。**
>
> 三份文档写同一件事已经打起架来了：页面 6 页 vs 5 页、工作量 44 vs 31.5 vs 19.5、
> 落地顺序 11 步 vs 7 期、架构两份 —— **三个真相在等着分叉，而且已经分叉了**。
> 冲突点逐条列在合并方案 §〇，取哪一边也写在那儿。


> **这一份是「长什么样」和「怎么做」。**
> 「为什么这么定」在 [`项目封装方案.md`](项目封装方案.md)（现状盘点 · 参数化 · 四件交互的可行性）；
> 写稿台／审片台／五种稿件形态的正文在 [`网页版方案.md`](网页版方案.md)（2026-08-22 定稿）。
> **三份各管一段，不互相抄。**
>
> **总账：31.5 人日**，分七期。**剪辑和多用户挂起，不在这本账里**（§八）。

---

## 一、一句话与四条边界

**把仓库已经有的能力接出来，让人能点、能看、能听、能确认 —— 管线一行不改。**

| 不做 | 为什么 |
|---|---|
| **一键从需求直接到成片** | 长片渲 **485 秒**、说书一期 10–21 分钟。按钮可以有，但**人工关卡挡在前面** |
| **通用旋钮** | 六条线的参数不通用。每个下拉框的选项**必须是仓库自己的能力** |
| **界面改共享音色 / 基础库** | 一个人改了，**所有人的片子都变音**（`cast:check` 那条纪律） |
| **第一刀写盘到 `projects/` 和 `jokes/`** | 写错目录是**数据损坏**，不是麻烦（`zhiyu-ep.ts` 原话） |

---

## 二、页面清单

| # | 页 | 干什么 | 写盘吗 |
|---|---|---|---|
| **P1** | **风格台** | 场景画风墙 ＋ 封面版式墙。**挑，不改** | ❌ |
| **P2** | **素材台** | 字体 / 音色（含试听）/ 视频素材 / 环境音 / 画面素材（含 rig 上传） | 只写 `uploads/` |
| **P3** | **制作台** | 风格 → 四栏 → 封面预览。**参数在这儿定** | 只写 `.ui-*.json` |
| **P4** | **工作台** | 竖进度条 ＋ 日志 ＋ 产物 ＋ **人工关卡** | 触发管线 |
| **P5** | **库** | 稿件库 / 视频库（三视图，扫盘） | ❌ |

**P1 P2 P5 全是纯读**，所以它们排在前面 —— 档期跑着也能做（§七）。

---

## 三、每页的界面方案

### P1 · 风格台

```
┌ 场景画风 ────────────────────────────────────────────────┐
│ [段子·一页故事 10] [老马短片 10] [老马长片 6] [说书][治愈]│
│  ▣grass  ▣room   ▣office ▣street ▣abstract ▣shore       │
│  ▣hillside ▣hillrocks ▣pond ▣eaves                      │
│  ── 老马长片 ──                                          │
│  ▣office_dawn ▣office_day ▣corridor ▣dinner              │
│  ▣street_dusk ▣office_night                              │
└──────────────────────────────────────────────────────────┘
┌ 封面版式 ────────────────────────────────────────────────┐
│  一行·右下签   一行·脚边签 ★缺省   ｜ 方版 [横排][竖排★] │
│  ▣210px  ▣200px   ← **主位是缩略图**，大图次位            │
│  代价：署名跟着角色走，攒不出台标 ← 每一档都显示 cost     │
└──────────────────────────────────────────────────────────┘
```

- 场景数据：`npm run scene -- <name>` ／ `longform.cjs` 的 `SCENES`（可 import）
- 版式数据：`LAYOUTS`，**连 `cost` 字段一起显示** —— 版式库里每一档都必填代价
- ⚠ **说书／治愈／心理／禅佛典的场景跟稿件绑，不能空跑** ——
  每条线要一份**只为出图用的样例稿**。不给的话那四格是空的，
  **而空白比「没有这个功能」更让人以为是坏了**

### P2 · 素材台

| 标签 | 内容 | 数据源 |
|---|---|---|
| **字体** | 10 个文件 170 MB，每个渲一行样字 | `fonts/` ＋ `FONT_FILES` |
| **音色** | 24 个基础 ＋ 58 条预设，**点即试听 ＋ AB 对比** | `cast.ts`、`voice-clone/presets.ts`，**并 `:5178` 进来** |
| **视频素材** | 手动 16 条 ＋ Pexels 缓存 269 条 | `assets/manual/`、`assets/cache/` |
| **环境音** | 9 条，**显示实测电平**（差 20+ dB） | 仓库根 `voice/*.mp3` |
| **画面素材** | rig / 摆件 / 符号，**＋ 上传 SVG** | `horse/`、`_symbols/`、`uploads/rigs/` |

⚠ **两条必须显示在界面上的状态**：

1. **`ffmpeg` 缺 `rubberband` → 变声静默降级**，试听到的跟出片出来的**不是一个人**。
   `doctor.ts` 已经在查，**界面要把它挂在音色标签的顶上**
2. **共享音色只读。** 界面上**只有「基于 XX 新建」，没有「编辑」**

**rig 上传**：`rig.svg` ＋ `manifest.json`（viewBox / headCx / depth / eyeClamp / 双眼中心 / 口型中心）

| checker 查（可判定） | 判据 |
|---|---|
| viewBox 四个数 | 正则 |
| 五个 `#mouth-*` 齐 | id 存在 |
| 默认只显示 `mouth-closed` | 其余四个带 `display:none` |
| `#eye-white-l/r` `#pupil-l/r` 齐 | id 存在 |
| 外接框裁紧 | 渲一张扫非透明像素 |
| **口型切换真的有变化** | 分渲 `closed` / `A`，**比 PNG 字节数** |

⚠ **checker 查不了那六个支点对不对** —— 眼珠飘不飘、扭头穿不穿帮。
**上传完立刻用它渲一张场景预览让人看**（P1 那面墙）。
这是「机器查可判定的、人看不可判定的」那条原则的又一次落地。

### P3 · 制作台

```
┌ 风格（三级联动）──────────────────────────────┐
│ [老马 ▾] › [长片 ▾] › [横版 ▾]                │  ← 定了它，下面全部填好默认值
└───────────────────────────────────────────────┘

┌ 文案 ───────┐┌ 画面 ────────┐┌ 音频 ───────┐┌ 字幕 ───────┐
│ 主题/选题    ││ **画面来源**▾││ 音色 ▾ 24    ││ 字体 ▾   C  │
│ ✦AI 生成文案 ││ ·系统勾勒 ✓  ││   试听 ▷     ││ 字号/色/描边 C│
│ 稿件（可贴） ││ ·自动抓取    ││ 语速（线定）  ││ 标点规则 ▾ C│
│ ✦AI 出关键词 ││ ·提供素材    ││ 环境音 ▾ 9   ││ 断行 ▾    C │
│ clip（英文） ││ 场景 ▾       ││ BGM / 电平   ││ 安全区  A 🔒│
└──────────────┘└──────────────┘└──────────────┘└─────────────┘

┌ 封面（实时，<0.3s）──────────────────────────┐
│ 版式档 ▾  ▣210px ▣200px  ｜ ▢16:9 ▢1:1       │
│ ✓字数 ✓高度 ⚠压到脸 ✓安全区 ✓不剧透说破段    │
└──────────────────────────────────────────────┘
```

**每个控件带一个级别标记**（这是这一页的核心）：

| 级 | 标记 | 界面行为 |
|---|---|---|
| **A · 平台事实** | 🔒 | **灰掉**，跟着选的平台变，不跟用户变 |
| **B · 物理事实** | ⚠ | 能改，**越界必报**（字号下限 109、对比度 3.2、静场 ≤1.5s） |
| **C · 频道预设** | 无 | 随便改，**但偏离要留痕** |

⚠ **偏离默认值时做两件事**（缺一不可）：
1. **把那条规范的原话显示出来**（一句话，不是整节）
2. **落进 `<项目>/.ui-overrides.json`** —— 出片后回看得到「这一期为什么跟别的不一样」

> 理由：这仓库每条规范都带着「为什么」（绳结被切掉、引号被删光、字距边只剩 5%），
> **界面上一个下拉框不会带那段理由。**

### P4 · 工作台

```
体检 ✓  →  配音 ✓  →  试听 ▷  →  静帧 ▣  →  封面 ▣  →  [出片]
                       ↑人确认    ↑人确认    ↑人确认      ↑三格没点＝灰
     每步：[重跑] [日志] [产物]        日志 SSE 实时滚
```

- 步骤来自**步骤登记表**（`steps.ts`），不是界面硬编码
- `gate` 三档：`auto` ／ `blocking`（退出码非零就停）／ `human`（人点过才解锁）
- ⚠ **「出片」是「从当前这一步往下跑」，不是「从头一键到底」**

### P5 · 库

稿件库 / 视频库两个视图，同一次扫盘。缩略图直接用现成封面 png，
带体检徽章、时长、体积、有没有发布文案。**媒体服务必须支持 Range 206。**

---

## 四、三个跨页机制

### 4.1 默认值三层

```
平台包（共享只读）   platforms/youtube.json · 抖音 · B站 · 微信
        ↓
频道包（用户自己的） channels/醒木不响.json   ← 现在散在代码里的常量搬进来
        ↓
稿件（单期覆盖）     jokes/<id>.json 的约 100 个字段  ← **已经有了**
```

⚠ **覆盖链已经是三段了**（`代码常量 → 环境变量 → 稿件字段`），
**缺的只是把最前面那段换成「从频道包读」—— 不是重构，是把常量搬个家。**

### 4.2 人工关卡

`试听` `静帧` `封面缩略图` 三格是 `human`。**没点过，出片按钮是灰的。**
这是 `RUNBOOK` §五「只有人能查的」从文档变成界面的那一下。

### 4.3 字节级回归

**参数化最大的风险不是做不出来，是搬完之后片子不一样了而且没人发现。**

```bash
npm run regress -- --record    # 建基线（老马长片001 / 老马短片 / 说书E05 / 治愈taoan-E01）
npm run regress                # 每一步做完跑一次
```

**判据：频道包填现在的值，重跑已出片的期，产物逐字节一致。**

---

## 五、架构

```
浏览器  Vite + React + TS
  │  REST（数据）+ SSE（日志）
  ▼
web/  :5180   ← 独立 workspace，加进根 package.json 的 workspaces
  ├─ preview/   封面（import 拿 Buffer）· 场景（spawn / import）
  ├─ assets/    素材扫描 · rig checker · uploads
  ├─ steps.ts   ← 从 joke-video/src/steps.ts import，不抄
  ├─ runner.ts  spawn tsx，单并发，日志落盘 + SSE
  ├─ gates.ts   人工关卡，落 <项目>/.web-state.json
  └─ media.ts   mp4/wav，**Range 206**
  ▼
现有管线   ← **单向依赖：管线永远不 import web/**
```

**四条隔离纪律（可验证）：**

| # | 纪律 | 怎么验 |
|---|---|---|
| 1 | 管线不 import `web/` | `grep -rn "from '.*web/" src/ joke-video/src/` **必须为空**。删掉 `web/`，出片一行不受影响 |
| 2 | 默认值只读不抄 | web 里出现硬编码的 `150` `0.062` `#FFD400` 就是 bug |
| 3 | 前端依赖只进 `web/package.json` | React/Vite 不出现在根和 `joke-video` 的依赖里 |
| 4 | CLI 永远可用 | **网页坏了不影响出片 —— 这是它能被信任的前提** |

---

## 六、制作计划

### 6.1 任务表（去重后的唯一一张）

| # | 任务 | 人日 | 依赖 | 碰管线吗 |
|---|---|---|---|---|
| T1 | 封面预览（实时 ＋ 版式档可换 ＋ 四尺寸） | 2 | — | ❌ |
| T2 | 场景画风预览（26 个具名场景 ＋ 四条线的样例稿） | 1.5 | — | ❌ |
| T3 | 素材台（字体／视频／环境音／画面素材） | 2 | — | ❌ |
| T4 | 音频试听（并 `:5178` 进来 ＋ 环境状态条） | 1.5 | — | ❌ |
| T5 | **回归基线**（选 4 期锁 md5，`npm run regress`） | 0.5 | — | ❌ |
| T6 | 步骤登记表（导出 2 条 ＋ 补 4 条 ＋ **逐条验证**） | 3.5 | — | ⚠ 只导出 |
| T7 | Skill（SKILL.md ＋ 三份跨线 reference） | 2 | T6 | ❌ |
| T8 | 拆开 B 级／C 级常量（`L` 那类对象） | 1 | T5 | ✅ |
| T9 | 频道包 schema ＋ 从常量导出并断言一致 | 1 | T8 | ✅ |
| T10 | 代码改成读频道包（常量留 fallback） | 2 | T9 · T5 | ✅ |
| T11 | 音色两层（基础库只读 ＋ 频道表继承） | 1.5 | T9 | ✅ |
| T12 | 平台包（遮挡区／比例／时长） | 1 | T9 | ✅ |
| T13 | 风格 → 默认值包（三级联动 ＋ ABC 分级） | 3 | T9 · T12 | ❌ |
| T14 | 四栏制作台（只读 ＋ 预览 ＋ 偏离留痕） | 3 | T13 · T1 | ❌ |
| T15 | 工作台（进度条 ＋ 人工关卡 ＋ 出片） | 3 | T6 | ❌ |
| T16 | 上传 rig（checker ＋ manifest ＋ 预览接线） | 3 | T8 · T2 | ✅ |
| T17 | 库（稿件库／视频库，扫盘 ＋ Range 206） | 2.5 | — | ❌ |
| | **合计** | **31.5** | | |

### 6.2 分期

| 期 | 内容 | 人日 | **验收判据** |
|---|---|---|---|
| **一 · 看得见** | T1 T2 | **3.5** | 页面上换封面版式档能实时看到 210／200；26 个场景铺成一面墙，四条不能空跑的线也有图 |
| **二 · 素材** | T3 T4 T17 | **6** | 六条线的素材在一个视图里；点音色即试听；**rubberband 缺失会显示在顶上** |
| **三 · 底座** | T5 T6 T7 | **6** | `npm run regress` 绿；六条线的步骤能机读、`verified` 字段如实；skill 能路由 |
| **四 · 参数化** | T8 T9 T10 T11 T12 | **6.5** | **回归全绿 —— 这是唯一判据**。频道包填现值，四期产物逐字节一致 |
| **五 · 制作台** | T13 T14 | **6** | 选风格 → 四栏默认值全填好；改任一项 → 落 `.ui-overrides.json` 并显示规范原话 |
| **六 · 工作台** | T15 | **3** | 三个 `human` 关卡没点过，出片按钮是灰的 |
| **七 · 上传 rig** | T16 | **3** | 传一个新 rig → checker 逐条报 → 立刻渲一张场景预览 |

### 6.3 顺序为什么是这个

1. **期一、期二不碰管线一行代码** —— 档期跑着也能做（缓冲只有三周，§七）
2. **T5 回归基线必须在 T8 之前** —— 没基线就不敢动那些常量
3. **T16 排最后**，因为它依赖 T8 的「支点搬家」——
   **它不是一件交互，是一次参数化，只是长着交互的样子**
4. **T6 登记表一份数据两个消费者**（skill 的 `references/lines.md` ＋ 工作台的进度图），
   所以 T7 和 T15 都挂在它后面

### 6.4 ⚠ 三条排期纪律

| # | 纪律 | 理由 |
|---|---|---|
| 1 | **档期日（周二／周四／周日）不装依赖、不动管线脚本** | `npm install` 那天炸了会连累出片 |
| 2 | **界面不排渲染队列**，只做纯读 ＋ 秒级预览 | ffmpeg 吃满 CPU，抢了档期就迟发 |
| 3 | **每完成一个碰管线的任务（T8–T12 T16），立刻跑 `npm run regress`** | 这条管线大多数错误不报错，只是结果不对 |

---

## 七、挂起（列入计划，不在这 31.5 里）

| 项 | 触发条件 |
|---|---|
| **剪辑**（导入 / 时间轴裁切 / 转场） | **要先有代理预览**（360p·6fps）。现在「渲完才能看」，长片 485 秒 —— **不解决预览，剪辑界面是摆设** |
| **转场** | 六条线现在一个都没有，节奏靠停顿和换镜做的。**加之前先问它替代什么** |
| **多用户** | 等 T5 回归基线 ＋ T10 频道包跑起来，再动 `paths.ts` 那一处。⚠ `projects/` 分租户会动到所有已出片目录 |
| **写稿台**（LLM 写稿 / 局部重写 / 锁段 / 四条填空） | 正文在 [`网页版方案.md`](网页版方案.md) §三，约 24 人日。**等制作台跑顺再上** |

---

## 八、风险

| 风险 | 大小 | 怎么顶 |
|---|---|---|
| **参数化搬完，片子悄悄变了** | **高** | T5 回归基线，逐字节验 |
| **规范每周在变，界面的默认值变旧** | **高** | §五 纪律 2：只读不抄，默认值全部 import 自代码 |
| **把只在文档里的规矩做成控件** | 中 | **判据：能被 `*-check.ts` 查的 → 控件；只在文档里的 → 只显示原话** |
| **闸被配成可关，退化成天天加 `--anyway`** | 中 | 闸的**阈值**可配、**存在与否**由频道包声明，**不给「这次忽略」** |
| **T6 登记表低估** | 中 | 六分之二已是代码（`ship.ts` ／ `shuoshu-ship.ts`），**导出不手抄**；`verified` 字段如实标 |
| 界面挤占档期 | 低 | §6.4 三条 |
