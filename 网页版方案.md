# 网页版方案：写稿台 + 审片台

> ⚠ **已并入 [`可视化方案.md`](可视化方案.md)（2026-08-26）。本份只作留档，别照它做。**
>
> 三份文档写同一件事已经打起架来了：页面 6 页 vs 5 页、工作量 44 vs 31.5 vs 19.5、
> 落地顺序 11 步 vs 7 期、架构两份 —— **三个真相在等着分叉，而且已经分叉了**。
> 冲突点逐条列在合并方案 §〇，取哪一边也写在那儿。


> **两根柱子，不是一根。**
> 左半边是**写稿台**：选类型 / 填需求出稿 / 贴现成稿 / 逐段改到能发 / 先看封面。
> 右半边是**审片台**：体检 → 试听 → 逐镜 → 出片，把人工关卡收进界面。
> 管线一行不改，网页层是外壳。
> 全量约 **44 人日**；建议的第一刀是 **30 人日**。

---

## 一、三件不能绕开的事实

### 1. 管线是重本地的，搬不上云

96 处 ffmpeg 调用、本机 `Noto Sans SC Black`（缺了不报错只是变细）、
resvg 原生模块、几个 G 的 `assets/cache/`、一版片子 10–21 分钟。
所以网页版是 `localhost` 服务。

**这仓库已经有先例**：`voice-clone/src/server.ts`（117 行，:5178 试听页）、
`preview.ts` 生成的 258KB 静态画廊。新服务是把这两个收编，不是从零起。

### 2. 管线全是 CLI + 文件产物，所以加壳比重写便宜一个数量级

6 条线、58 个 ts 文件、约 2.8 万行，每一步都是
`tsx src/xxx.ts --ep xxx` 吃文件吐文件，没有常驻状态。
网页层只需 spawn 子进程、推日志、扫产物。
**不把管线改造成可调用的库** —— 要动 96 处 ffmpeg 和 58 个入口，收益低风险高。

### 3. 库不需要数据库

`projects/<类型>/<日期>_<id>/` 已经是事实来源。
稿件库 / 视频库 / 音频库是同一次扫盘的三个视图，mtime 缓存即可。
**不引入 sqlite** —— 库和盘一分叉就得写同步，而磁盘永远是对的那一边。

---

## 二、视频类型：选、推荐、先看封面

### 2.1 「视频类型」在这仓库里是**三层**，不是一个下拉框

| 层 | 选什么 | 影响什么 |
|---|---|---|
| **① 线** | 记者读稿 · 段子 · 一页故事 · 说书 · 治愈 · 心理 · 禅佛典 | 管线脚本、出片目录、音色、`cpm`、体检脚本、封面版式 |
| **② 线内子类型** | 段子的 `type: A / B`；记者读稿的 `style`（空镜解说 / PPT 演示）+ `hookLayout`；治愈线的单期 / 上下分期 | 稿件字段，不换管线 |
| **③ 版式** | `--profile portrait / landscape / both`；封面 9:16 / 3:4 / 1280×720 / 1400 方版 | 只影响渲染和封面尺寸 |

> **①里是七项，但管线只有六条。** 段子和《一页故事》共用 `joke-video` 那条管线、
> 共用 `jokes/` 一个平目录、共用 `preview.ts` 那个画廊（理由见 `paths.ts` 顶上）。
> 它们在**内容上是两种**、在**技术上是一条**。
> 选择器按内容给七项（用户想的是「我要做哪种片子」），
> 底下映射到六条管线 —— 这个映射写在步骤登记表里，不写在界面上。

界面上这三层必须分开，而且**①定了②③的选项才成立** ——
选了治愈线就没有 `type: A/B`，选了段子线就没有「上下分期」。
做成一个扁平的下拉框，迟早会出现「治愈线 + type A」这种不存在的组合。

### 2.2 类型推荐：规则在前，LLM 只做兜底

判据大多是**可数的**，不需要 LLM：

| 信号 | 怎么量 | 指向 |
|---|---|---|
| 台词汉字数 | 实测：段子 A 型 59 · 一页故事 267–310 · 说书 E04 4612 · 治愈《枕草子》7859 | 数量级直接分线 |
| 有没有多角色对白 | 「某某说：」点名 / `characters[]` | 段子 · 一页故事 · 说书 |
| 有没有 `##` 分幕 | 四幕 + 每幕 700–900 下限 | 说书 |
| 有没有 `⏸` | 停顿标记 | 治愈 · 心理 · 禅佛典 |
| 古文直引 | 引号包起来的文言 | 说书 · 心理 · 禅佛典 |
| 古今顺序 | 古在前现实作锚点 / 现代在前古典作解释层 | 禅佛典 ↔ 心理（**这两条正好相反**） |
| 一句一行 20–31 字 | 行长分布 | 记者读稿 |

⚠ **推荐器必须跟体检用同一个字数口径。**
这仓库里「稿件字数」至少有三个意思：脚本集里故事本体的字数、
台词汉字数、md 全文汉字数（含注释和元数据）。
实测 `page-01-turtle.json`：**台词 267 汉字，但整份 json 里的中文字符是 1322**。
推荐说「够 4000 字，走说书」而体检说「不够」，比不推荐更糟。
所以推荐器**直接复用 `*-check.ts --json` 的计数**，绝不自己数一遍。

**推荐界面不给一个答案，给一张表。** 每条线一行，附「差多少」：

```
✅ 说书        4612 字 → 约 19:47    够（4000 起），四幕都过 700/800/800/900
⚠ 治愈系      4612 字 → 约 20:24    够单期；但这条线的规格是上下两期，7000+
⚠ 心理洞察    有古文引句，但古今顺序是「古在前」，更像禅佛典
✗ 一页故事    4612 字               差得远（实测区间 267–310）
✗ 记者读稿    没有一句一行 20–31 字的结构
```

LLM 只判两件数不出来的：**古今顺序**（禅佛典 vs 心理）和**题材调性**，
而且**只在规则给不出唯一答案时才调**。规则能定的事不花钱、不等待、可复现。

### 2.3 封面预览：这条比想象的便宜得多

四个封面生成器（`cover.ts` 449 行 · `shuoshu-cover.ts` 509 · `zhiyu-cover.ts` 508 ·
`xinli-cover.ts` 477）**全是 SVG → resvg → PNG，不碰 ffmpeg，不从 mp4 抽帧**。

`video.ts` 里有实测数据：单帧 1080×1920 光栅化 **240ms，其中 99.6% 是 resvg，
拼 SVG 只占 1ms**。封面 1280×720 像素更少。**一张不到 0.3 秒** ——
所以封面预览可以做成**改标题就重渲**的实时预览，不是「点一下等一会儿」。

**而且自查件是现成的规范动作，不是新加的功能**：

```
cover/全/upload-1280x720.png       投稿用
        first-frame-1920x1080.png  片头第一帧
        square-1400x1400.png       微信方版
        check-320.png              ← 缩略图自查
        check-square-200.png       ← 同上
```

`xinli-cover.ts` 顶上写着：「**motif 定稿前必须缩到 320px 看一眼**。
大图好看小图认不出等于没有」，后面记着两次翻车 ——
井口第一版被看成气球，竹简第一版像相机镜头。

**所以封面预览页的主位不是 1280×720 那张大图，是 320px 那张缩略图**，
旁边并排放同一条线已出片的封面缩略图。
`zhiyu-cover.ts` 的原话是「要在一屏十二个视频里被点开」——
那就在页面上真摆十二个，挑得出来才算过。大图放次位。

顺带三条硬约束由界面守住（代码里已有断言，网页只是把它显性化）：
右下角 240×100 留空（YouTube 时长角标压在那里）、不放人脸箭头红圈、元素不超过三个。

⚠ **这个障碍 2026-08-26 复核过，比原来写的小得多。** 原文说「这四个脚本顶层就跑
`resolveEp(process.argv)`，只能 spawn」—— **六个封面脚本里只有一个是这样**：

| 脚本 | 能不能 import | 要不要 `--out` |
|---|---|---|
| `xinli-cover.ts` | ❌ 顶层 `resolveEp(process.argv)`（第 34 行）**只能 spawn** | 要 |
| `cover.ts` | ✅ 纯库，`cli.ts` 调它 | 不用（不自己写盘） |
| `shuoshu-cover.ts` · `zhiyu-cover.ts` | ✅ 有 `if (process.argv[1]?.includes(…)) main()` 守卫 | 要 |
| `yt-cover.ts` · `laoma-long-cover.ts` | ✅ 同上，而且**导出 `renderYt()` / `renderLongCover()`，直接返回 Buffer** | **不用** |

所以「四个脚本各加一个 `--out`」缩成「**一个 spawn ＋ 两个加 `--out`**」，
另外两个（yt / 长片）连盘都不用落 —— 封面台可以直接 import、拿 Buffer 回内存。
**长片那一档还自带版式库**（`LAYOUTS`）和 1:1 方版，正好是「实时预览换一档看看」要的东西。

---

## 三、写稿台

### 3.1 六条线的稿件是**五种**形态，不是一种

| 形态 | 线 | 长相 | 落在哪 |
|---|---|---|---|
| **A** 结构化 JSON | 记者读稿 | `lines[]`：`text` / `style` / `speech` / `pauseAfterMs` / `clip`。约 50 行 | `projects/记者读稿/<项目>/script.json` |
| **A′** 结构化 JSON（更重） | 段子 · 一页故事 | 再加 `characters[]`（rig / cast / side / rate）、`say[].delivery`、`bgm` / `cues`、`type: A/B` | `joke-video/jokes/<id>.json` |
| **B** 散文 markdown | 说书 | `##` 切幕，「某某说：」点名对白，头部一段元数据（字数 / 选角 / 说破 / 诊断词） | `shuoshu/<id>.md` |
| **C** 多文件 markdown | 治愈 | `幕表.md`（配额表）+ 四份 `幕N-xx.md`（`⏸` 标停顿）+ `样章-幕一.md` | `projects/治愈/<项目>/` |
| **C′** 单文件 markdown | 心理 · 禅佛典 | `正文.md` + `方案.md`。**比治愈线简单，没有幕表和样章** | `projects/<类型>/<项目>/` |

**一个通用富文本框搞不定这五种。** 编辑器分形态：

- **A / A′ → 表格式编辑器。** 一行一句，字段是列，不是让人在 JSON 里手改。
  `style` 是下拉、`clip` 带素材预览、`delivery` 和 `cast` 从 `cast.ts` 拉选项。
- **B / C / C′ → 文本编辑器 + 右栏活体检。** `⏸` 渲染成可见的分隔条，
  文言直引高亮，`##` 生成幕导航。

C 形态多一件事：**幕表是配额表**（字数 / 时长 / `⏸` 数），现在是手写的。
网页版实时算 —— 字数变了时长跟着变，读 `zhiyu-lines.ts` 里的 `cpm`
（治愈 226 / 心理 258 / 禅佛典 248，说书实测 233.7）。
分幕字数下限是硬约束（说书 700/800/800/900，`shuoshu-preflight.ts` 会拦），
**边写边看数字够不够，比写完被体检打回来强**。

### 3.2 三个入口

```
┌─ 填需求 ────────────┐  ┌─ 贴稿件 ──────────┐  ┌─ 从旧稿起 ──────┐
│ 选类型 + 选题 + 要点 │  │ 粘贴 / 拖文件      │  │ 复制一份已有的  │
│      ↓ LLM          │  │   ↓ 解析 + 类型推荐 │  │      ↓          │
└──────────┬──────────┘  └─────────┬─────────┘  └────────┬────────┘
           └───────────────────────┴─────────────────────┘
                              ↓
                统一编辑器（按形态分 A / A′ / B / C / C′）
                              ↓
              右栏三层校验 · 局部重写 · 封面预览 · 版本
                              ↓
                     定稿 → 写进管线吃的位置
```

**「填需求」的 prompt 素材是现成的**，而且很厚：

| 用途 | 文件 | 大小 |
|---|---|---|
| 硬约束（必进 prompt） | `zhiyu/治愈系稿件规范.md` | 15.7 KB |
| 选题库（检索一条进 prompt） | `zhiyu/治愈系旁白书目_选题稿件.md` | 28 KB |
| 同上 | `zhiyu/心理洞察向_书目与稿件.md` | 74 KB |
| 同上 | `zhiyu/禅佛典向_小故事大道理_书目与稿件.md` | 48 KB |
| 出片约定 | `shuoshu/出片手册.md` · `joke-video/儿童故事出片方案.md` | 16 / 14.5 KB |

⚠ **不能整份塞进去。** 规范是硬约束，全文进；书目是选题库，
按选中的那一本检索出对应段落再进。记者读稿那条线的 SYSTEM 现在
**硬编码在 `src/steps/script.ts` 里**，要抽成 `web/prompts/记者读稿.md`，
让 prompt 变成能在页面上改的东西 —— 否则调 prompt 还得改代码重启。

### 3.3 改稿：**局部重写，不是整篇重生成**

这是整个写稿台最重要的一条，理由在仓库里已经写着了：

> `projects/治愈/2026-08-20_makura/幕一-春是拂晓.md` 开头 ——
> 「开头 637 字是样章-幕一.md 的原文，**一字未改** —— 那是听过并通过的调性和节奏。」

**整篇重生成会把已经通过试听的部分冲掉。** 所以改稿的动作是这样的：

| 动作 | 交互 |
|---|---|
| **局部重写** | 选中一段 → 写一句「哪儿不对」→ 只重写这一段 → 出 3 个候选，diff 对照三选一 |
| **锁段** | 标记「这段试听通过了」，任何重写都不许碰。样章那 637 字就该一进来就锁上 |
| **提为落点** | 选中一句 → 挪到幕尾。对应规范里那条「发现要从材料里长出来，不要外加」—— 好句子通常已经写出来了，只是埋在中间当过场 |
| **版本** | 每次 LLM 改动落一版，可回滚、可 diff。不覆盖 |
| **手打** | 直接改。永远保留，LLM 只是可选项 |

### 3.4 右栏是三层，不是一个「通过 / 不通过」

规范本身就是三层的，界面照抄它的结构：

| 层 | 出处 | 谁来查 | 界面 |
|---|---|---|---|
| **① 文本闸** | 规范第一节：叹号、问句、编辑记号 | 机器，实时 | 红波浪线，划到哪句标哪句 |
| **② 数值** | 段长、`⏸` 密度、分幕字数、字数→时长 | 机器，实时 | 右栏数字，出区间变黄。**可能是对的**，所以是提醒不是拦截 |
| **③ 人味儿** | 规范第十一节「四条硬判据」 | **只有人** | 四个**填空题**，见下 |

第三层是这套东西跟「AI 写稿工具」的分界线。规范里写死了：
「**答不上来就是没写好，不是风格问题**」，「这一条机器基本查不了」。

所以不做成四个复选框（复选框会被随手勾掉），做成**四个必填的填空**：

```
① 具体到不能替换的那个人和事，是哪一个？   [____________________]
② 听众带走的那句话，抄在这儿：             [____________________]
③ 你自己也没想通、并且说出来了的那一处：   [____________________]
④ 只可能出自这个人的那一句：               [____________________]
```

**四格填不满，「定稿」按钮是灰的。** 填的内容存进 `<项目>/.web-state.json`，
出片后回看的时候还能对着片子问一遍当初填的对不对。

### 3.5 定稿：写盘这一下要小心

按线路写到不同位置（A → `script.json`，A′ → `jokes/<id>.json`，
B → `shuoshu/<id>.md`，C → 项目目录下一组 md，C′ → `正文.md`）。

⚠ **覆盖前必须备份。** `zhiyu-ep.ts` 顶上原话：
「忘了改哪一个，那一步就把新书的产物写进方丈记的目录 —— **这是数据损坏，不是麻烦**」。
网页版比命令行更容易点错线路和期号，所以：写盘前先把旧文件挪进
`.web-versions/<时间戳>/`，并且**新建项目时期号冲突要拦，不给默认值、不猜**。

---

## 四、审片台

`ship.ts` 顶上自己写着，机器做不了写稿和试听。
出片纪律写着「**build 是交付动作不是调试手段**」。

所以审片台不是一个「生成视频」大按钮 —— 那会让人拿八分钟的渲染当调试。
它是一条竖进度条，每步三档 `gate`：

- `auto` —— 跑完就往下
- `blocking` —— 退出码非零就停（体检 / preflight / cast:check）
- `human` —— 人在界面上点过确认才解锁（试听 / 逐镜静帧 / **封面缩略图自查**）

**没点「听过了」，出片按钮是灰的。** 这就是 RUNBOOK 第五节
「只有人能查的」从文档变成界面的那一下。

顺带把 RUNBOOK 第二节那张「改了什么要重跑哪一步」变成真的依赖图：
改 `text` 只亮 `render`，改 `speech` 从 `tts` 起全亮。
以及那个坑做成红点：`voice_mix.m4a` 比 `voice.mp3` 新就复用，**换了 BGM 必须手动删**。

---

## 五、架构

```
浏览器  Vite + React + TS
  │  REST（数据）+ SSE（日志）
  ▼
本地服务  web/  :5180   ← 新增，独立 workspace
  ├─ script/           写稿台
  │   ├─ adapters/     A · A′ · B · C · C′ 五个形态的 读 / 写 / 校验
  │   ├─ prompts/      每条线一份 md，页面上可改
  │   ├─ classify.ts   类型推荐：规则在前，LLM 兜底
  │   ├─ llm.ts        Anthropic SDK，复用根 .env 的 key
  │   └─ versions.ts   .web-versions/ 版本与回滚
  ├─ cover.ts          封面预览：spawn 四个 cover 脚本（带 --out）→ 临时目录
  ├─ steps.ts          步骤登记表 —— 唯一需要「懂业务」的新文件
  ├─ runner.ts         任务队列：spawn tsx，单并发，日志落盘 + SSE
  ├─ library.ts        扫 projects/ + jokes/ + shuoshu/ + zhiyu/
  ├─ gates.ts          人工关卡，落 <项目>/.web-state.json
  └─ media.ts          mp4/wav 服务，**必须支持 Range 206**
  ▼
现有管线  src/ · joke-video/src/ · voice-clone/src/   ← 只加两个 flag
```

### 步骤登记表

网页层其余部分都是通用代码，只有这份文件是知识：

```ts
{ line: '治愈', epFlag: '--ep', form: 'C', cpm: 226,
  steps: [
    { id:'体检', cmd:['joke-video/src/zhiyu-check.ts'],   gate:'blocking' },
    { id:'封面', cmd:['joke-video/src/zhiyu-cover.ts'],   gate:'human', hint:'缩到 320px 认得出吗' },
    { id:'配音', cmd:['joke-video/src/zhiyu-listen.ts'],  out:'listen/**/*.wav' },
    { id:'试听', gate:'human', hint:'调子飘没飘只有耳朵知道' },
    { id:'画面', cmd:['joke-video/src/zhiyu-scene.ts'],   out:'成片/*/scenes/*.png' },
    { id:'逐镜', gate:'human' },
    { id:'出片', cmd:['joke-video/src/zhiyu-video.ts'],   out:'成片/*/*.mp4' },
    { id:'发布', cmd:['joke-video/src/zhiyu-publish.ts'], out:'发布文案.md' },
  ]}
```

> ⚠ **这份表最容易低估。** 六条线的步骤名、参数、产物路径散在 RUNBOOK、
> 四份出片方案 md 和 58 个脚本的头注释里。抄进来容易，**逐条真跑一遍验证难**。

---

## 六、页面

| 页 | 内容 |
|---|---|
| **写稿台** | 选类型（三层）/ 三入口 → 分形态编辑器 → 右栏三层校验 → 局部重写 / 锁段 / 版本 → 定稿 |
| **封面台** | 实时预览四个尺寸。**主位是 320px 缩略图 + 同线已出片封面并排十二格**，大图次位 |
| **工作台** | 单个项目的竖进度条，每步「重跑 / 日志 / 产物」，日志 SSE 实时滚 |
| **稿件库** | 六条线的稿件一张表，带体检徽章（过 / 留意 / 错），点开进写稿台 |
| **视频库** | `projects/**/成片/**/*.mp4`。缩略图直接用现成封面 png。时长 / 体积 / 有没有 `发布.json`（＝发没发）/ 播放器 |
| **音频库** | `voice/` `listen/` `voice-final/` `ab-*/`。**把 voice-clone 的 :5178 试听页并进来**当「试音」标签 —— 音色预设、变声链、AB 对比都是现成的 |

---

## 七、工作量

单位是**熟悉这个仓库的人**的人日。不熟的乘 1.5。

### 一期 · 骨架与库 —— 15 人日

| 项 | 人日 |
|---|---|
| 服务骨架 + 任务队列 + SSE 日志 | 2 |
| 库扫描 + 三视图 API（mtime 缓存，无数据库） | 2 |
| 媒体服务（Range 206 / 缩略图 / 波形） | 1 |
| 视频库 + 音频库前端 | 3 |
| **步骤登记表（6 条线，逐条真跑验证）** | **3** |
| 工作台前端（进度条 / 日志 / 产物） | 2 |
| Windows 坑（中文目录、路径空格、CRLF） | 2 |

### 二期 · 写稿台与封面 —— 24 人日

| 项 | 人日 | 说明 |
|---|---|---|
| 适配层 A / A′ | 2 | 表格式编辑器，字段选项从 `cast.ts` 拉 |
| 适配层 C（治愈，多文件 + 配额表实时算） | 2 | 最复杂的一个 |
| 适配层 B / C′（说书 / 心理 · 禅佛典） | 2 | C′ 是单文件，比 C 简单 |
| `*-check.ts` 加 `--json` + 右栏三层呈现 | 3 | **侵入之一** |
| **类型选择三层 + 推荐器** | **2.5** | 规则复用 check 的计数口径，LLM 只兜底 |
| **封面预览** | **2.5** | 四个 cover 脚本加 `--out`（**侵入之二**）+ 实时重渲 + 320 并排 |
| LLM 写稿（prompt 抽成 md + 选题库检索） | 3 | 记者读稿的 SYSTEM 要从代码里抽出来 |
| 局部重写 + diff 三选一 + 锁段 + 版本 | 4 | 写稿台的核心，别省 |
| 四条硬判据填空 + 定稿闸 | 1.5 | |
| 定稿落盘 + 覆盖前备份 + 期号冲突拦截 | 1.5 | 写错目录是数据损坏 |

### 三期 · 关卡与依赖图 —— 5 人日

| 项 | 人日 |
|---|---|
| gate 状态机 + 人工确认落盘 | 2 |
| 「改什么重跑什么」依赖图 | 2 |
| 稿件库前端 + 跨线徽章 | 1 |

### 合计 **44 人日**

**但不建议一次做全六条线。** 建议的第一刀：

> 一期全做（15）
> ＋ 写稿台**只做两条线**（治愈 C 形态、段子 A′ 形态，约 10）
> ＋ 类型推荐（2.5）＋ 封面预览（2.5）
> ＝ **30 人日**

这两条覆盖了散文和结构化两种形态。剩下三种是它们的变体 ——
C′（心理·禅佛典）是 C 去掉幕表和样章，B（说书）是 C 合成单文件，
A（记者读稿）是 A′ 去掉角色和变声。**每条约 2 天增量。**
封面预览和类型推荐是跨线的，一次做完六条线全受用。

---

## 八、要说清的一个风险

写稿台能保证的是：**格式对、数值在区间、四条硬判据被逼着回答过**。
它**不能**保证稿子有人味儿。规范第十一节自己写着「这一条机器基本查不了」，
《已读不回》就是过了体检但味儿不对的样本。

设计上做了三件事去顶：**局部重写**（不冲掉已通过的部分）、
**锁段**（护住试听过的调性）、**四条填空**（不填不许定稿）。
但最后那关还是人。**网页版让改稿变快，不让判断变便宜。**

封面同理：机器能守住「右下角留空」「元素不超过三个」这类断言，
守不住「缩到指甲盖认不认得出」—— 所以那一格是 `human` 关卡，不是 `blocking`。

---

## 九、明确不做

| 不做 | 原因 |
|---|---|
| 云端渲染 / SaaS | 本地依赖搬不动 |
| 多人协作、权限 | 单人仓库，加进来只是负担 |
| 把管线重构成可调用的库 | 96 处 ffmpeg、58 个入口，收益低风险高 |
| sqlite / 任何数据库 | 磁盘永远是对的那一边 |
| 并发渲染 | ffmpeg 吃满 CPU，并发只会都变慢 |
| 一键从需求直接到成片 | 会把「build 是交付动作」的纪律冲掉 |
| 整篇重生成 | 会冲掉已经通过试听的段落 |
| 扁平的「视频类型」下拉框 | 会出现「治愈线 + type A」这种不存在的组合 |
| 推荐器自己数字数 | 跟体检口径分叉，比不推荐更糟 |

---

## 十、改动纪律

1. **管线只加两个 flag，不改逻辑**：`*-check.ts --json`、四个 `*cover*.ts --out`。
   两处都只加输出分支。
2. **网页层放 `web/`**，加进根 `package.json` 的 workspaces。
3. **路径仍然只有两处出处**：`joke-video/src/paths.ts` 和 `src/config.ts`。
   网页层从这两个 import，**不许再抄一份常量** ——
   `zhiyu-lines.ts` 顶上「抄两份的下场」说的就是这个。
   同理 `cpm` 从 `zhiyu-lines.ts` 取，`cast` 从 `cast.ts` 取，字数从 `*-check.ts` 取。
4. **cover 脚本只能 spawn，不能 import** —— 它们顶层就跑 `resolveEp(process.argv)`。
5. **CLI 保持可用。** 网页跑的是同一条命令，终端里照样能跑。
   网页版坏了不影响出片 —— 这是它能被信任的前提。

---

## 十一、落地顺序

1. **视频库一页**（3 天）。纯扫盘 + 播放器，不碰执行。
   拿它验证扫描规则、Windows 中文目录、值不值得继续。
2. **封面台**（2.5 天）。加 `--out`、实时重渲、320 并排十二格。
   **这一步性价比最高**：改动最小、每期都用得上、立刻能看见效果。
3. **写稿台，只做治愈线**（5 天）。C 形态最复杂，跑通了别的都好说。
   先做「贴稿件 + 编辑 + 活体检」，**LLM 写稿放在这一步之后** ——
   编辑和校验没跑顺之前，生成出来的东西没地方落。
4. 类型推荐接上（2.5 天）。这时候各线的 `--json` 计数已经有了，直接用。
5. LLM 写稿接上治愈线，含局部重写。
6. 铺第二条线（段子 A′），验证适配层抽象站不站得住。
7. 工作台 + 步骤登记表。
8. 关卡、依赖图、剩下的线。

---

## 十二、⚠ 那张「四栏表单 + 生成视频」的参考图，为什么不照做（2026-08-26）

素材：`E:\ryu\laoma\功能需求界面.png` —— 四栏（文案设置／视频设置／音频设置／字幕设置）
＋ 底部一个红色「生成视频」大按钮。那是 MoneyPrinterTurbo 那一类工具的形状。

**它跟这个仓库不是一种产品。**

| 那张图 | 这个仓库 |
|---|---|
| **素材拼接**：AI 出文案 → 按关键词抓 Pexels 库存视频 → 随机拼 → TTS → 烧字幕 | **逐帧画**：六条线各有自己的渲染器、rig、场景库、音色表、体检闸、封面版式库 |
| 参数是**通用**的（它对内容一无所知，所以只能给通用旋钮） | 参数是**线专属**的：`centerK` 只对长片封面有意义，`⏸` 只对治愈／禅佛典有意义，`_layer` 只对长片有意义 |
| 「视频来源／拼接模式／转场」 | **这三样这儿根本没有对应物** |
| 「字幕设置」是给人调的旋钮（字体／颜色／描边／背景） | **字幕是规范定死的**：「结尾不留标点」「句中句号换逗号」「长片不照搬」是频道规矩 —— 做成颜色选择器等于把规矩打开了 |

⚠ 而且底部那个「生成视频」大按钮正是 §九 明确不做的那一条：
**「一键从需求直接到成片」会把「build 是交付动作」的纪律冲掉。**
长片 4180 帧渲 **485 秒**，说书一期 10–21 分钟 —— 一个大按钮会让人拿八分钟的渲染当调试。

### 但那张图里有三样是真需求，而且本方案里都有

| 那张图给的 | 本方案对应 |
|---|---|
| 一屏能看见所有参数 | §六「工作台」竖进度条 |
| 改完立刻看到效果 | §2.3「封面台」实时预览（一张不到 0.3 秒） |
| 一个地方触发 | §四 gate 状态机 —— 但**是分步 + 人工关卡，不是一个大按钮** |

**结论：抄它的「一屏、即时、集中」，不抄它的「一个按钮到底」和「通用旋钮」。**

---

> **另见 [`项目封装方案.md`](项目封装方案.md)**（2026-08-26，已到 v2）：那份盘了六条线的现状、
> 封了 skill、并把界面按「素材库 · 风格库 · 画面来源 · 规范作为默认值」重新组织。
> 它**不重写这一份** —— 写稿台／审片台／五种稿件形态仍旧以这一份为正文。
>
> ⚠ **那份 v2 推翻了本文 §十二 的一个判断。** §十二 说参考图那种表单跟这仓库
> 「不是一种产品」，理由之一是「视频来源／拼接模式／转场这三样没有对应物」——
> **核实之后两样有**：`src/steps/footage.ts`（Pexels 检索 330 行，缓存 269 条 2.8 GB）、
> `assets/manual/<keyword>.mp4`（提供素材，命中即覆盖检索），
> 连「AI 根据文案生成视频关键词」都在 `src/steps/script.ts` 的 SYSTEM 里。
> **只有转场是真没有。** 结论改成：**照做那个形状，但每个下拉框的选项换成仓库自己的能力。**

> 界面与排期另见 [`可视化界面方案.md`](可视化界面方案.md)（2026-08-26，31.5 人日分七期）。
